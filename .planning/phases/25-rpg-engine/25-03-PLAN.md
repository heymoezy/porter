---
phase: 25-rpg-engine
plan: "03"
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - backend/src/services/bridge/routing-engine.ts
  - backend/src/services/scheduler.ts
autonomous: true
requirements: [RPG-01, RPG-02, RPG-03, RPG-04, RPG-05, RPG-06, RPG-07, MD-01, MD-02, MD-03, MD-04, MD-05]

must_haves:
  truths:
    - "After a dispatch completes, awardXP(agentId, 'dispatch') is called fire-and-forget inside logDispatch"
    - "After awardXP detects a level-up or star-up, regenerateMdFiles() is triggered within the same call chain"
    - "The scheduler runs recalculateStats() for all RPG-enabled agents every 5 minutes (background cache refresh)"
    - "A dispatch by an agent with agentId set results in xp increment in agent_rpg_stats within seconds"
  artifacts:
    - path: "backend/src/services/bridge/routing-engine.ts"
      provides: "logDispatch calls awardXP for agents with agentId set"
      contains: "awardXP"
    - path: "backend/src/services/scheduler.ts"
      provides: "Background RPG recalculation job every 5 minutes"
      contains: "rpg.*recalculate\|recalculateStats\|rpg_enabled"
  key_links:
    - from: "backend/src/services/bridge/routing-engine.ts:logDispatch"
      to: "backend/src/services/rpg-engine.ts:awardXP"
      via: "fire-and-forget call inside async IIFE after INSERT succeeds"
      pattern: "awardXP.*agentId.*dispatch"
    - from: "backend/src/services/scheduler.ts"
      to: "backend/src/services/rpg-engine.ts:recalculateStats"
      via: "scheduled job every 300 seconds"
      pattern: "recalculateStats\|rpg.*job\|rpg.*schedule"
---

<objective>
Wire the RPG engine into the dispatch lifecycle and background scheduler:
1. logDispatch in routing-engine.ts calls awardXP(agentId, 'dispatch'|'failed') after each dispatch
2. Scheduler runs a background RPG recalculation sweep every 5 minutes over all rpg_enabled agents

Purpose: Stats must update automatically after real usage — no manual trigger required.
Output: Two small additions to existing files, both fire-and-forget, zero dispatch latency impact.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-rpg-engine/25-01-SUMMARY.md
@.planning/phases/25-rpg-engine/25-02-SUMMARY.md

<interfaces>
<!-- logDispatch in routing-engine.ts — exact call site -->
<!-- File: backend/src/services/bridge/routing-engine.ts -->
<!-- Line ~576: self.logDispatch(decision, ctx, result).catch(() => {}); -->
<!-- logDispatch signature: async logDispatch(decision, ctx, result, agentMsgCtx?) -->
<!-- ctx.agentId: string | undefined — the agent that fired this dispatch -->
<!-- result.error: exists if dispatch failed -->
<!-- After INSERT into bridge_dispatch_log succeeds (line ~348), the async IIFE continues -->
<!-- Insert awardXP call AFTER the pool.query INSERT succeeds, BEFORE the agent_notes block -->

<!-- awardXP signature (from rpg-engine.ts Plan 01) -->
export function awardXP(templateId: string, event: XpEvent): Promise&lt;void&gt;
export type XpEvent = 'dispatch' | 'feedback' | 'specialty' | 'battle_won' | 'battle_lost' | 'chain' | 'failed'

<!-- scheduler.ts pattern for adding new periodic jobs -->
<!-- File: backend/src/services/scheduler.ts -->
<!-- Existing jobs use setInterval or a tick loop — read the file to find the pattern -->
<!-- Background jobs are fire-and-forget, errors caught and logged -->

<!-- recalculateStats signature (from rpg-engine.ts Plan 01) -->
export function recalculateStats(templateId: string): Promise&lt;RpgStats&gt;

<!-- SQL to get all rpg-enabled agent template IDs for batch sweep -->
SELECT id FROM agent_templates WHERE rpg_enabled = 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook awardXP into routing-engine.ts logDispatch</name>
  <files>backend/src/services/bridge/routing-engine.ts</files>
  <read_first>
    - backend/src/services/bridge/routing-engine.ts (lines 265-400 — the full logDispatch method)
    - backend/src/services/rpg-engine.ts (the file created in Plans 01+02 — verify import path)
  </read_first>
  <action>
Make a MINIMAL, targeted addition to logDispatch. Read the full logDispatch method first to find
the exact insertion point.

**New import at top of routing-engine.ts (check existing imports — add only if missing):**
```typescript
import { awardXP } from '../rpg-engine.js';
```
(path relative to `backend/src/services/bridge/` — goes up one level to `services/`)

**Insertion point:** Inside the async IIFE in logDispatch, immediately AFTER the successful
`await pool.query(INSERT INTO bridge_dispatch_log...)` call completes and BEFORE the
`agent_notes` block. Insert:

```typescript
// RPG: award XP for this dispatch (fire-and-forget, never blocks)
if (ctx.agentId) {
  const xpEvent = result.error ? 'failed' : 'dispatch';
  awardXP(ctx.agentId, xpEvent).catch(() => {});
}
```

The `result.error` check: look at the BridgeDispatchResult type in
`backend/src/services/bridge/types.ts` to confirm the field name. If the field is named
differently (e.g., `result.success === false`), adjust accordingly.

Key constraint: this call MUST be inside the async IIFE (fire-and-forget), never in the
synchronous outer function body. The pattern is already used for agent_notes — follow it exactly.

The `ctx.agentId` guard ensures we only award XP when a dispatch is attributed to an agent.
Anonymous dispatches (no agentId) are not awarded XP.
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "awardXP\|rpg-engine" backend/src/services/bridge/routing-engine.ts returns import and call site
    - grep -n "awardXP.*agentId\|ctx\.agentId.*awardXP" backend/src/services/bridge/routing-engine.ts returns 1 match
    - The awardXP call is inside the async IIFE (not in the outer function body)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>awardXP called fire-and-forget in logDispatch for every attributed dispatch, TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 2: Add background RPG recalculation sweep to scheduler</name>
  <files>backend/src/services/scheduler.ts</files>
  <read_first>
    - backend/src/services/scheduler.ts (read FULL file — understand existing job patterns before adding)
    - backend/src/services/rpg-engine.ts (verify export names)
  </read_first>
  <action>
Read scheduler.ts fully first to understand the existing job registration pattern. Then add a
new periodic job that recalculates RPG stats for all rpg-enabled agents every 5 minutes.

**New import at top of scheduler.ts (add only if not present):**
```typescript
import { recalculateStats } from './rpg-engine.js';
```

**New job — follow the exact pattern used by existing jobs in scheduler.ts.**

The job should:
1. Run every 300 seconds (5 minutes)
2. Query for all rpg-enabled agents:
   ```sql
   SELECT id FROM agent_templates WHERE rpg_enabled = 1
   ```
3. For each agent, call `recalculateStats(id)` sequentially (not parallel — avoid DB overload)
4. Log result: `[scheduler:rpg] recalculated stats for {N} agents`
5. Catch all errors per-agent — one failure must not stop the batch

Pattern to follow (adapt to match whatever pattern scheduler.ts uses for existing jobs):
```typescript
// RPG stats background refresh — every 5 minutes
async function runRpgRecalculation(): Promise&lt;void&gt; {
  try {
    const { rows } = await pool.query&lt;{ id: string }&gt;(
      `SELECT id FROM agent_templates WHERE rpg_enabled = 1`
    );
    for (const row of rows) {
      try {
        await recalculateStats(row.id);
      } catch (e) {
        console.error('[scheduler:rpg] failed for', row.id, e instanceof Error ? e.message : e);
      }
    }
    if (rows.length > 0) {
      console.log(`[scheduler:rpg] recalculated stats for ${rows.length} agents`);
    }
  } catch (e) {
    console.error('[scheduler:rpg] batch error:', e instanceof Error ? e.message : e);
  }
}
```

Register this job in the same way as other jobs in scheduler.ts — whether that's a tick-based
interval check, a setInterval call, or a job registry pattern. Match the existing convention
exactly. Do NOT use a different pattern from what's already in the file.

The 300-second interval (5 minutes) is appropriate: RPG stats do not need sub-minute freshness
for the cache. The awardXP hook in routing-engine.ts handles real-time XP; this job handles
full stat recalculation (the heavier computation).
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "recalculateStats\|rpg-engine\|runRpgRecalculation\|rpg.*recalc" backend/src/services/scheduler.ts returns import + job definition + registration
    - grep -n "300\|rpg.*interval\|rpg.*5.*min" backend/src/services/scheduler.ts returns the interval definition
    - npx tsc --noEmit exits 0
    - npm run build exits 0 (full compile check)
  </acceptance_criteria>
  <done>Background RPG recalculation job registered in scheduler, fires every 5 minutes, TypeScript clean, full build passes</done>
</task>

</tasks>

<verification>
cd /home/lobster/documents/porter/backend && npm run build 2>&1 | tail -5
systemctl --user restart porter-fastify && sleep 8
curl -s http://127.0.0.1:3001/health | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('status'), d.get('version'))"
</verification>

<success_criteria>
- awardXP called in logDispatch after every attributed dispatch (fire-and-forget, inside async IIFE)
- Background RPG recalculation job runs every 5 minutes in scheduler
- npm run build exits 0
- Service restarts cleanly and health check returns ok
- Dispatching via the Bridge with an agentId set results in xp update visible via GET /api/admin/agents/:id/rpg-stats
</success_criteria>

<output>
After completion, create `.planning/phases/25-rpg-engine/25-03-SUMMARY.md`
</output>
