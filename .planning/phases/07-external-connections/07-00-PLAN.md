---
phase: 07-external-connections
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - /tmp/test_conn07.py
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-05]

must_haves:
  truths:
    - "Test script exists and runs without import errors"
    - "Script returns exit 0 (SKIP/STUB) before features are built, then passes once features land"
    - "Tests cover the primary success criterion for each CONN requirement"
  artifacts:
    - path: "/tmp/test_conn07.py"
      provides: "Behavioral test stubs for CONN-01 through CONN-05"
      contains: "test_conn"
  key_links: []
---

<objective>
Create Wave 0 behavioral test stubs for all five Phase 7 requirements. This script runs as the automated verify command throughout the phase -- initially as stubs (exit 0 with SKIP markers), then as real tests once features land.

Purpose: The VALIDATION.md contract requires test scripts before any Wave 1 plan can claim automated verification. Without these, all verify blocks fall back to `npx tsc --noEmit` only, providing zero behavioral coverage. Phases 2, 4, 5, and 6 each had a XX-00-PLAN.md for this purpose.

Output: One Python test script at /tmp/ following the established Phase 2/4/5/6 test convention.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-external-connections/07-CONTEXT.md
@.planning/phases/07-external-connections/07-RESEARCH.md
@.planning/phases/07-external-connections/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONN-01 through CONN-05 behavioral test script</name>
  <files>/tmp/test_conn07.py</files>
  <action>
    Create `/tmp/test_conn07.py` following the established pattern from Phase 6 (`/tmp/test_trns02_health.py`).

    The script must contain these test functions:

    **test_connections_list()** (CONN-01):
    - GET /api/v1/connections returns 200 with a JSON array
    - Response contains `ok: true` and `data` is a list
    - Each connection has `id`, `provider`, `status` fields
    - SKIP if endpoint returns 404 (not implemented yet)

    **test_github_token_encrypted()** (CONN-01):
    - Query porter.db directly: `SELECT meta_encrypted FROM workspace_connections WHERE provider='github'`
    - If row exists with meta_encrypted=1, PASS
    - If no github row, SKIP (not connected yet)
    - If meta_encrypted=0, FAIL (security violation)

    **test_email_connection()** (CONN-02):
    - Query porter.db: `SELECT provider, kind FROM workspace_connections WHERE provider='email'`
    - If row exists, verify kind is 'oauth2'
    - SKIP if no email connection

    **test_calendar_sync()** (CONN-03):
    - Query porter.db: `SELECT COUNT(*) FROM calendar_events`
    - If table exists and has rows, PASS
    - If table doesn't exist, SKIP
    - Also verify calendar_events table has correct columns: id, connection_id, project_id, google_event_id, title, start_at

    **test_whatsapp_webhook_verify()** (CONN-04):
    - GET /api/v1/webhooks/whatsapp?hub.mode=subscribe&hub.verify_token=test&hub.challenge=test123
    - If server returns 'test123' as body, PASS (webhook verification works)
    - If 404, SKIP (not implemented yet)
    - If 403, also acceptable (wrong verify_token means endpoint exists and validates)

    **test_no_hardcoding()** (CONN-05):
    - Grep backend/src/ for literal '127.0.0.1' excluding config.ts -- must return 0 matches
    - Grep backend/src/ for '/home/lobster' excluding config.ts -- must return 0 matches
    - Grep backend/src/ for 'lobster-2026' -- must return 0 matches
    - SKIP if backend/src/ directory doesn't exist

    **test_missing_secret_error()** (CONN-05):
    - Import credential-crypto module check: verify that `encryptCredential` called without PORTER_SECRET throws an error containing 'PORTER_SECRET'
    - Implemented as a subprocess call to node: `node -e "require('./backend/src/lib/credential-crypto.js').encryptCredential('test')"` with PORTER_SECRET unset
    - If exit code is non-zero and stderr contains 'PORTER_SECRET', PASS
    - SKIP if file doesn't exist yet

    **test_calendar_route()** (CONN-03):
    - GET /api/v1/projects/test-project/calendar-events returns 200 or 404
    - If 200 with data array, PASS
    - If 404, SKIP (not implemented yet)

    **main():**
    - Run all tests, collect results
    - Support `--quick` flag: only run test_connections_list and test_no_hardcoding
    - Exit 0 if all pass or skip, exit 1 if any fail

    Use Python stdlib only (urllib, json, sqlite3, subprocess, os, sys, re). No pip packages.
    Auth helper: login via POST /api/v1/auth/login with admin/porter credentials, extract session cookie.
    DB path: /home/lobster/documents/porter/porter.db
    Base URLs: http://127.0.0.1:8877 (porter.py) and http://127.0.0.1:3001 (Fastify)
  </action>
  <verify>
    <automated>python3 /tmp/test_conn07.py</automated>
  </verify>
  <done>Test script exists, runs without import errors, exits 0 (all tests SKIP or PASS). Covers CONN-01 through CONN-05 behavioral requirements.</done>
</task>

</tasks>

<verification>
- `python3 /tmp/test_conn07.py` exits 0
- `python3 /tmp/test_conn07.py --quick` exits 0
- Script tests all 5 CONN requirements
</verification>

<success_criteria>
- Test script exists at /tmp/test_conn07.py
- Runs without import errors
- Exits 0 when features not yet implemented (SKIP mode)
- Validates correct behavior once features land
- Covers: connection list, token encryption, email connection, calendar sync, webhook verify, hardcoding grep, missing secret error, calendar route
</success_criteria>

<output>
After completion, create `.planning/phases/07-external-connections/07-00-SUMMARY.md`
</output>
