---
phase: 07-external-connections
plan: 10
type: execute
wave: 4
depends_on: [07-03, 07-07]
files_modified:
  - backend/src/routes/v1/connections.ts
  - frontend/src/modules/connections/ProjectConnectionsPanel.tsx
  - frontend/src/modules/projects/ProjectDashboard.tsx
  - frontend/src/modules/projects/CalendarEventsDisplay.tsx
autonomous: true
requirements: [CONN-03, CONN-05]
must_haves:
  truths:
    - "Calendar deadlines appear in the project dashboard timeline alongside milestones"
    - "GET /api/v1/projects/:id/calendar-events returns calendar events for the project"
    - "Project detail view includes a dropdown to override workspace connection per project"
  artifacts:
    - path: "backend/src/routes/v1/connections.ts"
      provides: "New GET /projects/:projectId/calendar-events route"
      contains: "calendar-events"
    - path: "frontend/src/modules/connections/ProjectConnectionsPanel.tsx"
      provides: "Per-project connection override dropdown"
      min_lines: 30
    - path: "frontend/src/modules/projects/CalendarEventsDisplay.tsx"
      provides: "Calendar events timeline display component for project dashboard"
      min_lines: 25
  key_links:
    - from: "backend/src/routes/v1/connections.ts"
      to: "backend/src/services/calendar.ts"
      via: "import getProjectCalendarEvents"
      pattern: "getProjectCalendarEvents"
    - from: "frontend/src/modules/projects/ProjectDashboard.tsx"
      to: "frontend/src/modules/projects/CalendarEventsDisplay.tsx"
      via: "import and render CalendarEventsDisplay"
      pattern: "CalendarEventsDisplay"
    - from: "frontend/src/modules/projects/ProjectDashboard.tsx"
      to: "frontend/src/modules/connections/ProjectConnectionsPanel.tsx"
      via: "import and render ProjectConnectionsPanel in settings area"
      pattern: "ProjectConnectionsPanel"
---

<objective>
Deliver calendar events on the project dashboard and project-level connection overrides via dropdown UI.

Purpose: Two locked decisions from CONTEXT.md have no implementing task: (1) "Calendar display: project dashboard timeline as primary surface (integrated alongside milestones)" and (2) "Project-level overrides via dropdown pointing to a different workspace connection." This plan closes both gaps with a backend route for calendar events and two frontend components.

Output: Calendar events route, CalendarEventsDisplay component on project dashboard, ProjectConnectionsPanel dropdown component.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-external-connections/07-CONTEXT.md
@.planning/phases/07-external-connections/07-UI-SPEC.md
@.planning/phases/07-external-connections/07-03-SUMMARY.md
@.planning/phases/07-external-connections/07-07-SUMMARY.md

<interfaces>
From backend/src/services/calendar.ts (Plan 07):
```typescript
export function getProjectCalendarEvents(projectId: string): {
  id: string;
  title: string;
  startAt: string;
  endAt: string | null;
  allDay: boolean;
}[];
```

From backend/src/routes/v1/connections.ts (Plan 02):
```typescript
// Existing endpoints:
// GET /project/:projectId — list connections for a project
// POST /project/:projectId — attach workspace connection to project
// DELETE /project/:projectId/:connectionId — detach connection from project
```

From frontend/src/modules/projects/ProjectDashboard.tsx (existing):
```typescript
// Existing project dashboard component — renders milestones, activity, agents
// This is the primary surface for calendar events per CONTEXT.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calendar events route and ProjectConnectionsPanel frontend</name>
  <files>backend/src/routes/v1/connections.ts, frontend/src/modules/connections/ProjectConnectionsPanel.tsx</files>
  <read_first>
    - backend/src/routes/v1/connections.ts (existing routes from Plan 02)
    - backend/src/services/calendar.ts (getProjectCalendarEvents export from Plan 07)
    - frontend/src/modules/connections/ConnectionsPage.tsx (existing connections module pattern)
    - frontend/src/lib/api.ts (API client pattern)
    - .planning/phases/07-external-connections/07-UI-SPEC.md (ProjectConnectionsPanel spec at line 234-238)
  </read_first>
  <action>
    **Backend: Add route to connections.ts**

    Add import at top of connections.ts:
    ```typescript
    import { getProjectCalendarEvents } from '../../services/calendar.js';
    ```

    Add new endpoint AFTER the existing `/project/:projectId` routes:
    ```typescript
    // GET /project/:projectId/calendar-events — Calendar events for project dashboard
    fastify.get('/project/:projectId/calendar-events', { preHandler: [requireAuth] }, async (request, reply) => {
      const { projectId } = request.params as { projectId: string };
      const events = getProjectCalendarEvents(projectId);
      return reply.send(success(events));
    });
    ```

    This exposes `getProjectCalendarEvents()` (which Plan 07 creates as a service function) as an API route consumable by the frontend.

    **Frontend: Create ProjectConnectionsPanel.tsx**

    Create `frontend/src/modules/connections/ProjectConnectionsPanel.tsx`:
    - Props: `{ projectId: string }`
    - Fetches workspace connections: GET /api/v1/connections
    - Fetches project connections: GET /api/v1/connections/project/{projectId}
    - For each unique provider (github, email, google_calendar, whatsapp):
      - Show a row with provider icon + name
      - Dropdown select: "Use workspace default" (first option) + list of workspace connections of that provider type
      - Selecting a connection calls POST /api/v1/connections/project/{projectId} with `{ connection_id, access_mode: 'read_write' }`
      - Reverting to "Use workspace default" calls DELETE /api/v1/connections/project/{projectId}/{connectionId}
    - Admin check: if user is not admin, dropdowns are disabled (operators CAN change per CONTEXT.md -- actually operators CAN manage project-level overrides per UI-SPEC line 237, so only viewers are disabled)
    - Style: compact rows, each 44px height, Lucide icons (Github, Mail, Calendar, MessageCircle), --surface bg, --border dividers
    - Heading: "Connection Overrides" (Heading 16px/600)
    - Empty state: "Using workspace connections for all services" in --text3
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/backend && npx tsc --noEmit && cd /home/lobster/documents/porter/frontend && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/v1/connections.ts contains `calendar-events`
    - backend/src/routes/v1/connections.ts contains `getProjectCalendarEvents`
    - frontend/src/modules/connections/ProjectConnectionsPanel.tsx exists and is at least 30 lines
    - frontend/src/modules/connections/ProjectConnectionsPanel.tsx contains a `select` or dropdown pattern for connection override
    - frontend/src/modules/connections/ProjectConnectionsPanel.tsx contains `projectId` prop
    - TypeScript compilation succeeds in both backend/ and frontend/
  </acceptance_criteria>
  <done>GET /api/v1/connections/project/:projectId/calendar-events route exists. ProjectConnectionsPanel renders provider rows with override dropdowns.</done>
</task>

<task type="auto">
  <name>Task 2: Add CalendarEventsDisplay to project dashboard</name>
  <files>frontend/src/modules/projects/CalendarEventsDisplay.tsx, frontend/src/modules/projects/ProjectDashboard.tsx</files>
  <read_first>
    - frontend/src/modules/projects/ProjectDashboard.tsx (current dashboard layout, milestones section)
    - frontend/src/lib/api.ts (API client)
    - frontend/src/index.css (CSS variables)
    - .planning/phases/07-external-connections/07-UI-SPEC.md
  </read_first>
  <action>
    **Create CalendarEventsDisplay.tsx:**

    - Props: `{ projectId: string }`
    - Fetches calendar events: GET /api/v1/connections/project/{projectId}/calendar-events
    - If no events: render nothing (invisible -- do not clutter dashboard when calendar not connected)
    - If events exist: render a compact timeline section
    - Section heading: "Upcoming Deadlines" with Calendar icon (Lucide) -- 16px/600
    - Each event renders as a row:
      - Date badge: colored pill showing the date (e.g., "Mar 25") -- bg-[var(--raised)], text-xs, rounded
      - If all-day: show date only. If timed: show "Mar 25, 2:00 PM"
      - Event title: 14px/400, --text
      - If event is within 24 hours: date badge uses --warning background (approaching deadline)
      - If event is today: date badge uses --danger background (deadline today)
    - Max 5 events shown initially, "Show all" link if more
    - Events sorted by start_at ascending (API returns them sorted)
    - Timeline line: thin 2px vertical line on the left (--border color) connecting event dots

    **Wire into ProjectDashboard.tsx:**

    - Add import: `import { CalendarEventsDisplay } from './CalendarEventsDisplay';`
    - Add import: `import { ProjectConnectionsPanel } from '../connections/ProjectConnectionsPanel';`
    - Render `<CalendarEventsDisplay projectId={project.id} />` AFTER the milestones section and BEFORE the activity feed -- this places calendar events "alongside milestones" per CONTEXT.md
    - Render `<ProjectConnectionsPanel projectId={project.id} />` in the project settings/config area (if project dashboard has a settings section) or as a collapsible section at the bottom
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/frontend && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/modules/projects/CalendarEventsDisplay.tsx exists and is at least 25 lines
    - frontend/src/modules/projects/CalendarEventsDisplay.tsx contains `calendar-events` (API endpoint)
    - frontend/src/modules/projects/CalendarEventsDisplay.tsx contains `Upcoming Deadlines` or similar heading
    - frontend/src/modules/projects/ProjectDashboard.tsx contains `CalendarEventsDisplay`
    - frontend/src/modules/projects/ProjectDashboard.tsx contains `ProjectConnectionsPanel`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Calendar deadlines appear on project dashboard timeline alongside milestones. Project connection overrides accessible from project detail view. All per CONTEXT.md locked decisions.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in both backend/ and frontend/
- GET /api/v1/connections/project/test-project/calendar-events returns 200 with data array
- ProjectDashboard renders CalendarEventsDisplay after milestones
- ProjectConnectionsPanel renders dropdown per provider
- All 35 Playwright tests remain green
</verification>

<success_criteria>
- Calendar events route delivers data from calendar_events table to frontend
- CalendarEventsDisplay shows upcoming deadlines with color-coded date badges on project dashboard
- Deadlines within 24 hours highlighted with warning color, same-day with danger
- ProjectConnectionsPanel allows per-project connection override via dropdown
- Both locked CONTEXT.md decisions (calendar display on dashboard, project-level overrides) are fully implemented
</success_criteria>

<output>
After completion, create `.planning/phases/07-external-connections/07-10-SUMMARY.md`
</output>
