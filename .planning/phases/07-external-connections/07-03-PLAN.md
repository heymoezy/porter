---
phase: 07-external-connections
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/modules/connections/ConnectionsPage.tsx
  - frontend/src/modules/connections/ServiceCard.tsx
  - frontend/src/modules/connections/ConnectionStatusBadge.tsx
  - frontend/src/modules/connections/DisconnectDialog.tsx
  - frontend/src/modules/connections/ApiKeyForm.tsx
  - frontend/src/store/app.ts
  - frontend/src/components/Sidebar.tsx
  - frontend/src/components/Layout.tsx
autonomous: true
requirements: [CONN-05]
must_haves:
  truths:
    - "Connections page renders 4 service cards (GitHub, Email, Google Calendar, WhatsApp)"
    - "Each service card shows a status dot (green=connected, amber=needs_reauth, grey=disconnected)"
    - "Clicking 'Connect' on an API key service shows a masked input form"
    - "Sidebar shows 'Connections' nav item with Plug icon"
    - "Non-admin users see disabled action buttons with tooltip 'Only admins can manage connections'"
  artifacts:
    - path: "frontend/src/modules/connections/ConnectionsPage.tsx"
      provides: "Main connections page with service card grid"
      min_lines: 40
    - path: "frontend/src/modules/connections/ServiceCard.tsx"
      provides: "Individual provider card with icon, status, action button"
      min_lines: 30
    - path: "frontend/src/modules/connections/ConnectionStatusBadge.tsx"
      provides: "Status dot + label reusable component"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/Layout.tsx"
      to: "frontend/src/modules/connections/ConnectionsPage.tsx"
      via: "TabPlaceholder routing for 'connections' tab"
      pattern: "ConnectionsPage"
    - from: "frontend/src/components/Sidebar.tsx"
      to: "frontend/src/store/app.ts"
      via: "tabs array includes connections entry"
      pattern: "connections"
---

<objective>
Build the Connections page frontend UI with service cards, status badges, and API key form.

Purpose: Users need a visual interface to see which external services are connected, their status, and to manage credentials. This plan builds the frontend module that consumes the connections API from Plan 02. OAuth connect buttons are added in subsequent integration plans (05-08) since they need provider-specific OAuth routes.

Output: ConnectionsPage with 4 service cards, status badges, disconnect dialog, API key form, sidebar navigation.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-external-connections/07-CONTEXT.md
@.planning/phases/07-external-connections/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConnectionsPage, ServiceCard, and supporting components</name>
  <files>
    frontend/src/modules/connections/ConnectionsPage.tsx,
    frontend/src/modules/connections/ServiceCard.tsx,
    frontend/src/modules/connections/ConnectionStatusBadge.tsx,
    frontend/src/modules/connections/DisconnectDialog.tsx,
    frontend/src/modules/connections/ApiKeyForm.tsx
  </files>
  <read_first>
    - frontend/src/modules/health/SystemHealthPanel.tsx (existing module pattern — how modules are structured, CSS variable usage, component composition)
    - frontend/src/modules/projects/ProjectDashboard.tsx (existing module pattern)
    - frontend/src/lib/api.ts (API client pattern — how to call /api/v1/ endpoints)
    - frontend/src/index.css (CSS variables available: --bg, --surface, --raised, --border, --text, --text2, --text3, --accent, --success, --warning, --danger)
    - .planning/phases/07-external-connections/07-UI-SPEC.md (visual contract — colors, spacing, typography, copy, status dot system)
  </read_first>
  <action>
    Create the connections module directory and components:

    **ConnectionStatusBadge.tsx:**
    - Props: `{ status: 'connected' | 'needs_reauth' | 'disconnected' | 'error' }`
    - Renders: 8x8 rounded-full dot + label text
    - Color mapping: connected → `bg-[var(--success)]`, needs_reauth → `bg-[var(--warning)]`, disconnected → `bg-[var(--text3)]`, error → `bg-[var(--danger)]`
    - Labels: 'Connected', 'Reconnect needed', 'Not connected', 'Error'
    - Dot has `aria-label` with the label text
    - Label uses text-xs (12px), text-[var(--text3)] color

    **ServiceCard.tsx:**
    - Props: `{ provider: string; displayName: string; description: string; status: 'connected' | 'needs_reauth' | 'disconnected' | 'error'; lastSyncAt: number | null; connectionId: string | null; isAdmin: boolean; icon: React.ReactNode; onConnect: () => void; onDisconnect: (id: string) => void; onManage: (id: string) => void }`
    - Uses shadcn Card (div with bg-[var(--surface)], border border-[var(--border)], rounded-lg)
    - Layout: 32x32 icon + name (16px/600) + ConnectionStatusBadge in header row
    - Body: 1-sentence description (14px/400, --text2), last sync relative time (12px, --text3) or "Never synced"
    - Footer: full-width action button
      - disconnected → "Connect {displayName}" (bg-[var(--accent)] text-white, 44px min-h)
      - needs_reauth → "Reconnect" (border-[var(--warning)] text-[var(--warning)], 44px min-h)
      - connected → "Manage Connection" (ghost style, 44px min-h)
    - If !isAdmin: button is disabled with title="Only admins can manage connections"
    - Card hover: transition shadow + translateY(-1px) over 150ms

    **ApiKeyForm.tsx:**
    - Props: `{ provider: string; onSave: (key: string) => Promise<void>; onCancel: () => void }`
    - Masked input (type=password) with placeholder "Paste API key"
    - Toggle reveal button (eye icon from Lucide)
    - "Save" button disabled when input empty, shows spinner while saving
    - "Cancel" link/button to close form
    - Input uses shadcn Input pattern (border-[var(--border)], focus ring-[var(--accent)])
    - Associated `<label>` element for accessibility

    **DisconnectDialog.tsx:**
    - Props: `{ open: boolean; serviceName: string; onConfirm: () => void; onCancel: () => void }`
    - Uses shadcn Dialog pattern (overlay + centered card)
    - Title: "Disconnect {serviceName}?"
    - Body: "Connected agents will lose access. Active jobs will be paused."
    - Actions: "Keep Connected" (ghost) | "Disconnect" (bg-[var(--danger)] text-white)

    **ConnectionsPage.tsx:**
    - Fetches connections from GET /api/v1/connections using the api() client
    - Fetches current user role from /api/v1/profile or /api/me (check existing pattern)
    - Page header: "Connections" (20px/600), subtitle "Connect services to give agents real-world reach" (14px/400, --text2)
    - Service definitions array (static, 4 entries):
      ```
      [
        { provider: 'github', displayName: 'GitHub', description: 'Read repos, create branches, open PRs', icon: <Github /> },
        { provider: 'email', displayName: 'Email', description: 'Send notifications, receive and route emails', icon: <Mail /> },
        { provider: 'google_calendar', displayName: 'Google Calendar', description: 'Sync deadlines, push milestones', icon: <Calendar /> },
        { provider: 'whatsapp', displayName: 'WhatsApp', description: 'Send and receive messages via agents', icon: <MessageCircle /> },
      ]
      ```
    - Grid: `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6` (auto-fill, min 280px per card)
    - For each service: find matching connection from API response, render ServiceCard with merged status
    - Empty state: when no connections AND no services are configured (unlikely with static service defs, but guard with "No connections yet" heading)
    - Card entrance animation: stagger with 50ms delay per card, fade-in + translateY(8px) over 250ms
    - State for showing ApiKeyForm (selectedProvider string or null)
    - State for showing DisconnectDialog (selectedConnectionId + serviceName or null)
    - onConnect handler: for now, show ApiKeyForm (OAuth buttons added in integration plans)
    - onDisconnect handler: DELETE /api/v1/connections/:id
    - Use Lucide icons: Github, Mail, Calendar, MessageCircle from 'lucide-react'
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/modules/connections/ConnectionsPage.tsx contains `Connections` (page title)
    - frontend/src/modules/connections/ConnectionsPage.tsx contains `github` and `email` and `google_calendar` and `whatsapp` (all 4 providers)
    - frontend/src/modules/connections/ConnectionsPage.tsx contains `/api/v1/connections` (API call)
    - frontend/src/modules/connections/ServiceCard.tsx contains `ConnectionStatusBadge`
    - frontend/src/modules/connections/ServiceCard.tsx contains `Only admins can manage connections`
    - frontend/src/modules/connections/ConnectionStatusBadge.tsx contains `aria-label`
    - frontend/src/modules/connections/ConnectionStatusBadge.tsx contains `--success` and `--warning` and `--text3` and `--danger`
    - frontend/src/modules/connections/DisconnectDialog.tsx contains `Disconnect` and `Keep Connected`
    - frontend/src/modules/connections/ApiKeyForm.tsx contains `Paste API key`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>ConnectionsPage renders 4 service cards with status dots, connect/disconnect actions, API key form, and disconnect confirmation dialog. All visual specs from UI-SPEC.md implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Connections into Sidebar navigation and Layout routing</name>
  <files>frontend/src/store/app.ts, frontend/src/components/Sidebar.tsx, frontend/src/components/Layout.tsx</files>
  <read_first>
    - frontend/src/store/app.ts (TabId type union — need to add 'connections')
    - frontend/src/components/Sidebar.tsx (tabs array — need to add connections entry)
    - frontend/src/components/Layout.tsx (TabPlaceholder — need to route to ConnectionsPage)
  </read_first>
  <action>
    Update `frontend/src/store/app.ts`:
    - Add `'connections'` to the `TabId` type union (after `'health'`)

    Update `frontend/src/components/Sidebar.tsx`:
    - Add import: `import { Plug } from 'lucide-react';` (Plug icon for connections)
    - Add to tabs array (after the `health` entry): `{ id: 'connections', label: 'Connections', icon: Plug }`

    Update `frontend/src/components/Layout.tsx`:
    - Add import: `import { ConnectionsPage } from '../modules/connections/ConnectionsPage';`
    - In TabPlaceholder function, add before the default return:
      ```
      if (name === 'connections') return <ConnectionsPage />;
      ```
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/frontend && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/store/app.ts contains `'connections'` in TabId type
    - frontend/src/components/Sidebar.tsx contains `Plug` import from lucide-react
    - frontend/src/components/Sidebar.tsx contains `{ id: 'connections'`
    - frontend/src/components/Layout.tsx contains `import { ConnectionsPage }`
    - frontend/src/components/Layout.tsx contains `name === 'connections'`
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Connections tab appears in sidebar with Plug icon. Clicking it renders ConnectionsPage. Navigation works alongside all existing tabs.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` in frontend/ passes
- Sidebar contains Connections entry
- Layout routes to ConnectionsPage for connections tab
- All 35 Playwright tests remain green
</verification>

<success_criteria>
- Connections page accessible via sidebar navigation
- 4 service cards rendered (GitHub, Email, Google Calendar, WhatsApp)
- Each card shows correct status dot color based on connection state
- Admin-only enforcement on action buttons
- API key form allows credential entry for non-OAuth providers
- Disconnect dialog confirms before removal
</success_criteria>

<output>
After completion, create `.planning/phases/07-external-connections/07-03-SUMMARY.md`
</output>
