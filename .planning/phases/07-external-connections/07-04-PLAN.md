---
phase: 07-external-connections
plan: 04
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - backend/src/config.ts
  - porter.py
autonomous: true
requirements: [CONN-05]
must_haves:
  truths:
    - "Zero hardcoded paths, IPs, ports, or tokens remain in backend/src/ — all come from config.ts env vars"
    - "porter.py DEFAULT_MOUNTS is empty list on fresh installs — populated only from config"
    - "porter.py HOST and PORT read from env vars PORTER_HOST and PORTER_PORT"
    - "grep for literal '127.0.0.1' in backend/src/ returns zero results (all use config.ollamaUrl etc.)"
  artifacts:
    - path: "backend/src/config.ts"
      provides: "All previously-hardcoded values now configurable via env vars"
      contains: "PORTER_HOST"
    - path: "porter.py"
      provides: "Hardcoded paths replaced with PORTER_DATA_DIR derivation"
      contains: "PORTER_DATA_DIR"
  key_links:
    - from: "backend/src/config.ts"
      to: "process.env"
      via: "env var lookups for all service URLs and paths"
      pattern: "process.env"
---

<objective>
Remove all hardcoded paths, tokens, IPs, and ports from the codebase and replace with config lookups.

Purpose: CONN-05 requires zero hardcoded API keys, tokens, service URLs, or file paths in the codebase. This plan audits and fixes all known violations documented in CLAUDE.md's "Known Hardcoding Violations" section, plus any remaining hardcoded values in the Fastify backend.

Output: Updated config.ts with all values configurable, updated porter.py with env var lookups.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-external-connections/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge hardcoded values from Fastify backend</name>
  <files>backend/src/config.ts</files>
  <read_first>
    - backend/src/config.ts (current config — check for any remaining hardcoded fallbacks that should be env-only)
    - backend/src/services/ai-router.ts (check for hardcoded URLs or tokens)
    - backend/src/services/scheduler.ts (check for hardcoded values)
    - backend/src/services/event-triggers.ts (check for hardcoded values)
    - backend/src/index.ts (check for hardcoded values)
    - CLAUDE.md (Known Hardcoding Violations section)
  </read_first>
  <action>
    Audit every `.ts` file in `backend/src/` for hardcoded literals:
    - Search for literal IP addresses: `127.0.0.1`, `0.0.0.0`, `76.13.190.52`, `100.85.184.74`
    - Search for literal ports: `:8877`, `:11434`, `:18789`, `:3001`
    - Search for literal paths: `/home/lobster`, `~/.porter`, `~/.openclaw`
    - Search for literal tokens: `lobster-2026`

    For each finding in `backend/src/`:
    - Replace with reference to `config.xxx` from `config.ts`
    - If config.ts doesn't have the field yet, add it with env var lookup and sensible default

    Verify config.ts already has these (add if missing):
    - `port`: from `PORTER_BACKEND_PORT` (default `3001`) — already exists
    - `host`: from `PORTER_BACKEND_HOST` (default `127.0.0.1`) — already exists
    - `porterPyUrl`: from `PORTER_PY_URL` (default `http://127.0.0.1:8877`) — already exists
    - `dbPath`: from `PORTER_DB_PATH` — already exists
    - `dataDir`: from `PORTER_DATA_DIR` — already exists
    - `ollamaUrl`: from `OLLAMA_URL` — already exists
    - `openclawUrl`: from `OPENCLAW_URL` — already exists
    - `openclawToken`: from `OPENCLAW_TOKEN` — already exists

    The defaults in config.ts (like `'http://127.0.0.1:8877'`) are acceptable as sensible dev defaults — the key rule is that NO other file uses hardcoded values directly. All must import from config.ts.

    Check that no file in `backend/src/routes/`, `backend/src/services/`, `backend/src/plugins/` contains literal IPs, ports, or paths that bypass config.ts.
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter && grep -rn '127\.0\.0\.1\|76\.13\.190\.52\|100\.85\.184\.74\|lobster-2026\|/home/lobster' backend/src/ --include='*.ts' | grep -v 'config.ts' | grep -v 'node_modules' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep for `127.0.0.1` in backend/src/ excluding config.ts returns 0 results
    - grep for `76.13.190.52` in backend/src/ returns 0 results
    - grep for `/home/lobster` in backend/src/ returns 0 results
    - grep for `lobster-2026` in backend/src/ returns 0 results
    - All service URLs flow through config.ts imports
  </acceptance_criteria>
  <done>Zero hardcoded IPs, ports, paths, or tokens in backend/src/ outside of config.ts defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Purge hardcoded values from porter.py</name>
  <files>porter.py</files>
  <read_first>
    - porter.py (grep for the specific violations listed in CLAUDE.md)
    - CLAUDE.md (Known Hardcoding Violations — exact list of violations to fix)
  </read_first>
  <action>
    Fix the known hardcoding violations documented in CLAUDE.md:

    **Use a Python patch script at /tmp/patch_hardcoding.py** (porter.py is ~900KB, Edit tool fails).

    The patch script must:

    1. **DEFAULT_MOUNTS** — Find the `DEFAULT_MOUNTS` assignment. If it contains hardcoded paths like `/home/lobster/documents`, wrap the initialization so it reads from `PORTER_MOUNTS` env var (JSON string) or defaults to empty list `[]` on fresh install. The existing hardcoded entries should be treated as migration data — a comment should note this.

    2. **CONFIG_PATH** — Find `CONFIG_PATH` assignment. Replace hardcoded path with:
       ```python
       _data_dir = os.environ.get('PORTER_DATA_DIR', os.path.join(os.path.expanduser('~'), '.porter'))
       CONFIG_PATH = os.path.join(_data_dir, 'porter_config.json')
       ```

    3. **RUNTIME_DIR, AVATAR_DIR, MEMORY_DIR** — Replace any `/home/lobster/` prefix with `_data_dir` derivation:
       ```python
       RUNTIME_DIR = os.path.join(_data_dir, 'runtime')
       AVATAR_DIR = os.path.join(_data_dir, 'avatars')
       MEMORY_DIR = os.path.join(_data_dir, 'memory')
       ```

    4. **HOST** — Replace `HOST = "76.13.190.52"` with:
       ```python
       HOST = os.environ.get('PORTER_PUBLIC_IP', '')
       ```

    5. **PORT** — Replace hardcoded `PORT = 8877` with:
       ```python
       PORT = int(os.environ.get('PORTER_PORT', '8877'))
       ```

    6. **AGENT_WORKSPACE_DIR, OPENCLAW_STATE_DIR** — Replace `~/.openclaw` assumptions with:
       ```python
       AGENT_WORKSPACE_DIR = os.environ.get('PORTER_AGENT_WORKSPACE', os.path.join(os.path.expanduser('~'), '.porter', 'workspace'))
       OPENCLAW_STATE_DIR = os.environ.get('PORTER_OPENCLAW_STATE', os.path.join(os.path.expanduser('~'), '.openclaw'))
       ```

    Run: `python3 /tmp/patch_hardcoding.py`

    IMPORTANT: The patch script must:
    - Read porter.py, find the EXACT current lines using string matching
    - Replace only those lines, preserving surrounding code
    - Write back the full file
    - Print what was changed for verification
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter && python3 -c "
import re
with open('porter.py', 'r') as f:
    content = f.read()
violations = []
# Check for hardcoded lobster paths (not in comments)
for i, line in enumerate(content.split('\n'), 1):
    stripped = line.lstrip()
    if stripped.startswith('#'):
        continue
    if '/home/lobster' in line and 'PORTER_DATA_DIR' not in line and 'environ' not in line:
        violations.append(f'  line {i}: {line.strip()[:80]}')
if violations:
    print('FAIL — hardcoded paths found:')
    for v in violations[:10]:
        print(v)
else:
    print('PASS — no hardcoded /home/lobster paths in active code')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - porter.py CONFIG_PATH uses `os.environ.get('PORTER_DATA_DIR'` or `_data_dir` derivation
    - porter.py PORT uses `os.environ.get('PORTER_PORT'`
    - porter.py HOST uses `os.environ.get('PORTER_PUBLIC_IP'` or similar
    - grep for literal `/home/lobster` in porter.py active code (non-comment lines) returns 0 results
    - Porter still starts successfully after changes: `python3 porter.py` (quick smoke test)
  </acceptance_criteria>
  <done>All known hardcoding violations from CLAUDE.md are resolved. Every path, port, IP, and token reads from environment variables with sensible defaults.</done>
</task>

</tasks>

<verification>
- `grep -rn '127.0.0.1' backend/src/ --include='*.ts' | grep -v config.ts` returns nothing
- porter.py hardcoding check script passes
- `npx tsc --noEmit` in backend/ passes
- All 35 Playwright tests remain green
</verification>

<success_criteria>
- Zero hardcoded IPs, ports, paths, or tokens in backend/src/ outside config.ts defaults
- porter.py uses PORTER_DATA_DIR, PORTER_PORT, PORTER_PUBLIC_IP env vars
- DEFAULT_MOUNTS is empty on fresh install
- CONFIG_PATH, RUNTIME_DIR, AVATAR_DIR, MEMORY_DIR derive from PORTER_DATA_DIR
- All existing functionality preserved — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-external-connections/07-04-SUMMARY.md`
</output>
