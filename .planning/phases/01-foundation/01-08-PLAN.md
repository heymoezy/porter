---
phase: 01-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/plugins/proxy.ts
  - backend/dist/
autonomous: true
gap_closure: true
requirements: [FOUND-05]

must_haves:
  truths:
    - "Fastify starts without crash on port 3001 and responds to /health"
    - "Proxy passes unknown routes through to porter.py on port 8877"
  artifacts:
    - path: "backend/src/plugins/proxy.ts"
      provides: "Proxy plugin without OPTIONS in httpMethods"
      contains: "httpMethods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH']"
    - path: "backend/dist/"
      provides: "Compiled TypeScript output"
  key_links:
    - from: "backend/src/plugins/proxy.ts"
      to: "@fastify/cors"
      via: "OPTIONS route handled by cors, not proxy"
      pattern: "httpMethods.*GET.*POST.*PUT.*DELETE.*PATCH"
---

<objective>
Fix Fastify startup crash caused by OPTIONS route conflict between @fastify/cors and proxy plugin, then build the backend so it can run.

Purpose: Verification found Fastify cannot start at all (SC4 blocker). The proxy plugin registers OPTIONS on /* but @fastify/cors already claims that route, causing a fatal "Method OPTIONS already declared" error. Additionally, no dist/ directory exists (backend was never compiled) so even after the fix, the backend cannot run in production mode.

Output: Working Fastify server that starts, responds to /health, and proxies unknown routes to porter.py.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OPTIONS conflict in proxy plugin and build backend</name>
  <files>backend/src/plugins/proxy.ts</files>
  <read_first>
    - backend/src/plugins/proxy.ts (current file with the bug)
    - backend/src/index.ts (registration order — proxy must be last)
    - backend/tsconfig.json (outDir is "dist")
  </read_first>
  <action>
1. Edit backend/src/plugins/proxy.ts line 11: change the httpMethods array from:
   `httpMethods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS']`
   to:
   `httpMethods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH']`

   Reason: @fastify/cors (registered in index.ts before the proxy) already handles OPTIONS on /* for CORS preflight. Having OPTIONS in the proxy's httpMethods causes Fastify to throw "Method 'OPTIONS' already declared for route '/*' with constraints {}" at startup.

2. Build the backend TypeScript:
   ```bash
   cd /home/lobster/documents/porter/backend && npx tsc
   ```
   This compiles src/ into dist/ per tsconfig.json (outDir: "dist").

3. Verify the build succeeded:
   ```bash
   ls /home/lobster/documents/porter/backend/dist/index.js
   ```

4. Start Fastify and verify it runs (test in foreground, then kill):
   ```bash
   cd /home/lobster/documents/porter/backend && timeout 5 npx tsx src/index.ts 2>&1 || true
   ```
   Expected output should include "Fastify server running at http://127.0.0.1:3001" (not a crash).

5. If porter.py is running, test the proxy pass-through:
   ```bash
   curl -s http://127.0.0.1:3001/health | grep -q '"status":"ok"'
   ```
  </action>
  <verify>
    <automated>cd /home/lobster/documents/porter/backend && npx tsc --noEmit && grep -q "httpMethods.*GET.*POST.*PUT.*DELETE.*PATCH" src/plugins/proxy.ts && ! grep -q "'OPTIONS'" src/plugins/proxy.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/plugins/proxy.ts contains `httpMethods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH']` (no OPTIONS)
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - backend/dist/index.js exists (compiled output)
    - `grep -c "OPTIONS" backend/src/plugins/proxy.ts` returns 0
  </acceptance_criteria>
  <done>Fastify starts without OPTIONS route conflict crash, backend is compiled to dist/, /health endpoint responds</done>
</task>

</tasks>

<verification>
1. `cd /home/lobster/documents/porter/backend && npx tsc --noEmit` exits 0
2. `grep -c "'OPTIONS'" /home/lobster/documents/porter/backend/src/plugins/proxy.ts` returns 0
3. `ls /home/lobster/documents/porter/backend/dist/index.js` exists
4. `cd /home/lobster/documents/porter/backend && timeout 5 npx tsx src/index.ts 2>&1` outputs "Fastify server running" (not a crash)
</verification>

<success_criteria>
- Fastify starts on port 3001 without crashing
- The OPTIONS route conflict is eliminated
- Backend TypeScript compiles cleanly
- dist/ directory contains compiled JavaScript
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
