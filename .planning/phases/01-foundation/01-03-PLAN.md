---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - porter.py
autonomous: true
requirements: [FOUND-04]
must_haves:
  truths:
    - "All Cortex code paths are disabled — grepping for cortex_enabled returns zero active usages"
    - "ADMIN_PAGE HTML constant is deleted entirely"
    - "ROLE_CAPS dict and auth_check_cap() function are deleted"
    - "/admin/ route handler is deleted"
    - "Users system, admin, jacob are deleted from DB on startup"
    - "4 hidden admin nav tabs are removed from nav HTML"
    - "4 dead settings pages (Agents, Tasks, Policy) are removed"
    - "Duplicate function definitions are removed"
  artifacts:
    - path: "porter.py"
      provides: "Cleaned porter.py with admin system deleted, Cortex disabled, dead code removed"
      contains: "owner"
  key_links:
    - from: "porter.py auth checks"
      to: "session validation"
      via: "Simple is-authenticated check replaces role-based auth_check_cap()"
      pattern: "get_session"
---

<objective>
Delete the admin system (ADMIN_PAGE, /admin/ routes, ROLE_CAPS, auth_check_cap), disable Cortex code paths, remove dead users, remove hidden nav tabs, remove dead settings pages, and delete duplicate function definitions. This is the largest code deletion in Phase 1.

Purpose: Reduce porter.py complexity by removing ~2000+ lines of dead/deprecated code. The admin system is replaced by owner+member roles. Cortex is disabled (full deletion in Phase 2). This must happen after exception handling reform (Plan 02) so the new logging catches any issues during deletion.
Output: Significantly smaller porter.py with simplified auth model and no deprecated features.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Deletion targets from RESEARCH.md Pattern 7 -->

Admin system deletion targets:
- ROLE_CAPS dict: line 7361
- auth_check_cap() function: search for "def auth_check_cap"
- /admin/ route handler: search for "/admin/" in do_GET/do_POST
- ADMIN_PAGE HTML constant: line 46916+ (~1000+ lines)
- platform_admin role checks in embedded JS: lines 20746, 20853, 23704, 24319, 38375
- Hidden admin nav tabs: Policies, Tool Registry duplicate, Audit, Platform link (in nav HTML around lines 17000-17063)
- Dead settings pages: Agents, Tasks, Policy (in settings section around lines 17771-17972)

Cortex targets (disable, not full delete — Phase 2 does full removal):
- cortex_enabled preference: DEFAULT_PREFERENCES lines 100-104
- _cortex_extract functions: lines 1551-2156
- cortex consolidation: lines 1860-1908
- _wf_register("memory_extraction"...): workflow registration

User deletion targets:
- Users to delete from DB: system, admin, jacob
- Keep only: moe
- Tests use moe/porter credentials — safe to delete others

Duplicate functions to delete:
- _normalize_project_name (defined twice)
- _migrate_project_workspace_root (defined twice)
- _normalize_project_registry_names (defined twice)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete admin system — ADMIN_PAGE, ROLE_CAPS, auth_check_cap, /admin/ routes, hidden tabs, dead settings</name>
  <files>porter.py</files>
  <read_first>
    - porter.py (lines 7355-7380 for ROLE_CAPS dict)
    - porter.py (search for "def auth_check_cap" for function location)
    - porter.py (search for "ADMIN_PAGE" for HTML constant start)
    - porter.py (lines 17000-17063 for main nav tabs — identify hidden admin tabs)
    - porter.py (lines 17771-17972 for settings pages)
    - porter.py (search for "platform_admin" to find all role checks in embedded JS)
    - .planning/phases/01-foundation/01-CONTEXT.md (Admin System Deletion decisions)
  </read_first>
  <action>
CRITICAL: porter.py is ~900KB. ALL changes via Python patch scripts at /tmp/patch_*.py.

Write and execute `/tmp/patch_admin_delete.py` that performs these deletions in porter.py:

**1. Delete ADMIN_PAGE HTML constant:**
Find the line `ADMIN_PAGE = """` (or similar assignment around line 46916) and delete everything through the closing `"""`. This is ~1000+ lines of admin console HTML.

**2. Delete ROLE_CAPS dict:**
Find `ROLE_CAPS` at line 7361 and delete the entire dict definition (including all nested capability lists).

**3. Delete auth_check_cap() function:**
Find `def auth_check_cap(` and delete the entire function body.

**4. Replace all auth_check_cap() call sites:**
Search for `auth_check_cap(` calls throughout the file. Replace the check with a simple session validation:
- If the pattern is `if not auth_check_cap(session, "admin"):` → replace with `if not session:` (simple auth check)
- If the pattern returns 403 on failed cap check → keep the 403 but base it on session existence, not role

**5. Delete /admin/ route handler:**
Find the handler for `/admin/` path in do_GET() (and do_POST() if exists) and remove the entire block. The response should now be 404 for /admin/ paths.

**6. Remove hidden admin nav tabs from HTML:**
In the main nav HTML (around lines 17000-17063), find and remove these entries:
- Policies tab
- Tool Registry duplicate tab (keep the original if there are two)
- Audit tab
- Platform link tab

**7. Remove dead settings pages:**
In the settings section (around lines 17771-17972), remove the HTML/JS for:
- Agents settings page
- Tasks settings page
- Policy settings page
Keep only: Profile, Password, Changelog

**8. Remove platform_admin checks from embedded JS:**
Search for `platform_admin` in the embedded JavaScript and either:
- Remove the entire conditional block if it gates admin-only UI
- Replace with `true` if it gates something all users should see

**9. Add startup user cleanup:**
In the startup/boot section of porter.py, add a function that runs on startup:
```python
def _cleanup_legacy_users():
    """Delete legacy admin users, keep only real users."""
    conn = _db_conn()
    try:
        for username in ['system', 'admin', 'jacob']:
            conn.execute("DELETE FROM users WHERE username = ?", (username,))
        conn.commit()
        mlog.emit("info", "system", "boot.user_cleanup", "Deleted legacy admin users")
    except Exception as e:
        mlog.emit("warn", "system", "boot.user_cleanup_failed", str(e))
```
Call this function during startup (in the boot/init sequence).

**Constraints:**
- This is deletion-heavy. Back up porter.py before each sub-step in the script.
- Do NOT delete the `role` column from the users table — preserve for future use.
- Verify test credentials: Playwright tests use `moe` / `porter` — these are NOT deleted.
  </action>
  <verify>
    <automated>grep -c "ADMIN_PAGE" /home/lobster/documents/porter/porter.py | xargs -I{} test {} -le 2 && echo "PASS: ADMIN_PAGE mostly removed" || echo "FAIL: ADMIN_PAGE still large"; grep -c "ROLE_CAPS" /home/lobster/documents/porter/porter.py | grep -q "^0$" && echo "PASS: ROLE_CAPS deleted" || echo "FAIL: ROLE_CAPS remains"; grep -c "auth_check_cap" /home/lobster/documents/porter/porter.py | grep -q "^0$" && echo "PASS: auth_check_cap deleted" || echo "FAIL: auth_check_cap remains"; grep -c "platform_admin" /home/lobster/documents/porter/porter.py | grep -q "^0$" && echo "PASS: platform_admin removed" || echo "FAIL: platform_admin remains"; python3 -c "import ast; ast.parse(open('/home/lobster/documents/porter/porter.py').read()); print('SYNTAX OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ROLE_CAPS" porter.py` returns 0
    - `grep -c "def auth_check_cap" porter.py` returns 0
    - `grep -c "auth_check_cap(" porter.py` returns 0
    - `grep -c "platform_admin" porter.py` returns 0
    - ADMIN_PAGE HTML constant is deleted (grep -c "ADMIN_PAGE" should be 0 or at most 1-2 references to its former existence)
    - porter.py still passes syntax check
    - `grep -c "_cleanup_legacy_users" porter.py` returns at least 2 (def + call)
  </acceptance_criteria>
  <done>Admin system completely deleted. ROLE_CAPS gone. auth_check_cap gone. /admin/ route gone. ADMIN_PAGE HTML gone. Hidden nav tabs removed. Dead settings pages removed. Legacy users (system, admin, jacob) deleted on startup. Auth simplified to session-based check.</done>
</task>

<task type="auto">
  <name>Task 2: Disable Cortex code paths and remove duplicate functions</name>
  <files>porter.py</files>
  <read_first>
    - porter.py (lines 100-104 for DEFAULT_PREFERENCES cortex fields)
    - porter.py (lines 1551-1570 for cortex extract guard and functions)
    - porter.py (lines 1860-1908 for cortex consolidation)
    - porter.py (search for "_wf_register.*memory_extraction" for workflow registration)
    - porter.py (search for "def _normalize_project_name" to find duplicates)
    - porter.py (search for "def _migrate_project_workspace_root" to find duplicates)
    - porter.py (search for "def _normalize_project_registry_names" to find duplicates)
    - .planning/phases/01-foundation/01-RESEARCH.md (FOUND-04 context)
    - .planning/codebase/CONCERNS.md (Deprecated Features Still Active section)
  </read_first>
  <action>
Write and execute `/tmp/patch_cortex_disable.py`:

**1. Disable Cortex in DEFAULT_PREFERENCES:**
Find `DEFAULT_PREFERENCES` dict and set:
- `cortex_enabled` → `False` (force disabled)
- `cortex_min_response_len` → remove entirely
- `cortex_max_facts` → remove entirely
- Any other `cortex_*` preference fields → remove

**2. Disable memory_extraction workflow registration:**
Find the `_wf_register("memory_extraction", ...)` call and either:
- Comment it out with `# DISABLED: Cortex removed in Phase 1, full deletion in Phase 2`
- Or wrap in `if False:` block

**3. Add early-return guards to Cortex functions:**
For each Cortex function (`_cortex_extract`, `_cortex_consolidate`, `_mem_extract_signals` if cortex-dependent), add an early return at the top:
```python
def _cortex_extract(...):
    return  # DISABLED: Cortex removed in Phase 1
    # ... existing code below (left for Phase 2 full deletion)
```
This approach is safer than deleting the functions because callers may still reference them. Phase 2 does the full deletion.

**4. Remove MEMORY.md creation from _create_agent_project:**
Find line ~1004 where `MEMORY.md` is created in new project workspaces. Remove/skip that creation. STATE.md is the replacement.

**5. Delete duplicate function definitions:**
For each of these three duplicated functions, find both definitions and delete the FIRST one (keeping the second, which is the one Python actually uses since it shadows the first):
- `_normalize_project_name` — find both defs, keep the later one
- `_migrate_project_workspace_root` — find both defs, keep the later one
- `_normalize_project_registry_names` — find both defs, keep the later one

The script should verify the exact line ranges of both definitions before deleting to avoid removing the wrong one.

**Constraints:**
- Do NOT delete Cortex functions entirely — just disable them with early returns. Phase 2 handles full removal.
- Verify no callers break by checking each disabled function's call sites still work (they'll get None/no-op responses).
  </action>
  <verify>
    <automated>python3 -c "
import re
content = open('/home/lobster/documents/porter/porter.py').read()
# Check cortex disabled
cortex_prefs = re.findall(r'cortex_enabled.*?(?:True|False)', content)
print('cortex_enabled values:', cortex_prefs)
# Check duplicate functions
for fn in ['_normalize_project_name', '_migrate_project_workspace_root', '_normalize_project_registry_names']:
    count = len(re.findall(rf'def {fn}\(', content))
    status = 'PASS' if count <= 1 else 'FAIL'
    print(f'{status}: {fn} defined {count} time(s)')
print('SYNTAX:', end=' ')
import ast
try:
    ast.parse(content)
    print('OK')
except SyntaxError as e:
    print(f'FAIL: {e}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cortex_enabled.*True" porter.py` returns 0 (Cortex forced disabled)
    - Each of the three duplicate functions is defined exactly once (not twice)
    - `grep -c "DISABLED: Cortex removed" porter.py` returns at least 1 (early return markers)
    - porter.py passes syntax check
    - MEMORY.md creation is removed from _create_agent_project
  </acceptance_criteria>
  <done>Cortex disabled with early returns and preference override. Memory extraction workflow disabled. Duplicate functions removed (one definition each). MEMORY.md no longer created for new projects. All changes are safe for Phase 2 full Cortex deletion.</done>
</task>

</tasks>

<verification>
1. `grep -c "ROLE_CAPS\|auth_check_cap\|platform_admin" /home/lobster/documents/porter/porter.py` — must return 0
2. `grep -c "cortex_enabled.*True" /home/lobster/documents/porter/porter.py` — must return 0
3. `python3 -c "import ast; ast.parse(open('/home/lobster/documents/porter/porter.py').read()); print('OK')"` — exits 0
4. `cd /home/lobster/documents/porter/tests && npx playwright test` — all 35 tests pass
5. porter.py file size should be significantly smaller (estimate: 50-100KB less from ADMIN_PAGE deletion alone)
</verification>

<success_criteria>
- Admin system completely deleted (ADMIN_PAGE, ROLE_CAPS, auth_check_cap, /admin/ routes)
- Platform_admin string appears zero times in porter.py
- Cortex disabled (early returns, preferences forced false, workflow unregistered)
- Legacy users (system, admin, jacob) cleaned up on startup
- Duplicate functions reduced to single definitions
- MEMORY.md no longer created for new projects
- All 35 Playwright tests still pass (they use moe/porter, not admin)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
