# Phase 13.1: Memory V3 State Engine - Context

**Gathered:** 2026-03-24
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace Memory V2's extraction-heavy approach with a project-first state engine. New structured tables (directives, project_notes, agent_notes) replace freeform concept extraction. Tiered injection pipeline with clear priority ordering. Consolidation service for deduplication. Agent self-edit API. Admin memory overview endpoint.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure infrastructure phase.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `backend/src/routes/v1/memory.ts` — existing memory CRUD + search (concepts, FTS via tsvector)
- `backend/src/db/schema.ts` — concepts table already defined with pgTable, tsvector search_vector
- `backend/src/services/learner.ts` — current extraction service (to be replaced/modified)
- `backend/src/routes/v1/chat.ts` — where memory injection currently happens

### Established Patterns
- All routes use `ok()`/`err()` envelope from `lib/envelope.ts`
- PostgreSQL with `pool.query()` for all DB access
- Zod validation on request bodies
- tsvector + GIN indexes for full-text search

### Integration Points
- Memory injection into chat dispatch (chat.ts)
- Agent concepts display (agents.ts)
- Admin memory overview (new endpoint needed)
- Forge pipeline may reference memory during agent creation

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase

</specifics>

<deferred>
## Deferred Ideas

None

</deferred>
