---
phase: 13.1-memory-v3-state-engine
plan: 03
subsystem: api
tags: [pg_trgm, consolidation, memory, agent-notes, concepts, self-edit, admin-overview, fastify]

# Dependency graph
requires:
  - phase: 13.1-01
    provides: agent_notes, directives, project_notes tables + pg_trgm extension + Memory V3 schema migration

provides:
  - consolidateAgentMemory(agentId) function using pg_trgm similarity > 0.6 to merge near-duplicate agent_notes and concepts
  - POST /api/v1/memory/consolidate endpoint
  - POST /api/v1/memory/self-edit endpoint (promote/dismiss/create_directive actions)
  - GET /api/v1/memory/admin/overview endpoint with per-agent health scores (platform_admin only)
  - Route prefix updated from /memory/concepts to /memory (GET /concepts sub-path preserved)
  - Full smoke test coverage for MEMV3-01 through MEMV3-05

affects:
  - Phase 13.1-02 (injection pipeline)
  - Any downstream consumer of /api/v1/memory/concepts (URL unchanged)
  - Admin tooling consuming fleet memory overview

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "pg_trgm self-join similarity query pattern with a.id < b.id to avoid duplicate pairs"
    - "Superseded-set tracking within consolidation transaction to prevent double-superseding"
    - "Dismiss tries agent_notes first, then concepts — universal archive action"
    - "Health score formula: min(100, max(0, avg_confidence - pending_review_count * 5))"

key-files:
  created:
    - backend/src/services/consolidation.ts
  modified:
    - backend/src/routes/v1/memory.ts
    - backend/src/routes/v1/index.ts
    - tests/smoke-phase13.1.sh

key-decisions:
  - "concepts.scope_id uses persona.id directly in admin overview query (scope='agent' AND scope_id=p.id) — not template_id — admin overview shows what agents actually have at runtime"
  - "consolidateAgentMemory uses a dedicated pool.connect() with transaction; ROLLBACK on error; release in finally"
  - "Route prefix changed from /memory/concepts to /memory; existing GET route moved to /concepts sub-path preserving external URL"
  - "Dismiss action checks agent_notes first then concepts — single endpoint for both table types"

patterns-established:
  - "Memory self-edit pattern: copy content to agent_notes, archive source concept in same transaction"
  - "pg_trgm consolidation: similarity>0.6 threshold, higher confidence wins, alphabetically smaller id breaks ties"

requirements-completed: [MEMV3-03, MEMV3-04, MEMV3-05]

# Metrics
duration: 8min
completed: 2026-03-24
---

# Phase 13.1 Plan 03: Consolidation Service, Self-Edit API, and Admin Overview Summary

**pg_trgm consolidation service merging near-duplicate agent notes, self-edit API (promote/dismiss/create_directive), fleet-wide admin memory overview with health scores**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-24T09:06:05Z
- **Completed:** 2026-03-24T09:14:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created `consolidation.ts` service: finds near-duplicate agent_notes and concepts via pg_trgm similarity > 0.6, keeps higher-confidence note, marks loser as `superseded` with `superseded_by_id` pointing to winner
- Extended `memory.ts` with 3 new routes: POST /self-edit (promote/dismiss/create_directive), POST /consolidate, GET /admin/overview (platform_admin gated with health_score per agent)
- Updated route prefix from `/memory/concepts` to `/memory`; existing GET route preserved at `/concepts` sub-path — zero external URL breakage
- Replaced all 4 SKIP placeholders in smoke test with active HTTP-based checks for MEMV3-02 through MEMV3-05

## Task Commits

Each task was committed atomically:

1. **Task 1: Create consolidation service + extend memory routes** - `082dae4` (feat)
2. **Task 2: Complete smoke test scaffold with MEMV3-02..05 checks** - `3c9b885` (feat)

## Files Created/Modified

- `backend/src/services/consolidation.ts` - consolidateAgentMemory() using pg_trgm, handles agent_notes + concepts in single transaction
- `backend/src/routes/v1/memory.ts` - extended with /concepts (renamed from /), /self-edit, /consolidate, /admin/overview
- `backend/src/routes/v1/index.ts` - prefix changed from '/memory/concepts' to '/memory'
- `tests/smoke-phase13.1.sh` - MEMV3-02..05 checks active, no SKIP placeholders remain

## Decisions Made

- Used `p.id` (not `template_id`) as scope_id in admin overview concepts join — `scope_id` at runtime contains persona id for agent-scoped concepts
- Consolidation wraps only the update loop in a transaction (not the initial count query) using dedicated `pool.connect()` client for proper ROLLBACK support
- Route prefix change from `/memory/concepts` to `/memory` preserves external URL by renaming GET `/` to GET `/concepts`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 13.1 complete: Memory V3 schema (Plan 01), injection pipeline (Plan 02), consolidation + self-edit + admin overview (Plan 03)
- Full smoke test coverage for MEMV3-01 through MEMV3-05 in place
- Ready for Phase 14 (Billing) or any downstream phase consuming the memory API

---
*Phase: 13.1-memory-v3-state-engine*
*Completed: 2026-03-24*
