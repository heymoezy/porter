---
phase: 40-gateway-capability-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/bridge/capability-registry.ts
  - backend/src/services/bridge/types.ts
  - backend/src/services/bridge/startup-detector.ts
  - backend/src/services/bridge/routing-engine.ts
  - backend/src/db/migrate-bridge-v7.ts
  - backend/src/index.ts
autonomous: true
requirements:
  - GWC-01
must_haves:
  truths:
    - "Every gateway row in the DB has a structured capabilities object with cost_tier, strengths, context_window, tool_support, and agentic fields"
    - "Restarting the backend populates structured capabilities for all detected gateways via startup-detector"
    - "Old code reading capabilities as string[] still works via normalizeCapabilities/getLegacyTags helpers"
  artifacts:
    - path: "backend/src/services/bridge/capability-registry.ts"
      provides: "GatewayCapabilityRecord type, GATEWAY_CAPABILITY_REGISTRY constant, normalizeCapabilities(), getLegacyTags(), filterToolsBySupport()"
      exports: ["GatewayCapabilityRecord", "GATEWAY_CAPABILITY_REGISTRY", "normalizeCapabilities", "getLegacyTags", "filterToolsBySupport"]
    - path: "backend/src/db/migrate-bridge-v7.ts"
      provides: "DB migration converting flat capabilities arrays to structured JSONB objects"
      exports: ["migrateBridgeV7"]
    - path: "backend/src/services/bridge/startup-detector.ts"
      provides: "Startup detection writing structured capabilities from GATEWAY_CAPABILITY_REGISTRY"
    - path: "backend/src/services/bridge/routing-engine.ts"
      provides: "mapRawToGatewayRow normalizes capabilities for backward compat"
  key_links:
    - from: "backend/src/index.ts"
      to: "backend/src/db/migrate-bridge-v7.ts"
      via: "import and call migrateBridgeV7(pool)"
      pattern: "migrateBridgeV7"
    - from: "backend/src/services/bridge/startup-detector.ts"
      to: "backend/src/services/bridge/capability-registry.ts"
      via: "import GATEWAY_CAPABILITY_REGISTRY to populate capabilities on upsert"
      pattern: "GATEWAY_CAPABILITY_REGISTRY"
    - from: "backend/src/services/bridge/routing-engine.ts"
      to: "backend/src/services/bridge/capability-registry.ts"
      via: "import getLegacyTags for backward compat in mapRawToGatewayRow"
      pattern: "getLegacyTags"
---

<objective>
Create the gateway capability registry foundation: structured capability types, a static registry constant for all 5 gateway types, a DB migration converting flat string[] capabilities to structured JSONB objects, and startup-detector integration that writes structured capabilities on boot.

Purpose: GWC-01 requires every gateway to have a queryable capability record (strengths, cost_tier, context_window, tool_support, agentic). This plan establishes the data layer that Plan 02 consumes for dispatch routing and tool filtering.

Output: capability-registry.ts (types + constant + helpers), migrate-bridge-v7.ts (DB migration), updated startup-detector.ts, updated routing-engine.ts (backward compat), wired migration in index.ts.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-gateway-capability-registry/40-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From backend/src/services/bridge/types.ts:
```typescript
export type GatewayType =
  | 'ollama'
  | 'openclaw'
  | 'codex_cli'
  | 'claude_cli'
  | 'gemini_cli'
  | 'openai_compat';

export interface GatewayRow {
  id: string;
  type: GatewayType;
  capabilities: string[];  // <-- THIS is what gets evolved
  metadata: Record<string, unknown>;
  // ... other fields
}

export interface TaskRequest {
  prompt: string;
  cwd: string;
  timeoutMs?: number;
  tools?: string[];  // Optional tool allowlist
}
```

From backend/src/services/bridge/startup-detector.ts:
```typescript
// CLI_BINARIES array has capabilities: string[] per gateway
const CLI_BINARIES: Array<{
  type: GatewayType;
  binary: string;
  name: string;
  capabilities: string[];
}> = [ ... ];

// upsertGateway writes capabilities as JSON.stringify(params.capabilities)
interface UpsertGatewayParams {
  capabilities: string[];
  // ... other fields
}
```

From backend/src/services/bridge/routing-engine.ts (line 944):
```typescript
// mapRawToGatewayRow reads capabilities as flat array
capabilities: Array.isArray(raw.capabilities) ? (raw.capabilities as string[]) : [],
```

From backend/src/index.ts (lines 20-25, 191-196):
```typescript
import { migrateBridgeV1 } from './db/migrate-bridge-v1.js';
// ... through V6
await migrateBridgeV6(pool);
// migration chain — V7 goes after V6
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create capability-registry.ts with types, constant map, and helpers</name>
  <files>backend/src/services/bridge/capability-registry.ts, backend/src/services/bridge/types.ts</files>
  <read_first>
    - backend/src/services/bridge/types.ts (full file — need GatewayType, GatewayRow)
    - backend/src/services/bridge/routing-engine.ts (lines 940-950 — mapRawToGatewayRow)
    - backend/src/services/bridge/startup-detector.ts (lines 40-50 — CLI_BINARIES capabilities)
  </read_first>
  <action>
    1. Create `backend/src/services/bridge/capability-registry.ts` with:

       a. `GatewayCapabilityRecord` interface:
          - `legacy_tags: string[]` (backward compat flat array)
          - `strengths: string[]` (closed set: "reasoning", "coding", "analysis", "writing", "vision")
          - `cost_tier: 'premium' | 'standard' | 'budget'`
          - `context_window: number` (tokens)
          - `tool_support: 'full' | 'limited' | 'none'`
          - `agentic: boolean`

       b. `GATEWAY_CAPABILITY_REGISTRY` constant: `Record<GatewayType, GatewayCapabilityRecord>` with entries for all 6 GatewayType values:
          - `claude_cli`: legacy_tags=['chat','code','streaming','tool_use'], strengths=['reasoning','coding','analysis','writing'], cost_tier='premium', context_window=200000, tool_support='full', agentic=true
          - `codex_cli`: legacy_tags=['code','streaming'], strengths=['coding'], cost_tier='premium', context_window=128000, tool_support='full', agentic=true
          - `gemini_cli`: legacy_tags=['chat','code','streaming'], strengths=['reasoning','coding','analysis','writing'], cost_tier='standard', context_window=1000000, tool_support='full', agentic=true
          - `openclaw`: legacy_tags=['chat','code','streaming'], strengths=['reasoning','coding','analysis'], cost_tier='premium', context_window=128000, tool_support='full', agentic=true
          - `ollama`: legacy_tags=['chat','code','streaming'], strengths=['coding'], cost_tier='budget', context_window=32768, tool_support='limited', agentic=false
          - `openai_compat`: legacy_tags=['chat','code','streaming'], strengths=['coding','analysis'], cost_tier='standard', context_window=128000, tool_support='full', agentic=false

       c. `normalizeCapabilities(raw: unknown): GatewayCapabilityRecord | null` — if raw is an object with `cost_tier` key, cast and return; if raw is array or null, return null

       d. `getLegacyTags(raw: unknown): string[]` — if raw is array, return as string[]; otherwise call normalizeCapabilities and return rec.legacy_tags or []

       e. `filterToolsBySupport(tools: typeof PORTER_TOOLS_TYPE, support: 'full' | 'limited' | 'none'): same type` — 'none' returns [], 'limited' returns only tools where function.name is 'read_file' or 'list_directory', 'full' returns all tools. Use a generic type parameter so this works with any tools array of the same shape.

       All exports: GatewayCapabilityRecord, GATEWAY_CAPABILITY_REGISTRY, normalizeCapabilities, getLegacyTags, filterToolsBySupport.

    2. In `backend/src/services/bridge/types.ts`:
       - Do NOT change `GatewayRow.capabilities` type from `string[]` — too many callers depend on it.
       - Add a new optional field `capabilityRecord?: GatewayCapabilityRecord` to `GatewayRow` (import GatewayCapabilityRecord from capability-registry.ts, but to avoid circular imports, just define the field as `capabilityRecord?: import('./capability-registry.js').GatewayCapabilityRecord` or simply as `capabilityRecord?: Record<string, unknown>`). Actually, since the import direction is capability-registry importing GatewayType from types, add the optional field as `capabilityRecord?: Record<string, unknown>` to avoid circular deps.
  </action>
  <acceptance_criteria>
    - capability-registry.ts exports GatewayCapabilityRecord interface
    - capability-registry.ts exports GATEWAY_CAPABILITY_REGISTRY with entries for claude_cli, codex_cli, gemini_cli, openclaw, ollama, openai_compat
    - capability-registry.ts exports normalizeCapabilities function
    - capability-registry.ts exports getLegacyTags function
    - capability-registry.ts exports filterToolsBySupport function
    - Each registry entry has legacy_tags, strengths, cost_tier, context_window, tool_support, agentic fields
    - ollama has tool_support='limited' and agentic=false
    - types.ts GatewayRow still has capabilities: string[] (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/lobster/projects/porter/backend && npx tsc --noEmit 2>&1 | head -30 && grep -c "export.*GatewayCapabilityRecord\|export.*GATEWAY_CAPABILITY_REGISTRY\|export.*normalizeCapabilities\|export.*getLegacyTags\|export.*filterToolsBySupport" src/services/bridge/capability-registry.ts && grep "cost_tier.*premium\|cost_tier.*standard\|cost_tier.*budget" src/services/bridge/capability-registry.ts | wc -l && grep "tool_support.*limited" src/services/bridge/capability-registry.ts | wc -l && grep "agentic.*false" src/services/bridge/capability-registry.ts | wc -l</automated>
  </verify>
  <done>capability-registry.ts exists with all 5 exports, all 6 gateway types have structured capability records, ollama is limited/non-agentic, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Create migration + wire startup-detector + backward compat in routing-engine</name>
  <files>backend/src/db/migrate-bridge-v7.ts, backend/src/index.ts, backend/src/services/bridge/startup-detector.ts, backend/src/services/bridge/routing-engine.ts</files>
  <read_first>
    - backend/src/db/migrate-bridge-v6.ts (full file — migration pattern to follow exactly)
    - backend/src/index.ts (lines 18-30 and 185-200 — migration imports and call chain)
    - backend/src/services/bridge/startup-detector.ts (full file — upsertGateway, CLI_BINARIES, bootstrapEnvGateways)
    - backend/src/services/bridge/routing-engine.ts (lines 935-950 — mapRawToGatewayRow)
    - backend/src/services/bridge/capability-registry.ts (created in Task 1 — need GATEWAY_CAPABILITY_REGISTRY, getLegacyTags, normalizeCapabilities)
  </read_first>
  <action>
    1. Create `backend/src/db/migrate-bridge-v7.ts` following the exact pattern from migrate-bridge-v6.ts:
       - Export `migrateBridgeV7(pool: pg.Pool): Promise<void>`
       - BEGIN transaction, check schema_migrations for 'bridge_v7', skip if exists
       - UPDATE gateways SET capabilities = structured JSONB object using CASE on type column
       - Only update WHERE `capabilities IS NULL OR jsonb_typeof(capabilities) = 'array'` (idempotent inner guard)
       - Build the JSONB using `jsonb_build_object(...)` with:
         - 'legacy_tags' = the existing capabilities value (which is the old string array)
         - 'strengths' = CASE by type (same values as GATEWAY_CAPABILITY_REGISTRY)
         - 'cost_tier' = CASE by type
         - 'context_window' = CASE by type
         - 'tool_support' = CASE by type
         - 'agentic' = CASE by type
       - INSERT INTO schema_migrations (id) VALUES ('bridge_v7')
       - COMMIT, log success
       - ROLLBACK + throw on error, release client in finally

    2. Wire migration in `backend/src/index.ts`:
       - Add `import { migrateBridgeV7 } from './db/migrate-bridge-v7.js';` after the migrateBridgeV6 import
       - Add `await migrateBridgeV7(pool);` call right after `await migrateBridgeV6(pool);` in the migration chain

    3. Update `backend/src/services/bridge/startup-detector.ts`:
       - Import `GATEWAY_CAPABILITY_REGISTRY` from `./capability-registry.js`
       - Change `UpsertGatewayParams.capabilities` type from `string[]` to `unknown` (accepts both old string[] and new structured object)
       - In CLI_BINARIES array, keep the flat `capabilities` field as-is (it's only used as the fallback)
       - In the `upsertGateway` function, change `JSON.stringify(params.capabilities)` — this already works for objects too since JSON.stringify handles both arrays and objects
       - In each place that calls `upsertGateway`, use `GATEWAY_CAPABILITY_REGISTRY[cli.type]` as the capabilities value instead of `cli.capabilities`:
         a. In the CLI binary loop (~line 133): `capabilities: GATEWAY_CAPABILITY_REGISTRY[cli.type] ?? cli.capabilities`
         b. In bootstrapEnvGateways for Ollama (~line 193): `capabilities: GATEWAY_CAPABILITY_REGISTRY.ollama`
         c. In bootstrapEnvGateways for OpenClaw (~line 214): `capabilities: GATEWAY_CAPABILITY_REGISTRY.openclaw`
       - In `mapRawToGatewayRow`: import `getLegacyTags` from `./capability-registry.js`, change capabilities mapping from:
         `capabilities: Array.isArray(raw.capabilities) ? raw.capabilities : []`
         to:
         `capabilities: getLegacyTags(raw.capabilities)`
         Also populate the new optional field: `capabilityRecord: normalizeCapabilities(raw.capabilities) ?? undefined`
         (import normalizeCapabilities too)

    4. Update `backend/src/services/bridge/routing-engine.ts`:
       - Import `getLegacyTags` from `./capability-registry.js`
       - In `mapRawToGatewayRow` (~line 944): change
         `capabilities: Array.isArray(raw.capabilities) ? (raw.capabilities as string[]) : []`
         to:
         `capabilities: getLegacyTags(raw.capabilities)`
       - This ensures routing-engine still works with flat string[] even after capabilities become structured in DB
  </action>
  <acceptance_criteria>
    - migrate-bridge-v7.ts exports migrateBridgeV7 function
    - migrate-bridge-v7.ts checks schema_migrations for bridge_v7 idempotency
    - migrate-bridge-v7.ts updates gateways WHERE jsonb_typeof(capabilities) = 'array'
    - index.ts imports and calls migrateBridgeV7 after migrateBridgeV6
    - startup-detector.ts imports GATEWAY_CAPABILITY_REGISTRY
    - startup-detector.ts passes GATEWAY_CAPABILITY_REGISTRY[type] as capabilities in upsertGateway calls
    - routing-engine.ts uses getLegacyTags for backward compat
    - TypeScript compiles clean
  </acceptance_criteria>
  <verify>
    <automated>cd /home/lobster/projects/porter/backend && npx tsc --noEmit 2>&1 | head -30 && grep "migrateBridgeV7" src/index.ts | wc -l && grep "GATEWAY_CAPABILITY_REGISTRY" src/services/bridge/startup-detector.ts | wc -l && grep "getLegacyTags" src/services/bridge/routing-engine.ts | wc -l && grep "bridge_v7" src/db/migrate-bridge-v7.ts | wc -l && grep "jsonb_typeof.*array" src/db/migrate-bridge-v7.ts | wc -l</automated>
  </verify>
  <done>Migration file exists and is wired into boot chain, startup-detector writes structured capabilities from the registry constant, routing-engine uses getLegacyTags for backward compat, TypeScript compiles clean, running the backend populates structured capabilities in gateways table</done>
</task>

</tasks>

<verification>
1. `cd /home/lobster/projects/porter/backend && npm run build` — zero errors
2. `psql -d porter -c "SELECT type, capabilities->>'cost_tier' as cost_tier, capabilities->>'tool_support' as tool_support FROM gateways ORDER BY type"` — all gateways show structured cost_tier and tool_support values
3. `psql -d porter -c "SELECT 1 FROM schema_migrations WHERE id = 'bridge_v7'"` — returns 1 row
4. Grep checks: `grep -c "GATEWAY_CAPABILITY_REGISTRY" backend/src/services/bridge/capability-registry.ts` returns >= 1
</verification>

<success_criteria>
- All 5+ gateway types have entries in GATEWAY_CAPABILITY_REGISTRY
- Migration bridge_v7 converts existing flat arrays to structured JSONB
- startup-detector writes structured capabilities on boot
- routing-engine backward compat via getLegacyTags — no breakage to chat dispatch
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/40-gateway-capability-registry/40-01-SUMMARY.md`
</output>
