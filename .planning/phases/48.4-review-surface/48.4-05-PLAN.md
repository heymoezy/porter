---
phase: 48.4-review-surface
plan: 05
type: execute
wave: 4
depends_on:
  - 48.4-01
  - 48.4-02
  - 48.4-03
  - 48.4-04
files_modified:
  - backend/package.json
  - backend/src/index.ts
  - admin/frontend/app/lib/constants.ts
  - CHANGELOG.md
  - CHECKPOINT.md
  - tests/dreams.spec.js
autonomous: false
requirements:
  - RVS-07
  - RVS-13
must_haves:
  truths:
    - "Version bumped from v6.16.0 → v6.17.0 in all required ship locations: backend/package.json, backend/src/index.ts docstring + startup log + /api/version + /health porter_version + SSE version metadata, admin/frontend/app/lib/constants.ts VERSION (if present)"
    - "CHANGELOG.md gets a new v6.17.0 entry summarizing Phase 48.4 (Review Surface — admin /dreams UI, transactional accept matrix, auto-expiry, SSE wiring, full Dream Silos series complete)"
    - "Frontend rebuilt (`cd admin/frontend && npx react-router build`) so served statics match the new code"
    - "Backend rebuilt (`cd backend && npm run build`) and restarted via systemctl"
    - "Service health: curl http://127.0.0.1:3001/health returns JSON containing '6.17.0'"
    - "Playwright RVS-13 test un-skipped and passes: full E2E loop (dispatch dream-run → SSE-driven proposal appearance → click proposal → drawer opens → accept → directive lands in DB → list refreshes via SSE)"
    - "All Playwright tests in tests/dreams.spec.js pass (no remaining .skip)"
    - "Full smoke suite green: bash tests/smoke-48.1.sh && bash tests/smoke-48.2.sh && bash tests/smoke-48.3.sh && bash tests/smoke-48.4.sh — all RVS-* and predecessors [ ok ] or [skip] (skip only for non-applicable conditions)"
    - "Live human-verify checkpoint: ONE unmocked manual cycle proves the full pipeline against production software-silo data: (1) Run Now button in /dreams dispatches a real Sonnet dream-run; (2) SSE 'dreams:run-completed' event fires; (3) New proposal(s) appear in list without page refresh; (4) Click a proposal → drawer opens with full content; (5) Accept the proposal → directive lands in directives table with source_type='dream_worker'; (6) intellect_events row written with event_type='proposal_accepted'; (7) Next CLI session in /home/lobster/projects/Porter shows the new directive injected in /api/v1/intellect/context"
    - "CHECKPOINT.md updated with Phase 48.4 ship + Dream Silos series closing notes"
  artifacts:
    - path: "backend/package.json"
      provides: "version 6.17.0"
      contains: "6.17.0"
    - path: "CHANGELOG.md"
      provides: "v6.17.0 entry for Phase 48.4 + Dream Silos series complete"
      contains: "v6.17.0"
    - path: "CHECKPOINT.md"
      provides: "Phase 48.4 ship summary + closing notes for Dream Silos series"
      contains: "48.4"
---

<objective>
Close out the Dream Silos series (48.1 + 48.2 + 48.3 + 48.4): bump version v6.16.0 → v6.17.0, ship the frontend build, restart the service, un-skip the RVS-13 full E2E Playwright test, verify the full smoke suite is green, and human-verify one live unmocked cycle (dispatch → SSE → review → accept → directive injected).

Purpose: RVS-07 SSE confirmation under live conditions + RVS-13 full E2E loop. Phase 48 series complete; capture → dispatch → propose → review → directive update cycle closes.

Output: Version bump everywhere, frontend + backend built + restarted, /health reports v6.17.0, Playwright dreams.spec.js fully green, smoke suite all four phases green, Moe-approved human checkpoint. autonomous=false because the live-CLI verification requires a human run.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.4-review-surface/48.4-RESEARCH.md
@.planning/phases/48.4-review-surface/48.4-01-SUMMARY.md
@.planning/phases/48.4-review-surface/48.4-02-SUMMARY.md
@.planning/phases/48.4-review-surface/48.4-03-SUMMARY.md
@.planning/phases/48.4-review-surface/48.4-04-SUMMARY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@admin/CLAUDE.md
@CHECKPOINT.md

<interfaces>
<!-- Ship surfaces to update. -->

From CLAUDE.md (Porter ship process — non-negotiable):
1. cd admin/frontend && npx react-router build (if frontend touched — YES, Plans 03+04 touched it)
2. cd backend && npm run build
3. pkill -9 -f "porter/backend"; sleep 4
4. systemctl --user start porter-fastify; sleep 8
5. curl -s http://127.0.0.1:3001/health → expect current version
6. Update CHECKPOINT.md

From 48.3-05-PLAN.md (precedent for the same shape):
- Version bump locations: backend/package.json, backend/src/index.ts (docstring + startup log), /api/version handler, SSE version metadata, /health porter_version field, CHANGELOG.md
- Plus admin/frontend/app/lib/constants.ts VERSION if it exists (read it first)

Grep before editing:
```bash
grep -rn "6\.16\.0" backend/src/ backend/package.json admin/frontend/app/ --include="*.ts" --include="*.tsx" --include="*.json" 2>/dev/null | grep -v "node_modules"
```

Live human-verify expectations (full E2E, production software-silo):
- Software-silo corpus is ~633 turns (production count as of 2026-05-13)
- Real Sonnet 4.6 dispatch latency: 30-180s
- Run Now in the admin UI POSTs to /api/v1/intellect/dream-run (127.0.0.1-only)
- SSE event 'dreams:run-completed' fires on completion → list invalidates → proposals appear
- Accepting a proposal lands a directive with source_type='dream_worker' in directives table
- Next CLI session in /home/lobster/projects/Porter triggers /api/v1/intellect/context which injects the new silo-scoped directive
- intellect_events row event_type='proposal_accepted' captures the review

CHANGELOG.md shape (mirror v6.16.0 entry from 48.3-05):
- Heading: `## v6.17.0 — Phase 48.4 Review Surface (Dream Silos series complete) (2026-05-13)`
- Sections: Added, Changed (if applicable), Privacy & audit
- Bullet points reference the new files + endpoints + UI behavior
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump v6.16.0 → v6.17.0 + CHANGELOG entry</name>
  <files>backend/package.json, backend/src/index.ts, admin/frontend/app/lib/constants.ts, CHANGELOG.md</files>
  <read_first>
    - backend/package.json (current "version" field)
    - backend/src/index.ts (top docstring + startup log + /health handler if inline)
    - admin/frontend/app/lib/constants.ts (if exists — VERSION export)
    - CHANGELOG.md (recent entries — mirror v6.16.0 shape)
    - CLAUDE.md (ship process)
  </read_first>
  <action>
**Step 1 — Find every v6.16.0 occurrence:**

```bash
grep -rn "6\.16\.0" backend/src/ backend/package.json admin/frontend/app/ --include="*.ts" --include="*.tsx" --include="*.json" 2>/dev/null | grep -v "node_modules"
```

**Step 2 — Bump each occurrence to v6.17.0** using Edit tool (per-line edits, never Write the whole file).

Likely locations:
- backend/package.json: `"version": "6.16.0"` → `"version": "6.17.0"`
- backend/src/index.ts: docstring `Porter v6.16.0` and startup log
- A /api/version route handler (search for the version string in backend/src/routes/)
- /health handler porter_version field (likely backend/src/index.ts or a health route)
- SSE version metadata (search for `version: '6.16.0'` in any SSE setup file)
- admin/frontend/app/lib/constants.ts: if it exports `export const VERSION = '6.16.0'`, bump

**Step 3 — CHANGELOG.md entry.** Read the top of the file and prepend (under the heading):

```markdown
## v6.17.0 — Phase 48.4 Review Surface (Dream Silos series complete) (2026-05-13)

### Added
- **Admin Dreams page** (`/dreams` route in admin/frontend) — list of `memory_proposals` with silo + status filters, click-to-detail drawer, Accept/Reject mutations with toast feedback, delete-kind confirmation modal, simple line-diff preview for supersede/merge previews, expanded recent-runs sidebar with status badges + dispatch_id pills + per-run filter, Run Now button to dispatch a manual dream-run from the UI.
- **Admin API endpoints** under `/api/admin/dreams/*` (admin-cap guarded, mirroring `/api/admin/evolution/*` shape): `GET /proposals` (list with filters + pagination + counts_by_status), `POST /proposals/:id/accept` (transactional 4-kind matrix: new_directive INSERT, supersede UPDATE, merge INSERT+archive×N, delete soft-archive — all with FOR UPDATE row locks, pre-flight SEALED_SEED + SILO_MISMATCH + TARGET_GONE checks, atomic intellect_events audit row, post-commit SSE broadcast), `POST /proposals/:id/reject` (single-statement UPDATE + audit + broadcast), `GET /runs` (list with correlated per-status counts), `GET /runs/:id` (run + nested proposals + dispatch detail).
- **Auto-expiry workflow** — new BUILTIN entry `Expire stale memory proposals` (every_24h, action_type=`memory_proposals_expire`) flips `pending` proposals past `expires_at` to `expired`, writes one `intellect_events` audit row, broadcasts SSE `proposals:resolved` with `{event:'expired', count}`.
- **SSE event topics**: `proposals:created` (fired by dream-worker post-INSERT-COMMIT when new proposals land), `proposals:resolved` (fired by accept/reject/expire handlers), `dreams:run-completed` (fired by dream-worker post-status-UPDATE on both completed + failed paths). All broadcasts are POST-COMMIT (Pitfall 1 enforced). Frontend `useAdminSSE` invalidates React Query caches on every event for true real-time UI updates with no polling.
- **`<ProposalKindBadge/>` + `<ProposalDetailDrawer/>` + `<DiffBlock/>`** components in admin/frontend/app/components/, composed from existing shadcn primitives (Sheet, Dialog, Input, Button, Badge, Table, Card, Select, Skeleton). Zero one-off markup; no new npm dep beyond optional sonner (toasts) installation.
- **Smoke harness** `tests/smoke-48.4.sh` covering RVS-01..RVS-14 with smoke-silo isolation (`silo_id='software-smoke-48.4'`), idempotent cleanup, graceful skips. **Playwright spec** `tests/dreams.spec.js` covering RVS-08..RVS-13 with the auth-setup fixture pattern. **Seed fixture** `tests/fixtures/dreams-mock-proposals.sql` covers all 4 proposal_kind paths + SILO_MISMATCH + SEALED_SEED + expiry probes.

### Changed
- `dream-worker.ts` gets 3 new `broadcast()` call sites at post-commit/post-update insertion points (`proposals:created` after the proposal-INSERT transaction commits, `dreams:run-completed` after the final UPDATE of dream_runs.status on both success + failure paths).
- `workflow-engine.ts` extends `actionHandlers` with `memory_proposals_expire` + adds the matching `BUILTIN_WORKFLOWS` entry. The handler uses the partial index `memory_proposals_expiry_idx` provisioned by 48.3-02 — sweep is index-only.
- `admin/frontend/app/hooks/use-admin-sse.ts` adds 3 switch cases mapping the new SSE topics to React Query invalidations + a window CustomEvent for run-completed toasts.
- Sidebar nav adds **Dreams** under Ops (lucide Moon icon, between Intellect and Bridge).

### Privacy & audit
- Every accept and reject writes an `intellect_events` row (`event_type='proposal_accepted'` or `'proposal_rejected'`, `source_type='review_surface'`). Audit payload contains only proposal_id, dream_run_id, silo_id, proposal_kind, target_directive_ids_touched, reviewer — NEVER turn content, NEVER proposed_content body, NEVER source_evidence content.
- Every auto-expiry sweep writes a single `proposals_expired` event with `{count, ids}` (capped at 20 ids).
- Seed immutability enforced at two layers: pre-flight check in the accept handler (clearer 422 SEALED_SEED error) + `directive_immutable_moe_direct` PostgreSQL trigger as defense-in-depth.
- Silo-mismatch enforced as a pre-flight 422 — `software` silo proposals cannot mutate other silos' directives.

### Dream Silos series — complete (Phase 48.1 + 48.2 + 48.3 + 48.4)
With this ship, the full closed loop is live:
1. **48.1** seeded silo registry + silo-aware injection (CLI sessions in code projects get 4 software-silo directives injected at session start).
2. **48.2** wired transcript capture (Stop + UserPromptSubmit hooks persist silo-tagged turns to `session_transcript_turns`; PII-scrubbed; 30-day retention).
3. **48.3** shipped the Software Dream Worker (weekly model-driven consolidation reads silo-tagged transcripts, dispatches Sonnet 4.6 with raw passthrough, enforces refine-don't-append doctrine, writes structured `memory_proposals`).
4. **48.4** (this release) closes the loop with the human review surface: Moe can see proposals, accept the good ones, reject the bad ones, and watch directives update in real-time. Next CLI session injects the refined rules.
```

**Step 4 — Run the Porter ship process:**

```bash
# Frontend build (Plans 03 + 04 touched the admin/frontend)
cd /home/lobster/projects/Porter/admin/frontend && npx react-router build

# Backend build
cd /home/lobster/projects/Porter/backend && npm run build

# Restart
pkill -9 -f "porter/backend" 2>/dev/null || true
sleep 4
systemctl --user restart porter-fastify
sleep 8

# Verify
curl -sf http://127.0.0.1:3001/health
# Must contain "6.17.0"
```

NOTES:
- The frontend MUST be rebuilt — the backend serves the built statics. Skipping the rebuild = blank screen (per Moe's hard rule `feedback_always_restart_after_build.md`).
- After restart, /health MUST show porter_version='6.17.0' AND the new /api/admin/dreams/* endpoints respond 200 (or 401 if no auth cookie, which is correct).
- If grep finds version strings in admin/frontend/ that don't seem to be the canonical VERSION constant, evaluate case-by-case — most badge displays read /health dynamically.
  </action>
  <verify>
    <automated>node -e "console.log(require('/home/lobster/projects/Porter/backend/package.json').version)" | grep -q "^6\.17\.0$" && ! grep -rn "6\.16\.0" backend/src/ admin/frontend/app/ --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v "node_modules" | head -1 && grep -q "v6.17.0" /home/lobster/projects/Porter/CHANGELOG.md && curl -sf http://127.0.0.1:3001/health | grep -q "6.17.0"</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json version reads "6.17.0"
    - No remaining "6.16.0" references in backend/src/ or admin/frontend/app/
    - CHANGELOG.md has v6.17.0 entry at the top with the Dream Silos series complete note
    - curl -sf http://127.0.0.1:3001/health returns JSON containing "6.17.0"
    - systemctl --user is-active porter-fastify returns "active"
    - Admin UI loads at http://127.0.0.1:3001/ with the new Dreams sidebar entry visible
  </acceptance_criteria>
  <done>
    Version v6.17.0 shipped; service restarted; health endpoint reports the new version; frontend statics served match the build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Un-skip Playwright RVS-13 + run full Playwright + full smoke suite</name>
  <files>tests/dreams.spec.js</files>
  <read_first>
    - tests/dreams.spec.js (current state after Plans 03 + 04 un-skipped RVS-08..RVS-12)
  </read_first>
  <action>
**Step 1 — Edit `tests/dreams.spec.js`** and remove `.skip` from the RVS-13 test definition:

```javascript
// Before:
test.skip('RVS-13 — full E2E: dispatch → SSE → list update → accept → DB', async ({ page, request }) => {

// After:
test('RVS-13 — full E2E: dispatch → SSE → list update → accept → DB', async ({ page, request }) => {
```

This is the last skip in the file. Verify `grep -c "test.skip" tests/dreams.spec.js` returns 0.

**Step 2 — Run the full Playwright suite for dreams.spec.js:**

```bash
cd /home/lobster/projects/Porter/tests
psql -d porter -f fixtures/dreams-mock-proposals.sql  # belt-and-braces
npx playwright test dreams.spec.js
```

Expected: all 7 tests pass (RVS-08, RVS-09, RVS-10, RVS-10b, RVS-11, RVS-12, RVS-13). Total runtime ~60s (RVS-13 polls up to 30s for the dream-run to complete).

**Step 3 — Run the full smoke suite:**

```bash
cd /home/lobster/projects/Porter
bash tests/smoke-48.1.sh && bash tests/smoke-48.2.sh && bash tests/smoke-48.3.sh && bash tests/smoke-48.4.sh
```

Expected: every TRC-*, DRW-*, RVS-* line prints `[ ok ]` or `[skip]` (skip only for SSE timing edge cases that human-verify covers). Final exit 0 from all four scripts.

NOTES:
- RVS-13 dispatches a real dream-run via the 48.3 endpoint with `sample_size_override: 5000` (small budget for fast test execution). If Sonnet is slow or unavailable, the test annotation accepts 0 proposals as a legitimate outcome (doctrine fired or empty corpus).
- The polling loop uses 60×500ms = 30s timeout, enough for typical Sonnet response.
- If a Playwright test races, prefer adding `.waitFor()` or extending Playwright's per-test timeout rather than weakening assertions.
- If the full smoke suite has any RVS-* [FAIL], STOP — fix the underlying issue (in Plans 02/03/04 code) before continuing. Do not patch smoke harness assertions to make them pass.
  </action>
  <verify>
    <automated>grep -c "test.skip" tests/dreams.spec.js | awk '{exit ($1==0)?0:1}' && bash /home/lobster/projects/Porter/tests/smoke-48.1.sh > /tmp/smoke1.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.2.sh > /tmp/smoke2.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.3.sh > /tmp/smoke3.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.4.sh > /tmp/smoke4.log 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/dreams.spec.js has 0 `.skip` remaining
    - All 7 Playwright tests in dreams.spec.js pass (or RVS-13 passes with the 0-proposal legitimate-outcome annotation)
    - bash tests/smoke-48.1.sh exits 0
    - bash tests/smoke-48.2.sh exits 0
    - bash tests/smoke-48.3.sh exits 0
    - bash tests/smoke-48.4.sh exits 0 with all RVS-* [ ok ] or [skip] (skip only on documented timing-sensitive SSE assertions)
  </acceptance_criteria>
  <done>
    Phase 48.4 fully tested. Full smoke suite (4 phases) green. Playwright dreams.spec.js fully green. Ready for live human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live unmocked full-loop verification — dispatch → SSE → review → accept → injection</name>
  <files>(no files — human verification only)</files>
  <action>Pause execution and present the verification steps in &lt;how-to-verify&gt; below to the user. Wait for resume signal. Do not proceed past this checkpoint without explicit "approved" from Moe.</action>
  <verify>
    <automated>bash /home/lobster/projects/Porter/tests/smoke-48.1.sh > /tmp/sm1.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.2.sh > /tmp/sm2.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.3.sh > /tmp/sm3.log 2>&1 && bash /home/lobster/projects/Porter/tests/smoke-48.4.sh > /tmp/sm4.log 2>&1 && cd /home/lobster/projects/Porter/tests && npx playwright test dreams.spec.js > /tmp/playwright.log 2>&1</automated>
  </verify>
  <done>Full smoke suite green AND Playwright green AND one live unmocked admin-UI cycle completed: Run Now in /dreams dispatched a real Sonnet run, proposals appeared via SSE (no manual refresh), Moe accepted one proposal, directive landed with source_type='dream_worker', and a subsequent CLI session in /home/lobster/projects/Porter shows the new directive in the silo-injected block.</done>
  <what-built>
    Phase 48.4 Review Surface — full surface:
    - Backend: admin/dreams.ts (5 endpoints, transactional accept matrix, post-commit SSE)
    - Workflow: memory_proposals_expire BUILTIN + handler (every_24h sweep)
    - Worker: 3 broadcast() additions in dream-worker.ts (proposals:created + dreams:run-completed × 2)
    - Frontend: /dreams route, sidebar nav, list view, detail drawer, accept/reject mutations, diff preview, delete-kind confirmation, failure-mode toasts, run-history sidebar, run-completed toast listener
    - SSE: 3 topics colon-namespaced (proposals:created, proposals:resolved, dreams:run-completed), 3 new useAdminSSE switch cases
    - Tests: smoke-48.4.sh covers RVS-01..RVS-14, dreams.spec.js covers RVS-08..RVS-13 (all un-skipped)
    - v6.17.0 shipped; Dream Silos series complete
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm health + version:**
    ```bash
    curl -sf http://127.0.0.1:3001/health | jq
    ```
    Expected: porter_version=6.17.0; service alive.

    **Step 2 — Open the Dreams admin UI:**
    Navigate to: http://127.0.0.1:3001/dreams
    
    Expected:
    - Sidebar has "Dreams" entry (Moon icon) under Ops, between Intellect and Bridge
    - Page header reads "Dreams" with Moon icon and counts strip
    - Silo filter shows "software" by default; Status filter "Pending" by default
    - Empty state if no pending proposals (or list of any existing pending proposals from prior runs)
    - "Run Now" button visible top-right

    **Step 3 — Live dispatch from the UI (Run Now button):**
    Click "Run Now". Observe:
    - Button shows "Dispatching…" briefly
    - A toast appears (or browser console shows the dispatch fired)
    - In a separate terminal:
      ```bash
      psql -d porter -c "SELECT id, status, model_used, started_at FROM dream_runs WHERE silo_id='software' ORDER BY started_at DESC LIMIT 1"
      ```
      Expected: a fresh row with status='running' or 'completed', model_used contains 'sonnet' or similar.

    **Step 4 — Wait for SSE 'dreams:run-completed':**
    Within 30-180s, the page should update on its own (no refresh):
    - A toast appears: "Dream run completed: N proposals (software)"
    - "Recent runs" footer shows the new run with status badge
    - If proposals were generated (N > 0), they appear in the proposals table

    If status='failed' is shown, that's STILL a valid outcome for this checkpoint — proves the SSE wiring fires on both paths. Document the failure mode (timeout / doctrine / parse) and Moe approves.

    **Step 5 — Inspect a proposal (if any pending):**
    Click any proposal row. Drawer opens with:
    - Kind badge + monospace proposal id in header
    - Silo / status / conceptual_area in subheader
    - Full proposed_content
    - Target directive(s) listed
    - Metadata JSON (priority, conceptual_area, source_type)
    - Source evidence JSON (sample_turn_ids, phrasing_examples, reasoning)
    - For supersede kind with a fetched target: a DiffBlock showing before → after
    - Accept + Reject buttons + optional reason input

    **Step 6 — Accept a proposal:**
    For a new_directive kind: click Accept. Toast: "Proposal accepted — directive landed". Drawer closes. List refreshes (proposal disappears from pending).
    
    DB verification:
    ```bash
    psql -d porter -c "SELECT id, content, priority, source_type, status FROM directives WHERE scope='silo' AND scope_id='software' AND source_type='dream_worker' ORDER BY created_at DESC LIMIT 3"
    psql -d porter -c "SELECT event_type, details_json FROM intellect_events WHERE event_type='proposal_accepted' ORDER BY created_at DESC LIMIT 3"
    ```
    Expected: new directive row with source_type='dream_worker'; intellect_events row with proposal_id + reviewer.

    **Step 7 — Test delete-kind confirmation modal (optional, if a delete proposal exists):**
    Click a delete proposal → Accept. Dialog opens: "Archive this directive?". Click Cancel — nothing changes. Click Accept again → Archive — directive flips to status='archived'.

    **Step 8 — Test reject:**
    Click another pending proposal → enter a reason (optional) → click Reject. Toast: "Proposal rejected". Drawer closes. Proposal flips to status='rejected'.

    **Step 9 — Verify the closing loop — next CLI session injects the new directive:**
    Open a fresh terminal in `/home/lobster/projects/Porter`:
    ```bash
    curl -sf "http://127.0.0.1:3001/api/v1/intellect/context?cwd=/home/lobster/projects/Porter&projectId=porter" | jq '.directives.silo_block' -r
    ```
    Or:
    ```bash
    # Check the raw silo directives the next session would receive
    psql -d porter -c "SELECT id, content, priority FROM directives WHERE scope='silo' AND scope_id='software' AND status='active' ORDER BY priority DESC, created_at DESC"
    ```
    Expected: the newly-accepted directive is present in the active list (priority + content match what was in the proposal).

    **Step 10 — SSE verification (RVS-07 live):**
    With the /dreams page open in one browser tab, in another terminal:
    ```bash
    curl -sf -b /tmp/admin-cookie -X POST http://127.0.0.1:3001/api/admin/dreams/proposals/<some-pending-id>/reject -d '{}' -H "Content-Type: application/json"
    ```
    Expected: the browser tab's proposals list updates without manual refresh (the rejected proposal moves out of the pending view).

    **Acceptable outcomes:**
    - SUCCESS: Full loop completes end-to-end. New directive injected in next CLI session.
    - LEGITIMATE EMPTY: Dream-run returns 0 proposals (model judged corpus has no refinements needed). SSE event still fires; list still refreshes; intellect_events row dream_run_completed written. APPROVES.
    - LEGITIMATE DOCTRINE FAIL: Status='failed' with error_message containing 'Doctrine violation'. Proves Layer 2 fires on live data. APPROVES.
    - LEGITIMATE TIMEOUT: Status='failed' with error_message='Bridge dispatch timeout (180s)'. APPROVES.

    **Failure modes that BLOCK approval:**
    - /dreams page renders but proposals don't appear after a successful dream-run (SSE wiring broken or React Query invalidation not firing)
    - Accept clicked but directive doesn't land in DB (accept handler broken)
    - Accepting a delete-kind proposal HARD-DELETEs the directive instead of soft-archiving (UPDATE status='archived' missing)
    - Accepting any kind of proposal mutates a `source_type='moe-direct'` seed (pre-flight SEALED_SEED check broken; PostgreSQL trigger may have caught it but the UX shows the wrong error)
    - Mutation of a directive in another silo (SILO_MISMATCH pre-flight broken)
    - Toast doesn't appear on accept/reject (toast library not wired)
    - Next CLI session does NOT show the new directive injected (the entire loop is broken)
    - Any orphan intellect_events row pointing at a proposal that's still pending (audit row written outside the transaction)
  </how-to-verify>
  <resume-signal>
    Reply `approved` if (a) full smoke + Playwright are green, (b) Run Now in /dreams dispatched a live run, (c) SSE drove the list update without refresh, (d) Accept landed a directive in the directives table, (e) the next CLI session sees the new directive injected, and (f) NONE of the blocking failure modes fired.
    Reply with the specific failure details if any blocker triggered.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update CHECKPOINT.md with Phase 48.4 ship + Dream Silos series complete</name>
  <files>CHECKPOINT.md</files>
  <read_first>
    - CHECKPOINT.md (current state)
  </read_first>
  <action>
Append (or prepend, depending on existing CHECKPOINT.md convention) a new section:

```markdown
## 2026-05-13 — Phase 48.4 Review Surface SHIPPED — Dream Silos series complete (v6.17.0)

**What landed:**
- Admin Dreams page at `/dreams` with silo + status filters, detail drawer, accept/reject mutations, delete-kind confirmation modal, diff preview, failure-mode toasts, expanded run-history sidebar with dispatch_id pills + per-run filter, Run Now button (POSTs to existing 48.3 manual-trigger endpoint).
- 5 admin endpoints under `/api/admin/dreams/*`: list proposals, accept (transactional 4-kind matrix with FOR UPDATE + pre-flight SEALED_SEED/SILO_MISMATCH/TARGET_GONE + post-commit SSE), reject (single-statement + audit + SSE), runs list (correlated per-status counts), run detail (run + nested proposals + dispatch).
- Auto-expiry workflow row (`every_24h`, action_type=`memory_proposals_expire`) + handler that flips pending past-expiry rows to expired, logs one intellect_events row, broadcasts SSE.
- SSE topics `proposals:created`, `proposals:resolved`, `dreams:run-completed` (colon-namespaced) wired into `dream-worker.ts` (3 broadcast call sites) and admin handlers; useAdminSSE invalidates React Query caches on every event.
- `<ProposalKindBadge/>`, `<ProposalDetailDrawer/>`, `<DiffBlock/>` components composed from existing shadcn primitives — zero one-off markup.

**Dream Silos series — the loop closes:**
1. 48.1 — silo registry + injection on session start (DRM-01..05).
2. 48.2 — transcript capture via Stop + UserPromptSubmit hooks, PII scrub, 30-day retention (TRC-01..08).
3. 48.3 — Software Dream Worker (weekly Sonnet 4.6 raw-passthrough consolidation with refine-don't-append doctrine, writes memory_proposals) (DRW-01..13).
4. 48.4 — review surface: Moe accepts good proposals → directives update → next CLI session injects the refined rules (RVS-01..14).

**Smoke + Playwright status:**
- `bash tests/smoke-48.1.sh && bash tests/smoke-48.2.sh && bash tests/smoke-48.3.sh && bash tests/smoke-48.4.sh` — all green.
- `cd tests && npx playwright test dreams.spec.js` — all 7 tests green (RVS-08..RVS-13 + RVS-10b).
- `cd tests && npx playwright test` — full Porter Playwright suite still green.

**Version:** v6.16.0 → v6.17.0
**Files touched:**
- backend/src/routes/admin/dreams.ts (new)
- backend/src/routes/admin/index.ts (register)
- backend/src/services/intellect/workflow-engine.ts (+ memory_proposals_expire action + BUILTIN entry)
- backend/src/services/intellect/dream-worker.ts (+ 3 broadcast calls)
- admin/frontend/app/routes/dreams.tsx (new)
- admin/frontend/app/routes.ts (register /dreams)
- admin/frontend/app/components/layout/sidebar.tsx (Moon icon nav entry)
- admin/frontend/app/hooks/use-admin-sse.ts (+ 3 SSE switch cases)
- admin/frontend/app/components/ProposalKindBadge.tsx (new)
- admin/frontend/app/components/ProposalDetailDrawer.tsx (new)
- admin/frontend/app/components/DiffBlock.tsx (new)
- tests/smoke-48.4.sh (new)
- tests/dreams.spec.js (new)
- tests/fixtures/dreams-mock-proposals.sql (new)
- backend/package.json + backend/src/index.ts + admin/frontend/app/lib/constants.ts (version bumps)
- CHANGELOG.md (v6.17.0 entry)

**Next:**
- Phase 48 series complete. Future work: admin / data-room silo (separate phase per Moe's framing — different mechanism, deferred).
- Possible v1 follow-ups (deferred): bulk accept/reject, edit-in-place, proposal search, silos list endpoint for the Silo Select.

**Coordination ledger entries:** 48.4-01..05 entries all marked `Status: done` in `.coordination/SESSIONS.md`.
```
  </action>
  <verify>
    <automated>grep -q "Phase 48.4" /home/lobster/projects/Porter/CHECKPOINT.md && grep -q "v6.17.0" /home/lobster/projects/Porter/CHECKPOINT.md && grep -q "Dream Silos series complete" /home/lobster/projects/Porter/CHECKPOINT.md</automated>
  </verify>
  <acceptance_criteria>
    - CHECKPOINT.md has the new Phase 48.4 + v6.17.0 + Dream Silos series complete section
    - Mentions all files touched + smoke/Playwright status
    - Captures the closing-the-loop narrative across 48.1..48.4
  </acceptance_criteria>
  <done>
    Checkpoint reflects current state. Next session opening this repo knows the Dream Silos series is closed and what to verify (next CLI session sees injected directives).
  </done>
</task>

</tasks>

<verification>
- Version v6.17.0 in all 7 ship locations
- `curl http://127.0.0.1:3001/health` returns 6.17.0
- Frontend built + backend built + service restarted
- Playwright `npx playwright test dreams.spec.js` returns 0 (all 7 tests pass; 0 skipped)
- Full smoke suite (48.1 + 48.2 + 48.3 + 48.4) all green
- Live human-verify approved by Moe: Run Now → SSE → list update → accept → directive lands → next CLI session injects
- CHECKPOINT.md captures the ship + series-complete state
- Coordination ledger: this plan claims backend/package.json + admin/frontend/app/lib/constants.ts + CHANGELOG.md + CHECKPOINT.md + tests/dreams.spec.js — no overlap with Plans 02/03/04 file scopes (those wrote source code; Plan 05 ships version + docs)
</verification>

<success_criteria>
RVS-07 verified live (real SSE on real dream-run dispatch). RVS-13 satisfied (full E2E loop green in Playwright). Dream Silos series 48.1 + 48.2 + 48.3 + 48.4 complete. Moe can use the review surface in production to converge his software-silo directives via the Worker's proposals.
</success_criteria>

<output>
After completion, create `.planning/phases/48.4-review-surface/48.4-05-SUMMARY.md` with:
- The 7 version-bump locations (verbatim file paths)
- Result of the live unmocked run (run_id, status, proposals_extracted, latency, model_used)
- Confirmation that the new directive was injected in the next CLI session (`/api/v1/intellect/context` output excerpt)
- Confirmation that the SSE-driven list update happened without manual refresh
- Full smoke + Playwright status (all 4 phases green; 7/7 Playwright tests green)
- Note: Dream Silos series complete. Admin / data-room silo deferred to a separate phase.
</output>
