---
phase: 48.1-silo-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 48.1-02
  - 48.1-03
files_modified:
  - /home/lobster/.claude/hooks/porter-session-start.js
autonomous: false
requirements:
  - DRM-02
  - DRM-03
must_haves:
  truths:
    - "porter-session-start.js reads SessionStart event payload from stdin to extract session_id and cwd"
    - "Hook forwards session_id + cwd as query params to /api/v1/intellect/context"
    - "If stdin parse fails or stdin is empty, hook falls back gracefully: continues with the project-name-only call path (current behavior)"
    - "Hook output (stdout) still contains the markdown context returned by the API — silo section now visible to Claude on session start"
    - "Version bump from v6.11.0 to v6.12.0 in all 8 locations (Porter ship process)"
    - "Admin frontend version strings empirically located and updated (or empirically confirmed to read from /health)"
    - "Final ship runs the full backend build → restart → /health → smoke cycle"
  artifacts:
    - path: "/home/lobster/.claude/hooks/porter-session-start.js"
      provides: "Session-start hook with stdin parse + session_id+cwd forwarding"
  key_links:
    - from: "porter-session-start.js"
      to: "/api/v1/intellect/context?project=X&cwd=Y&session_id=Z"
      via: "extended query string"
      pattern: "session_id=.*cwd="
---

<objective>
Wire the session-start hook so that silos AND `/silo` overrides actually take effect at session start. The hook must read its stdin event payload (currently it reads nothing), extract `session_id` and `cwd`, and pass both to `/api/v1/intellect/context`. Without this, the silo section never appears in a real Claude CLI session and overrides set via `/silo` are invisible across new sessions.

Final step: version bump v6.11.0 → v6.12.0 across all 8 locations, ship process, and final smoke run.

Purpose: DRM-02 + DRM-03 end-to-end (live Claude session). Plan 02 made the API work; Plan 03 made overrides settable; this plan makes both visible to the actual CLI subprocess.

**Ordering note:** This plan is wave 4 (NOT wave 3). Task 3 runs `bash tests/smoke-48.1.sh` which includes SC-5 (the `/silo` override check). SC-5 only works after Plan 03 has shipped, so Plan 04 explicitly depends on `[48.1-02, 48.1-03]` and cannot run parallel with Plan 03.

Output: Updated hook + version bump + green smoke. **Includes a human-verify checkpoint** to confirm a fresh CLI session in `/home/lobster/projects/Porter` actually shows the silo header.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-silo-foundation/48.1-CONTEXT.md
@.planning/phases/48.1-silo-foundation/48.1-RESEARCH.md
@.planning/phases/48.1-silo-foundation/48.1-VALIDATION.md
@.planning/phases/48.1-silo-foundation/48.1-01-SUMMARY.md
@.planning/phases/48.1-silo-foundation/48.1-02-SUMMARY.md
@.planning/phases/48.1-silo-foundation/48.1-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Current porter-session-start.js (~141 lines per RESEARCH Investigation 4):
  - Does NOT read stdin. Calls main() directly.
  - detectProject(): extracts project name from process.cwd() via /home/lobster/projects/([^/]+) regex.
  - queryPorterContext(project): GET http://127.0.0.1:3001/api/v1/intellect/context?project=<name>
  - Writes API response's .context field to stdout (this is what Claude injects into the prompt).

New requirement (per RESEARCH Blocker #2):
  - Read stdin as JSON (SessionStart event payload contains session_id, hook_event_name, cwd, etc.)
  - On parse success: extract session_id, cwd. Append both to context URL.
  - On parse failure or empty stdin: fall back to current behavior (project-only call). Print a warn to stderr.
  - The stdin read pattern is already used by gsd-context-monitor.js — use it as reference.

Porter ship version bump locations (per Porter/CLAUDE.md):
  1. backend/src/index.ts — docstring/comment (line ~2)
  2. backend/src/index.ts — Fastify startup log
  3. backend/src/routes/v1/version.ts (or wherever GET /api/version serves) — VERSION constant
  4. SSE endpoint version metadata
  5. GET /health response porter_version field
  6. CHANGELOG.md — new section
  7. Admin frontend version badge (if visible — usually a constant in a Header component)
  8. Admin page footer version

For 48.1 there is NO frontend rebuild. However, the admin string constants STILL must be updated (BLOCKER 4): the Porter ship process mandates all 8 locations be touched. Use `grep -rn "v6\\.11\\.0" admin/` to empirically locate occurrences. If grep returns zero matches, document empirically (e.g., admin reads version from /health) and skip — but only after empirical confirmation, not assumption.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend porter-session-start.js to read stdin and forward session_id+cwd</name>
  <files>/home/lobster/.claude/hooks/porter-session-start.js</files>
  <read_first>
    - /home/lobster/.claude/hooks/porter-session-start.js (full file)
    - /home/lobster/.claude/hooks/gsd-context-monitor.js (stdin parse reference)
    - /home/lobster/.claude/hooks/porter-user-prompt.js (its stdin pattern is the closest match)
    - .planning/phases/48.1-silo-foundation/48.1-RESEARCH.md (Investigation 4 + Blocker #2)
  </read_first>
  <action>
Read the existing porter-session-start.js. Identify:
  - The current entry-point (likely `main()` called at the bottom).
  - The function that builds the API URL (likely inside `queryPorterContext`).
  - Whether it is CommonJS or ESM.

Modify the entry-point to first read stdin (with a short timeout so the hook doesn't hang if Claude doesn't pipe anything), parse JSON, and extract `session_id` and `cwd`. Then pass both to the context call.

Add this pattern at the start of `main()` (adapt syntax to existing module style):

    async function readStdinJson(timeoutMs = 1000) {
      return new Promise((resolve) => {
        let buf = '';
        const t = setTimeout(() => resolve(null), timeoutMs);
        process.stdin.setEncoding('utf8');
        process.stdin.on('data', (chunk) => { buf += chunk; });
        process.stdin.on('end', () => {
          clearTimeout(t);
          try { resolve(JSON.parse(buf || '{}')); } catch { resolve(null); }
        });
        process.stdin.on('error', () => { clearTimeout(t); resolve(null); });
      });
    }

In main(), replace:

    const project = detectProject();
    const ctx = await queryPorterContext(project);

with:

    const project = detectProject();
    const eventData = await readStdinJson();
    const sessionId = (eventData && eventData.session_id) ? String(eventData.session_id) : null;
    const eventCwd  = (eventData && eventData.cwd)        ? String(eventData.cwd)        : process.cwd();
    const ctx = await queryPorterContext(project, eventCwd, sessionId);

Update queryPorterContext to accept the new args and build the URL:

    async function queryPorterContext(project, cwd, sessionId) {
      const params = new URLSearchParams();
      if (project)   params.set('project', project);
      if (cwd)       params.set('cwd', cwd);
      if (sessionId) params.set('session_id', sessionId);
      const url = 'http://127.0.0.1:3001/api/v1/intellect/context?' + params.toString();
      // ... rest of existing fetch + JSON parse logic, unchanged ...
    }

Preserve the existing fallback behavior: if the API is unreachable or returns non-200, output the minimal header it currently outputs.

Add ONE stderr log line on stdin failure so it is visible in `journalctl --user-unit porter-fastify` (or wherever hook stderr lands):

    if (eventData === null) {
      try { process.stderr.write('[porter-session-start] stdin payload missing or unparseable — falling back to project-only call\n'); } catch {}
    }

Validate hook syntax:

    node --check /home/lobster/.claude/hooks/porter-session-start.js

And verify the stdin path works end-to-end via:

    echo '{"session_id":"ssh-test-1","cwd":"/home/lobster/projects/Porter"}' | node /home/lobster/.claude/hooks/porter-session-start.js | head -c 200
  </action>
  <verify>
    <automated>node --check /home/lobster/.claude/hooks/porter-session-start.js && grep -q "readStdinJson\\|stdin" /home/lobster/.claude/hooks/porter-session-start.js && grep -q "session_id" /home/lobster/.claude/hooks/porter-session-start.js && grep -q "cwd" /home/lobster/.claude/hooks/porter-session-start.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check /home/lobster/.claude/hooks/porter-session-start.js` exits 0
    - `grep -q "JSON.parse" /home/lobster/.claude/hooks/porter-session-start.js` returns 0 (stdin parse is present)
    - `grep -q "session_id" /home/lobster/.claude/hooks/porter-session-start.js` returns 0
    - `grep -q "cwd" /home/lobster/.claude/hooks/porter-session-start.js` returns 0
    - `grep -q "URLSearchParams\\|cwd=\\|session_id=" /home/lobster/.claude/hooks/porter-session-start.js` returns 0 (query construction)
    - `echo '{"session_id":"ssh-test-1","cwd":"/home/lobster/projects/Porter"}' | node /home/lobster/.claude/hooks/porter-session-start.js` exits 0 and prints non-empty stdout (containing the API context including `## Silo: Software Development` if Porter Fastify is running)
    - `echo '' | node /home/lobster/.claude/hooks/porter-session-start.js` still exits 0 (fallback path)
    - `grep -q "stdin payload missing" /home/lobster/.claude/hooks/porter-session-start.js` returns 0 (fallback warn present)
  </acceptance_criteria>
  <done>
    Hook reads stdin event payload, extracts session_id + cwd, forwards both to /context. Fallback to project-only call preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Version bump v6.11.0 → v6.12.0 + CHANGELOG entry (all 8 locations including admin/)</name>
  <files>backend/src/index.ts, backend/src/routes/v1/version.ts (or wherever version is served), CHANGELOG.md, admin/ string constants</files>
  <read_first>
    - CLAUDE.md (Porter/CLAUDE.md ship process section + global CLAUDE.md "Version bump locations" list)
    - backend/src/index.ts (find current version string — search for "v6.11" or "VERSION")
    - CHANGELOG.md (most recent entry style)
  </read_first>
  <action>
**Step 1 — Find ALL current occurrences of v6.11.0 across the repo (backend + admin):**

```bash
grep -rn "v6\\.11\\.0\\|6\\.11\\.0" /home/lobster/projects/Porter/backend/src /home/lobster/projects/Porter/CHANGELOG.md /home/lobster/projects/Porter/admin 2>/dev/null | grep -v node_modules | grep -v dist | grep -v ".d.ts" | head -50
```

**Step 2 — BLOCKER 4: explicitly grep admin/ for version strings.**

```bash
grep -rn "v6\\.11\\.0" /home/lobster/projects/Porter/admin 2>/dev/null | grep -v node_modules | grep -v dist
```

For EACH hit, update to `v6.12.0`. The admin frontend version badge (location 7) and admin page version (location 8) MUST be updated — Porter/CLAUDE.md mandates all 8 ship locations are non-negotiable. **Do not defer** these locations on assumption that "no frontend rebuild needed" — the string constants must update even without a rebuild.

If the admin grep returns ZERO matches, document this empirically (e.g., "admin reads version from `/health` at runtime — no hardcoded constants exist"). Only then is it acceptable to skip locations 7-8. Record the empirical finding in the task summary.

For each hit elsewhere, update to v6.12.0. Be careful: changelog entries for past versions must NOT be edited — only NEW entries get added.

Per Porter/CLAUDE.md, the 8 locations:
  1. Backend docstring/comment at top of index.ts
  2. Backend startup log message
  3. backend/src/routes/v1/version.ts — GET /api/version response
  4. SSE version metadata (search for version in SSE handlers)
  5. /health endpoint porter_version field
  6. CHANGELOG.md — add new entry
  7. Admin frontend version badge (string constant) — update if grep finds occurrences
  8. Admin page footer version (string constant) — update if grep finds occurrences

If the backend uses a single shared CURRENT_VERSION constant referenced from multiple places, update it ONCE and ensure all 8 logical surfaces consume from it.

Add a new CHANGELOG.md entry at the top of the unreleased / latest section:

    ## v6.12.0 — 2026-05-11 — Phase 48.1 Silo Foundation

    **Added**
    - `silos` registry table with software seed row (DRM-01)
    - `session_silo_overrides` table for per-CLI-session silo override (DRM-04)
    - `directive_immutable_moe_direct` Postgres trigger protecting moe-direct directives (DRM-05)
    - `silo-detector` service: deterministic silo detection from cwd + project type + cwd_markers (DRM-03)
    - `/api/v1/intellect/context` now injects `## Silo: <name> — Operating Rules` section between system and project directives (DRM-02)
    - `POST /api/v1/intellect/silo-command` endpoint for `/silo software` / `/silo none` / `/silo` (status)
    - UserPromptSubmit hook intercepts `/silo` slash commands and emits `decision:block` JSON
    - SessionStart hook reads stdin payload and forwards `session_id` + `cwd` to /context

    **Internal**
    - In-process silos cache warmed at startup, reloadable via `reloadSiloCache()`
    - Bypass for moe-direct mutation: `SET LOCAL porter.allow_moe_direct_mutation = 'true'` per-tx

Verify build still passes after edits:

    cd backend && npm run build 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -q "v6\\.12\\.0" /home/lobster/projects/Porter/CHANGELOG.md && grep -q "v6\\.12\\.0" /home/lobster/projects/Porter/backend/src/index.ts && cd /home/lobster/projects/Porter/backend && npm run build 2>&1 | tail -3 | grep -qv "error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v6\\.12\\.0" /home/lobster/projects/Porter/CHANGELOG.md` returns >= 1
    - `grep -c "v6\\.12\\.0" /home/lobster/projects/Porter/backend/src/index.ts` returns >= 1
    - `grep -c "v6\\.11\\.0" /home/lobster/projects/Porter/backend/src/index.ts` returns 0 (no stale references)
    - `cd backend && npm run build` exits 0
    - CHANGELOG entry includes mentions of DRM-01 through DRM-05 (`grep -c "DRM-0" CHANGELOG.md` returns >= 5)
    - Phase 48.1 entry is at the top of CHANGELOG.md (above all v6.11.x entries)
    - **BLOCKER 4 — admin/ version grep performed empirically:** `grep -rn "v6\\.11\\.0" /home/lobster/projects/Porter/admin 2>/dev/null | grep -v node_modules | grep -v dist` was run; result documented in task summary.
    - **BLOCKER 4 — admin/ string constants updated OR documented as nonexistent:** EITHER `grep -rc "v6\\.12\\.0" /home/lobster/projects/Porter/admin 2>/dev/null | grep -v node_modules | grep -v dist | awk -F: '{s+=$2} END {exit !(s>=1)}'` exits 0 (at least one admin file updated) OR the task summary explicitly states "grep -rn 'v6.11.0' admin/ returns zero matches — admin reads version from /health, no string constants to update" with the empirical grep output attached.
    - **No stale v6.11.0 anywhere in admin/:** `grep -rc "v6\\.11\\.0" /home/lobster/projects/Porter/admin 2>/dev/null | grep -v node_modules | grep -v dist | awk -F: '{s+=$2} END {exit !(s==0)}'` exits 0.
  </acceptance_criteria>
  <done>
    All version references at v6.12.0, build clean, CHANGELOG describes the phase. **Admin/ empirically grep'd and either updated (string constants found) or documented as runtime-served (no constants).**
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ship process — build, restart, /health, smoke</name>
  <files>(verification — no source changes)</files>
  <read_first>
    - CLAUDE.md (Porter ship process)
    - Porter/CLAUDE.md (Porter-specific ship variant)
    - tests/smoke-48.1.sh
  </read_first>
  <action>
Execute the Porter ship cycle. NO frontend build needed for 48.1 (admin string constants updated in Task 2 are read at next admin build cycle, but no rebuild required for this phase to function).

    cd /home/lobster/projects/Porter/backend && npm run build
    pkill -9 -f "porter/backend" 2>/dev/null || true
    sleep 4
    systemctl --user restart porter-fastify
    sleep 8
    curl -sf http://127.0.0.1:3001/health
    echo ""

Confirm health JSON contains "v6.12.0":

    curl -sf http://127.0.0.1:3001/health | grep -o '"porter_version":"[^"]*"\\|"version":"[^"]*"'

Run the full smoke harness one more time. ALL 6 SCs must be green:

    bash /home/lobster/projects/Porter/tests/smoke-48.1.sh

Then run the hook end-to-end with stdin payload to confirm session-start integration:

    echo '{"session_id":"final-sanity-1","cwd":"/home/lobster/projects/Porter"}' | node /home/lobster/.claude/hooks/porter-session-start.js | grep -c "## Silo: Software Development"
    # expect: 1

And the negative case:

    echo '{"session_id":"final-sanity-2","cwd":"/home/lobster/projects/Funds"}' | node /home/lobster/.claude/hooks/porter-session-start.js | grep -c "## Silo:"
    # expect: 0
  </action>
  <verify>
    <automated>bash /home/lobster/projects/Porter/tests/smoke-48.1.sh 2>&1 | tail -3 | grep -q "all checks green" && curl -sf http://127.0.0.1:3001/health | grep -q "v6.12.0"</automated>
  </verify>
  <acceptance_criteria>
    - `systemctl --user is-active porter-fastify` returns "active"
    - `curl -sf http://127.0.0.1:3001/health` returns 200 with version "v6.12.0" in body
    - `bash tests/smoke-48.1.sh` prints "all checks green (SC-1..SC-6)" and exits 0
    - Hook stdin smoke (cwd=Porter): output contains `## Silo: Software Development`
    - Hook stdin smoke (cwd=Funds): output contains zero `## Silo:` lines
    - Backend startup log contains `[migrate-silos-v1] complete` AND `[silo-detector] cache loaded — 1 enabled silo(s)`
  </acceptance_criteria>
  <done>
    Ship complete, all 6 SCs green, hook end-to-end injection verified by simulating stdin payload.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN VERIFY — fresh Claude CLI session shows the silo header</name>
  <files>(no files — human verification only)</files>
  <action>Pause execution and present the verification steps in <how-to-verify> below to the user. Wait for resume signal. Do not proceed past this checkpoint without explicit "approved" from Moe.</action>
  <verify>(manual verification — see how-to-verify below; checkpoint resolves when user types "approved")</verify>
  <done>Moe types "approved" after running all 5 manual verification steps in a live Claude CLI session.</done>
  <what-built>
    Phase 48.1 complete: silos table seeded, immutability trigger live, context endpoint injects software silo for code projects, /silo command persists override per session, session-start hook forwards session_id+cwd. Version v6.12.0 shipped.
  </what-built>
  <how-to-verify>
    Manual verifications per VALIDATION.md "Manual-Only Verifications" table — cannot be automated because they require a live Claude CLI subprocess:

    1. Open a NEW Claude Code CLI session in /home/lobster/projects/Porter (e.g., new terminal, new `claude` invocation).
       - Expected: SessionStart output (visible in /context or in the very first system message digest) contains `## Silo: Software Development — Operating Rules` followed by the 5 silo-sw-* directive bullets.
       - PASS if the header AND at least one directive body line are visible.

    2. Open a NEW Claude Code CLI session in /home/lobster/projects/Funds (or any non-code project — Flywheel, Bitcoin Yield, etc.).
       - Expected: NO `## Silo:` header in the SessionStart output.
       - PASS if zero silo headers appear.

    3. In the Funds session from step 2, type: `/silo software`
       - Expected: a short confirmation message appears (e.g., "/silo software — override applied for this session"); the next /context query for this session injects software silo even though cwd is non-code. The raw prompt should NOT reach the model (Claude should not respond to "/silo software" as if it were a question).
       - Optional verification: in another terminal, run `psql -d porter -c "SELECT * FROM session_silo_overrides ORDER BY set_at DESC LIMIT 3"` and confirm a row with your session_id and silo_id='software'.
       - PASS if confirmation appears AND override is persisted in DB.

    4. In the same Funds session, type: `/silo none`
       - Expected: confirmation that the override was cleared; subsequent /context calls return no silo section.
       - PASS if confirmation appears AND DB row has silo_id=NULL.

    5. Try `psql -d porter -c "UPDATE directives SET content='hacked' WHERE source_type='moe-direct'"` directly.
       - Expected: ERROR mentioning `directive_immutable_moe_direct` or "moe-direct directives are sealed".
       - PASS if the error appears AND `psql -c "SELECT content FROM directives WHERE id='silo-sw-design-system'"` still shows the original content.

    Report back which steps passed/failed. If all 5 pass, type "approved". If any fail, paste the exact output.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 manual checks pass, otherwise describe which step failed and the observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- All 6 success criteria pass via `bash tests/smoke-48.1.sh`
- Version v6.12.0 reflected in /health
- Admin/ string constants empirically grep'd and updated or documented
- Manual checkpoint approval received from Moe
</verification>

<success_criteria>
Phase 48.1 complete:
- DRM-01: silos table + software seed live
- DRM-02: /context injects scope='silo' directives in labeled section
- DRM-03: cwd-based detection at session start (project_type + cwd_markers, null fallback)
- DRM-04: /silo CLI command persists per-session override; /silo none clears
- DRM-05: moe-direct directives sealed at DB layer with bypass available

Porter v6.12.0 shipped. Fresh CLI session in Porter shows silo header; fresh session in non-code project does not.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-silo-foundation/48.1-04-SUMMARY.md` with:
- Final smoke output (all 6 SCs green)
- Hook stdin verification output
- Version v6.12.0 confirmation
- Admin/ version grep result + actions taken
- Manual checkpoint sign-off
</output>
</output>
