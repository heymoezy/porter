---
phase: 48.1
slug: silo-foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
revised: 2026-05-11
---

# Phase 48.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bash smoke tests (matches `tests/smoke-phase*.sh` pattern) + Playwright (`tests/`, npx playwright 1.x) for API-level scenarios |
| **Config file** | `tests/playwright.config.ts` (exists); smoke scripts are standalone |
| **Quick run command** | `bash tests/smoke-48.1.sh` |
| **Full suite command** | `bash tests/smoke-48.1.sh && cd tests && npx playwright test --grep "silo"` |
| **Estimated runtime** | ~15 seconds (smoke) + ~30 seconds (Playwright if added) |

---

## Sampling Rate

- **After every task commit:** Run `bash tests/smoke-48.1.sh`
- **After every plan wave:** Run `bash tests/smoke-48.1.sh && cd tests && npx playwright test --grep "silo"`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 45 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 48.1-05-01 | 05 (Smoke harness) | 0 | DRM-01..DRM-05 | smoke (script creation) | `bash -n tests/smoke-48.1.sh && test -x tests/smoke-48.1.sh` | ❌ W0 | ⬜ pending |
| 48.1-01-01 | 01 (Schema) | 1 | DRM-01 | smoke | `psql -d porter -c "SELECT id, enabled FROM silos WHERE id='software';" \| grep -q "software \| t"` | ❌ W0 | ⬜ pending |
| 48.1-01-02 | 01 (Schema) | 1 | DRM-01 | smoke | `psql -d porter -c "\d silos" \| grep -E "(prompt_path\|cadence_seconds\|detect_rules\|default_model)" \| wc -l` should be 4 | ❌ W0 | ⬜ pending |
| 48.1-01-03 | 01 (Schema) | 1 | DRM-05 | smoke | `psql -d porter -c "UPDATE directives SET content='x' WHERE source_type='moe-direct' AND id='silo-sw-design-system';" 2>&1 \| grep -q "directive_immutable"` | ❌ W0 | ⬜ pending |
| 48.1-01-03b | 01 (Schema) | 1 | DRM-05 | smoke (BLOCKER 3 positive test) | `psql -d porter -tAc "UPDATE directives SET verified_at=EXTRACT(EPOCH FROM NOW()) WHERE source_type='system' AND id=(SELECT id FROM directives WHERE source_type='system' LIMIT 1)"` → command tag `UPDATE 1` (no error). Guarded by `SELECT count(*) FROM directives WHERE source_type='system' >= 1`; surrogate insert/update/delete probe if no system rows exist. Proves non-moe-direct UPDATE survives (memory-pruner-safe). | ❌ W0 | ⬜ pending |
| 48.1-02-01 | 02 (Detection + Context) | 2 | DRM-02 | smoke | `curl -s "http://127.0.0.1:3001/api/v1/intellect/context?project=Porter&cwd=/home/lobster/projects/Porter" \| grep -c "## Silo: Software Development"` returns `1` | ❌ W0 | ⬜ pending |
| 48.1-02-02 | 02 (Detection + Context) | 2 | DRM-03 | smoke | `curl -s "http://127.0.0.1:3001/api/v1/intellect/context?project=Funds&cwd=/home/lobster/projects/Funds" \| grep -c "## Silo:"` returns `0` | ❌ W0 | ⬜ pending |
| 48.1-02-02b | 02 (Detection + Context) | 2 | DRM-03 | smoke (BLOCKER 2 null-return path) | `curl -s "http://127.0.0.1:3001/api/v1/intellect/context?project=Porter" \| grep -c "## Silo:"` returns `0` — no cwd param must yield zero silo sections (backward compat). Also asserted as SC-4b in smoke-48.1.sh. | ❌ W0 | ⬜ pending |
| 48.1-02-03 | 02 (Detection + Context) | 2 | DRM-02 | smoke | The 5 silo-sw-* directive contents appear in the context payload for the Porter cwd test | ❌ W0 | ⬜ pending |
| 48.1-03-01 | 03 (`/silo` command + override store) | 3 | DRM-04 | smoke | POST `/api/v1/intellect/silo-command {session_id, silo:'software'}` → next GET `/context?session_id=…` from non-code cwd still injects software silo | ❌ W0 | ⬜ pending |
| 48.1-03-02 | 03 (`/silo` command + override store) | 3 | DRM-04 | smoke | POST `/api/v1/intellect/silo-command {session_id, silo:'none'}` → next GET `/context?session_id=…` from Funds cwd returns 0 silo sections | ❌ W0 | ⬜ pending |
| 48.1-03-03 | 03 (`/silo` command + override store) | 3 | DRM-04 | unit (WARNING 1 + WARNING 3 — empirical block-format guard, Risk 2 retirement) | `echo '{"prompt":"/silo","session_id":"t1"}' \| node ~/.claude/hooks/porter-user-prompt.js \| jq -r '.decision' \| grep -q '^block$'` — if this fails, halt Plan 03 ship and update hook block format. | ❌ W0 | ⬜ pending |
| 48.1-04-01 | 04 (Session-start integration) | **4** | DRM-02, DRM-03 | smoke | Simulate `SessionStart` event payload via stdin → hook output includes "Silo: Software Development" header when cwd=Porter | ❌ W0 | ⬜ pending |
| 48.1-04-02 | 04 (Session-start integration) | **4** | (Porter ship — BLOCKER 4) | smoke | Admin/ version grep performed: `grep -rn "v6\\.11\\.0" admin/` returns either updated files OR empirical documentation that admin reads version from /health. No stale v6.11.0 remains. | ❌ W0 | ⬜ pending |
| 48.1-04-03 | 04 (Session-start integration) | **4** | DRM-01..DRM-05 | smoke | `bash tests/smoke-48.1.sh` prints "all checks green (SC-1..SC-6)" — full end-to-end. Includes SC-4b (no-cwd) and SC-6 row-existence guard. | ❌ W0 | ⬜ pending |
| 48.1-04-04 | 04 (Session-start integration) | **4** | DRM-02..DRM-05 | manual checkpoint | 5-step live CLI session verification (see Manual-Only Verifications table) | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Wave assignments (post-revision):**
- Wave 0 — Plan 05 (smoke harness)
- Wave 1 — Plan 01 (schema)
- Wave 2 — Plan 02 (detection + context)
- Wave 3 — Plan 03 (silo-command + hook)
- **Wave 4 — Plan 04 (session-start hook + version bump + final smoke).** Plan 04 was moved from wave 3 to wave 4 per BLOCKER 1: Task 3's `bash tests/smoke-48.1.sh` runs SC-5 (`/silo` override), which only works after Plan 03 has shipped. Plan 04 now `depends_on: [48.1-02, 48.1-03]`.

---

## Wave 0 Requirements

- [ ] `tests/smoke-48.1.sh` — new bash smoke script covering SC-1..SC-6. Idempotent. Exits non-zero on first failure. Includes setup (curl against running :3001) and teardown (delete test session_silo_overrides rows). Includes SC-4b (no-cwd backward-compat) and SC-6 row-existence guard.
- [ ] `tests/silo-foundation.spec.ts` — optional Playwright spec for API-level scenarios (SC-2, SC-4, SC-5). Skip if smoke covers it adequately.
- [x] Verify Claude Code `UserPromptSubmit` hook block decision format against Claude Code hook docs — **resolved by Plan 03 Task 3's empirical guard**: `echo '{"prompt":"/silo","session_id":"probe"}' | node ~/.claude/hooks/porter-user-prompt.js | jq -r '.decision'` must return `block`; if not, Plan 03 cannot ship until hook is updated with the correct format. Risk 2 retired empirically inside Plan 03 rather than pre-emptively.
- [ ] No framework install needed — Playwright already configured at `tests/playwright.config.ts`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Fresh Claude CLI session in `/home/lobster/projects/Porter` actually shows silo directives | DRM-02, DRM-03 | Can't fully simulate the live CLI subprocess from a unit test; must visually confirm hook output | Open a new CLI session via `claude` in the Porter directory; grep the SessionStart output for "Silo: Software Development". Pass if present. |
| Fresh Claude CLI session in a non-code project (e.g., `Funds`, `Flywheel`) shows no silo directives | DRM-03 | Same as above — needs live subprocess | Open a new CLI session in `/home/lobster/projects/Funds`; grep for "Silo:". Pass if 0 matches. |
| User typing `/silo software` interactively flips the silo for that session | DRM-04 | Slash command requires actual interactive Claude CLI session | In a Funds session, type `/silo software`. Verify the next prompt gets software-silo directives injected (visible in next SessionStart-like context reload, or via curl to `/context?session_id=`). |
| Memory pruner UPDATE on non-moe-direct rows still works | DRM-05 (negative — must NOT block legitimate dedup) | Triggered by background service; explicit positive test added in Plan 01 Task 3 acceptance criteria (BLOCKER 3) | `psql -d porter -tAc "UPDATE directives SET verified_at=EXTRACT(EPOCH FROM NOW()) WHERE source_type='system' AND id=(SELECT id FROM directives WHERE source_type='system' LIMIT 1)"` returns `UPDATE 1` (no error). Surrogate insert/update/delete probe if no system rows exist. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/smoke-48.1.sh`, optional Playwright)
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner completes and checker approves)
- [ ] **Revision check (2026-05-11):** All 4 BLOCKERS + Warnings 1, 3, 4, 5, 6 addressed in plan files.

**Approval:** pending (post-revision)
