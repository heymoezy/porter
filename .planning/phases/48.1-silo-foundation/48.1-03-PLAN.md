---
phase: 48.1-silo-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 48.1-02
files_modified:
  - backend/src/routes/v1/intellect.ts
  - /home/lobster/.claude/hooks/porter-user-prompt.js
autonomous: true
requirements:
  - DRM-04
must_haves:
  truths:
    - "POST /api/v1/intellect/silo-command accepts {session_id, command} and persists to session_silo_overrides"
    - "Command '/silo <name>' upserts {session_id, silo_id=<name>, set_at=NOW()}"
    - "Command '/silo none' upserts {session_id, silo_id=NULL, set_at=NOW()} (explicit clear)"
    - "Command '/silo' (no arg) returns the current effective silo + source (override vs detected) without writing"
    - "UserPromptSubmit hook intercepts any prompt starting with '/silo', POSTs to backend, emits {decision:'block'} JSON to stdout"
    - "Hook emits additionalContext with the backend's confirmation text so the user sees feedback"
    - "Non-silo prompts pass through unmodified (existing /correction behavior preserved)"
    - "Hook block format is empirically verified: piping /silo through the hook produces decision=block (Risk 2 retired)"
  artifacts:
    - path: "backend/src/routes/v1/intellect.ts"
      provides: "POST /silo-command endpoint"
      contains: "/silo-command"
    - path: "/home/lobster/.claude/hooks/porter-user-prompt.js"
      provides: "/silo slash-command interception"
      contains: "/silo"
  key_links:
    - from: "porter-user-prompt.js"
      to: "POST /api/v1/intellect/silo-command"
      via: "synchronous HTTP request before stdout JSON"
      pattern: "silo-command"
    - from: "POST /silo-command"
      to: "session_silo_overrides table"
      via: "UPSERT ON CONFLICT (session_id) DO UPDATE"
      pattern: "ON CONFLICT.*session_id.*DO UPDATE"
---

<objective>
Make the `/silo` slash command a real interactive control. Users type `/silo software`, `/silo none`, or `/silo` in any Claude CLI session; the UserPromptSubmit hook intercepts before the model sees it, calls a new backend endpoint, persists the override, and echoes a confirmation. Plan 02's detector already reads `session_silo_overrides` — this plan makes it write to it.

Purpose: DRM-04. Closes the override loop end-to-end. After this plan ships, Moe can type `/silo software` in `/home/lobster/projects/Funds` and force software-silo injection for that session.
Output: New backend endpoint + extended UserPromptSubmit hook. SC-5 turns green.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48.1-silo-foundation/48.1-CONTEXT.md
@.planning/phases/48.1-silo-foundation/48.1-RESEARCH.md
@.planning/phases/48.1-silo-foundation/48.1-VALIDATION.md
@.planning/phases/48.1-silo-foundation/48.1-01-SUMMARY.md
@.planning/phases/48.1-silo-foundation/48.1-02-SUMMARY.md

<interfaces>
Current UserPromptSubmit hook surface (~/.claude/hooks/porter-user-prompt.js, ~61 lines):
  1. Reads stdin JSON: { prompt, session_id, ... }
  2. If prompt.length less than 12, exits silently
  3. POSTs to /api/v1/intellect/correction (fire-and-forget)
  4. exit 0 (never writes to stdout)

New requirement: BEFORE the length check / correction post, detect "/silo" prefix. If matched:
  - Build POST body: { session_id, command: '<the full /silo line>' }
  - Synchronously POST to http://127.0.0.1:3001/api/v1/intellect/silo-command
  - Read JSON response: { ok, message, current_silo }
  - Write to stdout EXACTLY this JSON (Claude Code UserPromptSubmit block format):
      { "decision": "block", "reason": "<message>", "hookSpecificOutput": { "hookEventName": "UserPromptSubmit", "additionalContext": "<message>" } }
  - process.exit(0)

NOTE on the block format: per Claude Code docs, { "decision": "block", "reason": "..." } prevents the prompt from reaching the model. Embedding hookSpecificOutput.additionalContext provides feedback that gets shown inline. RESEARCH.md Risk 2 flags this format as not yet tested in this codebase. **Task 3 of this plan retires Risk 2 by piping `/silo` through the hook and asserting `.decision == "block"` empirically.** If the assertion fails, halt the plan and document the actual hook block-format that does work — do not ship Plan 03 if /silo passes through to the model.

The session_id from stdin IS the Claude CLI session_id (per RESEARCH Investigation 5).

The HTTP call must use Node 18+ global fetch with a 3000ms timeout so a hung backend cannot lock the prompt pipeline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add POST /api/v1/intellect/silo-command endpoint</name>
  <files>backend/src/routes/v1/intellect.ts</files>
  <read_first>
    - backend/src/routes/v1/intellect.ts (full file — find existing POST handlers to mirror auth/body parsing pattern)
    - .planning/phases/48.1-silo-foundation/48.1-01-SUMMARY.md (confirm session_silo_overrides shape)
    - .planning/phases/48.1-silo-foundation/48.1-02-SUMMARY.md (confirm detectSilos signature)
    - backend/src/services/intellect/silo-detector.ts (the detector module from Plan 02)
  </read_first>
  <behavior>
    - POST /api/v1/intellect/silo-command with body { session_id, command }
    - "/silo none"     to UPSERT session_silo_overrides {session_id, silo_id: null, set_at: NOW()}
    - "/silo <name>"   to validate <name> against silos.id (must exist + enabled). UPSERT with silo_id=<name>. If unknown, return 400 with available list.
    - "/silo" (no arg) to no write; read current row and return status.
    - Response shape: { ok: boolean, message: string, current_silo: string | null, source: 'override' | 'detected' | 'none' }
  </behavior>
  <action>
**Pre-step (WARNING 5): determine auth posture for new route.**

Run:
```bash
grep -n "preHandler\\|auth\\|onRequest" backend/src/routes/v1/intellect.ts
```

If the file's route registration applies auth middleware (e.g., `preHandler: requireAuth` on a wrapper, or `fastify.register(..., { onRequest: ... })`), the new POST route inherits it — no extra work. If NO auth middleware is present, leave the route open (127.0.0.1-only via server bind) AND add this one-line comment immediately above the new route handler:

```typescript
// 127.0.0.1-only; relies on server bind for protection. See README for production auth plans.
```

Record the empirical finding (auth inherited vs comment added) in the task summary.

Open backend/src/routes/v1/intellect.ts and add a new POST route inside the existing route-registration function. Place it adjacent to other POST handlers (e.g., near /correction). Use the existing route style — likely fastify.post('/silo-command', async (req, reply) => {...}).

Ensure detectSilos is imported at top of file (if not already added in Plan 02):

    import { detectSilos } from '../../services/intellect/silo-detector.js';

Add this exact handler verbatim (the JS template literals use backtick — preserve them):

    // Phase 48.1: /silo CLI slash-command handler
    // 127.0.0.1-only; relies on server bind for protection. (Remove this comment if route inherits auth.)
    fastify.post('/silo-command', async (req, reply) => {
      const body = (req.body || {}) as { session_id?: string; command?: string };
      const sessionId = (body.session_id || '').trim();
      const command   = (body.command   || '').trim();

      if (!sessionId) return reply.code(400).send({ ok: false, message: 'session_id required' });
      if (!command.startsWith('/silo')) return reply.code(400).send({ ok: false, message: "command must start with /silo" });

      // Parse: "/silo", "/silo software", "/silo none"
      const arg = command.slice('/silo'.length).trim().toLowerCase();

      // Case A — status query (no argument)
      if (arg === '') {
        const overrideRes = await pool.query(
          `SELECT silo_id, set_at FROM session_silo_overrides
           WHERE session_id = $1 AND set_at > NOW() - INTERVAL '24 hours'`,
          [sessionId],
        );
        if (overrideRes.rowCount && overrideRes.rowCount > 0) {
          const row = overrideRes.rows[0] as { silo_id: string | null };
          if (row.silo_id === null) {
            return reply.send({ ok: true, message: 'Silo override: none (explicitly cleared)', current_silo: null, source: 'override' });
          }
          return reply.send({ ok: true, message: 'Silo override: ' + row.silo_id, current_silo: row.silo_id, source: 'override' });
        }
        return reply.send({ ok: true, message: 'No override set; silo detected from cwd', current_silo: null, source: 'detected' });
      }

      // Case B — explicit clear
      if (arg === 'none') {
        await pool.query(
          `INSERT INTO session_silo_overrides (session_id, silo_id, set_at)
           VALUES ($1, NULL, NOW())
           ON CONFLICT (session_id) DO UPDATE SET silo_id = NULL, set_at = NOW()`,
          [sessionId],
        );
        return reply.send({ ok: true, message: '/silo none — override cleared for this session', current_silo: null, source: 'override' });
      }

      // Case C — set a silo by name. Validate against silos.id
      const siloName = arg;
      const validRes = await pool.query(
        `SELECT id, display_name FROM silos WHERE id = $1 AND enabled = TRUE LIMIT 1`,
        [siloName],
      );
      if (!validRes.rowCount) {
        const availRes = await pool.query(`SELECT id FROM silos WHERE enabled = TRUE ORDER BY id`);
        const avail = (availRes.rows as Array<{id: string}>).map((r) => r.id).join(', ') || '(none enabled)';
        return reply.code(400).send({ ok: false, message: "Unknown silo '" + siloName + "'. Available: " + avail });
      }

      await pool.query(
        `INSERT INTO session_silo_overrides (session_id, silo_id, set_at)
         VALUES ($1, $2, NOW())
         ON CONFLICT (session_id) DO UPDATE SET silo_id = $2, set_at = NOW()`,
        [sessionId, siloName],
      );

      return reply.send({
        ok: true,
        message: '/silo ' + siloName + ' — override applied for this session',
        current_silo: siloName,
        source: 'override',
      });
    });

NOTES:
- Reuses the `pool` variable already in scope in intellect.ts (same one used in /context).
- Uses silos.id (not display_name) as the canonical reference — matches the silos table PK.
- ON CONFLICT clauses are required because session_silo_overrides.session_id is the PK.
- The validator checks enabled = TRUE so disabled silos cannot be overridden into.
- TTL (24h) is implicit at READ time. Stale rows simply get ignored at read time.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10; grep -q "/silo-command" backend/src/routes/v1/intellect.ts && grep -q "ON CONFLICT (session_id) DO UPDATE" backend/src/routes/v1/intellect.ts && grep -q "Unknown silo" backend/src/routes/v1/intellect.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "fastify.post.*silo-command" backend/src/routes/v1/intellect.ts` returns 0
    - `grep -q "ON CONFLICT (session_id) DO UPDATE" backend/src/routes/v1/intellect.ts` returns 0
    - `grep -q "WHERE id = \\$1 AND enabled = TRUE" backend/src/routes/v1/intellect.ts` returns 0
    - `grep -q "Unknown silo" backend/src/routes/v1/intellect.ts` returns 0
    - `grep -q "session_silo_overrides" backend/src/routes/v1/intellect.ts` returns 0
    - **Auth posture documented (WARNING 5):** `grep -n "preHandler\\|auth\\|onRequest" backend/src/routes/v1/intellect.ts` was run; finding noted in task summary. If no auth middleware exists, the new POST handler carries the `// 127.0.0.1-only` comment. Verify: `grep -B1 "fastify.post.*silo-command" backend/src/routes/v1/intellect.ts` shows the 127.0.0.1-only comment OR the file uses an auth middleware visible in the grep output.
    - `cd backend && npx tsc --noEmit` exits 0 (no new errors)
  </acceptance_criteria>
  <done>
    Endpoint registered, parses /silo / /silo none / /silo <name>, UPSERTs override, validates silo id, returns structured JSON. Auth posture empirically verified (either inherited from existing middleware or documented as 127.0.0.1-only via inline comment).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend porter-user-prompt.js hook to intercept /silo</name>
  <files>/home/lobster/.claude/hooks/porter-user-prompt.js</files>
  <read_first>
    - /home/lobster/.claude/hooks/porter-user-prompt.js (full current file — preserve its structure and the /correction call)
    - /home/lobster/.claude/hooks/gsd-context-monitor.js lines 140-156 (additionalContext output pattern)
    - .planning/phases/48.1-silo-foundation/48.1-RESEARCH.md (Investigation 5 + Planning Blocker #1)
  </read_first>
  <behavior>
    - When stdin JSON's prompt starts with "/silo" (after trim):
        - Call POST http://127.0.0.1:3001/api/v1/intellect/silo-command with { session_id, command: prompt } and a 3000ms timeout.
        - On success: write { "decision": "block", "reason": "<message>", "hookSpecificOutput": { "hookEventName": "UserPromptSubmit", "additionalContext": "<message>" } } to stdout and exit 0.
        - On HTTP failure / timeout: still emit a block decision with message "silo command backend unreachable" so the raw /silo line is not sent to the model.
    - When stdin JSON's prompt does NOT start with /silo: run the existing /correction logic unchanged.
    - Never throw; on any unexpected error, log to stderr and exit 0 (keep the hook non-fatal — Claude prompt pipeline must continue).
  </behavior>
  <action>
First, read the existing /home/lobster/.claude/hooks/porter-user-prompt.js in full to preserve its style (CommonJS or ES module, fetch wrapper style, stderr logging conventions).

At the top of the main handler — AFTER stdin JSON parse, BEFORE the existing prompt.length check — insert this exact block:

    // ─── Phase 48.1: /silo slash command interception ──────────────────────
    const rawPrompt = (data.prompt || '').trim();
    if (rawPrompt.startsWith('/silo')) {
      const sessionId = data.session_id || '';
      const cmd = rawPrompt;

      let message = 'silo command backend unreachable';
      try {
        const ac = new AbortController();
        const timeout = setTimeout(() => ac.abort(), 3000);
        const resp = await fetch('http://127.0.0.1:3001/api/v1/intellect/silo-command', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ session_id: sessionId, command: cmd }),
          signal: ac.signal,
        });
        clearTimeout(timeout);
        const j = await resp.json().catch(() => ({}));
        if (j && typeof j.message === 'string') {
          message = j.message;
        } else if (!resp.ok) {
          message = `silo command failed (HTTP ${resp.status})`;
        } else {
          message = 'silo command accepted';
        }
      } catch (e) {
        try { process.stderr.write(`[porter-user-prompt] /silo failed: ${e && e.message ? e.message : e}\n`); } catch {}
      }

      // Block the raw /silo command from reaching the model + echo feedback as additionalContext
      const out = {
        decision: 'block',
        reason: message,
        hookSpecificOutput: {
          hookEventName: 'UserPromptSubmit',
          additionalContext: message,
        },
      };
      process.stdout.write(JSON.stringify(out));
      process.exit(0);
    }
    // ─── end /silo interception ────────────────────────────────────────────

Adapt syntax to match the existing module style:
- If the file is CommonJS (`require`, no top-level await), wrap the fetch + parse in an `(async () => { ... })()` IIFE OR convert the main handler to async if it isn't already. The existing /correction call is fire-and-forget; the /silo path needs to be awaitable. If the current handler is sync, the entire silo block must be inside an async wrapper that the file already provides for stdin reading.
- If `fetch` is not globally available in the Node version running the hook, use the same fetch shim already imported by the file (e.g., `node-fetch` or a `http.request` helper). Inspect the existing /correction POST to see which transport is used and reuse it.

Preserve the existing /correction call path exactly — only add the early-return /silo branch above it.

After editing, validate JSON output by piping a test prompt through the hook:

    echo '{"prompt":"/silo software","session_id":"hook-test-1"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js | jq -r '.decision'

Expected stdout: `block`. If `jq` reports a parse error, fix the hook to emit valid JSON ONLY (no extra stderr-confused-with-stdout writes).
  </action>
  <verify>
    <automated>echo '{"prompt":"/silo","session_id":"sanity-check-1"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js | jq -r '.decision' | grep -q '^block$'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check /home/lobster/.claude/hooks/porter-user-prompt.js` exits 0 (syntactically valid)
    - `grep -q "/silo" /home/lobster/.claude/hooks/porter-user-prompt.js` returns 0
    - `grep -q "silo-command" /home/lobster/.claude/hooks/porter-user-prompt.js` returns 0
    - `grep -q "decision.*block\\|'decision': 'block'\\|\"decision\":\"block\"" /home/lobster/.claude/hooks/porter-user-prompt.js` returns 0 (one of these spellings is present)
    - `grep -q "hookSpecificOutput" /home/lobster/.claude/hooks/porter-user-prompt.js` returns 0
    - `grep -q "additionalContext" /home/lobster/.claude/hooks/porter-user-prompt.js` returns 0
    - **WARNING 1 live-pipe test:** `echo '{"prompt":"/silo","session_id":"sanity-check-1"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js | jq -r '.decision'` returns exactly `block` (stdout-only, no parse errors)
    - Existing /correction path preserved: `grep -q "/api/v1/intellect/correction" /home/lobster/.claude/hooks/porter-user-prompt.js` still returns 0
    - Non-silo prompt smoke test: `echo '{"prompt":"hello world this is long enough to pass","session_id":"sanity-check-2"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js` exits 0 (and does NOT emit a block JSON — output is empty per existing fire-and-forget behavior)
  </acceptance_criteria>
  <done>
    Hook intercepts /silo, calls backend, emits valid block JSON to stdout (live-pipe verified), preserves existing /correction behavior, handles timeouts gracefully.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build + restart + run smoke SC-5 + EMPIRICAL block-format guard</name>
  <files>(verification + ship step)</files>
  <read_first>
    - CLAUDE.md (Porter ship process)
    - tests/smoke-48.1.sh (SC-5 section)
  </read_first>
  <action>
Backend-only ship (no frontend changes):

    cd /home/lobster/projects/Porter/backend && npm run build
    pkill -9 -f "porter/backend" 2>/dev/null || true
    sleep 4
    systemctl --user restart porter-fastify
    sleep 8
    curl -sf http://127.0.0.1:3001/health | head -c 200
    echo ""

**WARNING 3 — Block-format empirical guard (Risk 2 retirement).**

Before any other SC-5 testing, run this assertion:

    HOOK_OUT=$(echo '{"prompt":"/silo","session_id":"block-format-probe"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js)
    DECISION=$(echo "$HOOK_OUT" | jq -r '.decision' 2>/dev/null)
    if [ "$DECISION" != "block" ]; then
      echo "FAIL: hook block-format wrong. stdout was:" >&2
      echo "$HOOK_OUT" >&2
      echo "Expected: .decision == 'block'. Halt and document the actual Claude Code hook block-format that works." >&2
      echo "Acceptable: update porter-user-prompt.js with the correct format and re-run." >&2
      exit 1
    fi
    echo "OK: hook block format empirically verified (decision=block)"

**DO NOT proceed past this guard if it fails.** Update the hook with the correct format (e.g., per https://docs.claude.com/en/docs/claude-code/hooks UserPromptSubmit), re-build, re-run. Do not ship Plan 03 if `/silo` passes through to the model.

Manual SC-5 sanity:

    SID="manual-smoke-$(date +%s)"

    # Set override
    curl -sf -X POST http://127.0.0.1:3001/api/v1/intellect/silo-command \
      -H "Content-Type: application/json" \
      -d "{\"session_id\":\"$SID\",\"command\":\"/silo software\"}"

    # Verify the override forces software silo even from a non-code cwd
    curl -s "http://127.0.0.1:3001/api/v1/intellect/context?project=Funds&cwd=/home/lobster/projects/Funds&session_id=$SID" | grep -c "## Silo: Software Development"
    # expect: 1

    # Clear
    curl -sf -X POST http://127.0.0.1:3001/api/v1/intellect/silo-command \
      -H "Content-Type: application/json" \
      -d "{\"session_id\":\"$SID\",\"command\":\"/silo none\"}"

    # Confirm cleared
    curl -s "http://127.0.0.1:3001/api/v1/intellect/context?project=Funds&cwd=/home/lobster/projects/Funds&session_id=$SID" | grep -c "## Silo:"
    # expect: 0

    # Cleanup
    psql -d porter -c "DELETE FROM session_silo_overrides WHERE session_id='$SID'"

Hook smoke:

    echo "{\"prompt\":\"/silo software\",\"session_id\":\"$SID\"}" | node /home/lobster/.claude/hooks/porter-user-prompt.js | jq -r '.decision'
    # expect: block

Run the full smoke harness — should now print "all checks green (SC-1..SC-6)":

    bash /home/lobster/projects/Porter/tests/smoke-48.1.sh
  </action>
  <verify>
    <automated>HOOK_OUT=$(echo '{"prompt":"/silo","session_id":"block-format-probe"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js); echo "$HOOK_OUT" | jq -r '.decision' | grep -q '^block$' && bash /home/lobster/projects/Porter/tests/smoke-48.1.sh 2>&1 | tail -5 | grep -q "all checks green"</automated>
  </verify>
  <acceptance_criteria>
    - `systemctl --user is-active porter-fastify` returns "active"
    - **WARNING 3 block-format guard:** `echo '{"prompt":"/silo","session_id":"block-format-probe"}' | node /home/lobster/.claude/hooks/porter-user-prompt.js | jq -r '.decision'` returns exactly `block`. If not, hook is updated with the correct Claude Code UserPromptSubmit block format until this assertion passes.
    - POST /api/v1/intellect/silo-command with {session_id, command:"/silo software"} returns 200 with body containing "current_silo":"software"
    - POST with {command:"/silo none"} returns 200 with body containing "current_silo":null
    - POST with {command:"/silo nonsense"} returns 400 with body containing "Unknown silo"
    - GET /context with the session_id used by /silo software shows `## Silo: Software Development` even when cwd is non-code
    - GET /context after /silo none returns zero `## Silo:` matches even when cwd is code
    - Hook stdout contains valid JSON with `decision: "block"` when piped a /silo command
    - `bash tests/smoke-48.1.sh` prints "all checks green (SC-1..SC-6)" and exits 0
  </acceptance_criteria>
  <done>
    All 6 success criteria green. SC-5 (override persistence + clear) verified by smoke. **Hook block format empirically verified by piping /silo to the hook and asserting `.decision == "block"` (Risk 2 retired).**
  </done>
</task>

</tasks>

<verification>
- `cd backend && npx tsc --noEmit` clean
- `node --check ~/.claude/hooks/porter-user-prompt.js` clean
- **Empirical block-format guard: `/silo` piped through hook returns `decision=block` (Risk 2 retired)**
- `bash tests/smoke-48.1.sh` → "all checks green (SC-1..SC-6)"
- Manual /silo software in a Funds session injects software silo on the next /context call
</verification>

<success_criteria>
DRM-04 satisfied: /silo CLI command persists explicit silo tagging for the active session. /silo none clears it. /silo (no arg) reports status. **Hook block format empirically verified.**
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-silo-foundation/48.1-03-SUMMARY.md` recording the new endpoint path, the hook modification summary, auth posture finding, block-format guard result, and the full smoke script output showing all 6 SCs green.
</output>
</output>
