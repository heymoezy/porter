---
phase: 02-memory-v2
plan: 01
type: execute
wave: 1
depends_on:
  - 02-00
files_modified:
  - porter.py
autonomous: true
requirements:
  - MEM-01
must_haves:
  truths:
    - "Cortex is fully removed from porter.py — zero cortex function definitions remain"
    - "Porter starts and serves requests without errors after cortex deletion"
    - "Memory V2 functions (_mem_insert, _mem_search, _mem_promote, _mem_dismiss, _mem_inject_for_dispatch, _mem_extract_signals) are present and intact"
    - "No functional cortex references remain (only changelog/release note text)"
  artifacts:
    - path: "porter.py"
      provides: "Cortex-free codebase with Memory V2 as sole memory system"
      contains: "def _mem_insert"
  key_links:
    - from: "porter.py (_mem_extract_signals)"
      to: "memories table"
      via: "inline extraction (no cortex intermediary)"
      pattern: "_mem_insert\\("
---

<objective>
Delete all Cortex code from porter.py — functions, table, preferences, workflow entries, and call sites — leaving Memory V2 as the sole memory system.

Purpose: Cortex is dead code disabled in Phase 1 with early-return guards. This plan removes it entirely so no NameError crashes occur when subsequent plans modify memory paths. Clean slate per locked decision — no data migration from cortex_memories.

Output: porter.py with zero cortex references (except release notes/changelog text), FTS5 index rebuilt after any stale rows cleaned.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-v2/02-CONTEXT.md
@.planning/phases/02-memory-v2/02-RESEARCH.md

<interfaces>
<!-- Key functions and patterns the executor needs -->

From porter.py — cortex functions to DELETE (lines 1822-2468 area):
- def _cortex_stem(word):
- def _cortex_tokenize(text):
- def _cortex_extract_facts(text, min_len=...):
- def _cortex_store_fact(...):
- def _cortex_consolidate_facts(...):
- def _cortex_inject_context(...):
- def _cortex_extract_and_route(prompt, response, backend=...):
- def _cortex_extract_and_route_inner(prompt, response, backend=...):
- def _cortex_consolidate_once():
- (approximately 30 functions total with _cortex_ prefix)

From porter.py — cortex_memories table CREATE (lines 10394-10413 area):
- CREATE TABLE IF NOT EXISTS cortex_memories (...)

From porter.py — cortex preferences in DEFAULT_PREFERENCES (line ~100):
- "cortex_enabled": False
- "cortex_min_response_len": ...
- "cortex_max_facts": ...
- "cortex_inject_limit": ...
- "cortex_consolidate_hours": ...

From porter.py — cortex workflow dispatch (line ~57600):
- "cortex_consolidation": lambda: _cortex_consolidate_once()
- Adjacent config handling at lines 57657-57659

From porter.py — cortex call sites:
- Line ~50985: _cortex_extract_and_route(_cx_prompt, _cx_resp, backend=_cx_be)
- Line ~44611: background thread call to _cortex_extract_and_route

From porter.py — Memory V2 functions to KEEP:
- def _mem_insert(...) (line 3805)
- def _mem_search(...) (line 3847)
- def _mem_promote(...)
- def _mem_dismiss(...)
- def _mem_inject_for_dispatch(...) (line 2961)
- def _mem_extract_signals(...) (line 3046) — currently disabled with return 0
- def _mem_consolidation_loop() (line 2490) — V2, keep
- def _mem_consolidate_once() (line 2545) — V2, keep
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all Cortex code via patch script</name>
  <files>porter.py</files>
  <read_first>
    - /home/lobster/documents/porter/porter.py (grep for exact markers: "def _cortex_stem", "cortex_memories", "cortex_consolidation", "cortex_enabled", "_cortex_extract_and_route")
    - /home/lobster/documents/porter/.planning/phases/02-memory-v2/02-RESEARCH.md (pitfalls 1-4 detail exact locations)
  </read_first>
  <action>
Create a Python patch script at /tmp/patch_02_01_cortex_delete.py that reads porter.py and performs these deletions using STRING MARKERS (never line numbers — they shift):

1. **Delete all _cortex_* function definitions** — find the block starting at `def _cortex_stem(word):` and ending before `def _mem_consolidation_loop():` (or whatever non-cortex function follows the last cortex function). Remove the entire block. Verify by grepping the result for `def _cortex_` — count must be 0.

2. **Delete cortex_memories table CREATE** — find the block containing `CREATE TABLE IF NOT EXISTS cortex_memories` in _db_init(). The block ends at the next CREATE TABLE or closing parenthesis+semicolon. Remove it.

3. **Remove cortex preferences from DEFAULT_PREFERENCES** — find and remove these keys from the DEFAULT_PREFERENCES dict: `"cortex_enabled"`, `"cortex_min_response_len"`, `"cortex_max_facts"`, `"cortex_inject_limit"`, `"cortex_consolidate_hours"`. Remove each entire line (key: value with trailing comma).

4. **Remove cortex_consolidation workflow entry** — find `"cortex_consolidation"` in the workflow dispatch dict (near line 57600). Remove the entire entry. Also remove any adjacent config handling for cortex_consolidation at lines 57657-57659 area (find by string marker).

5. **Replace _cortex_extract_and_route call sites** — grep for ALL occurrences of `_cortex_extract_and_route(` in the result. There are at least 2 call sites (near line 50985 and line 44611). Replace each call with a comment: `# Cortex removed — extraction handled by _mem_extract_signals() in Phase 2 plan 02-02`. Do NOT wire _mem_extract_signals yet (it's still disabled).

6. **Remove commented-out workflow registrations** — find and delete lines containing `_wf_register("cortex_consolidation")` and `_wf_register("memory_extraction")` (they are commented out from Phase 1).

7. **Clean up any remaining cortex references** — grep the result for `cortex` (case-insensitive). Remaining references should ONLY be in:
   - Release notes / changelog text strings (acceptable)
   - Comments referencing the deletion (acceptable)
   - Variable names that happen to contain "cortex" in non-functional contexts (acceptable)
   Remove any functional references (imports, function calls, table queries, config keys).

8. **Rebuild FTS5 index** — after deletions, add a startup safety check: if `memories_fts` exists, run `INSERT INTO memories_fts(memories_fts) VALUES('rebuild')` to ensure index consistency after any stale cortex-migrated rows are gone.

Run the patch: `python3 /tmp/patch_02_01_cortex_delete.py`
Verify syntax: `python3 -c "compile(open('/home/lobster/documents/porter/porter.py').read(), 'porter.py', 'exec')"`
  </action>
  <verify>
    <automated>python3 /tmp/test_grep_zero.py</automated>
  </verify>
  <done>All ~30 cortex functions deleted, cortex_memories table CREATE removed, cortex preferences removed from DEFAULT_PREFERENCES, workflow dispatch entry removed, all call sites replaced with comments, FTS5 rebuild safety added. porter.py compiles without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Porter starts and Memory tab loads</name>
  <files>porter.py</files>
  <read_first>
    - /home/lobster/documents/porter/porter.py (verify _mem_insert, _mem_search still present)
  </read_first>
  <action>
1. Restart Porter: `systemctl --user restart porter`
2. Wait 3 seconds for startup.
3. Check health: `curl -s http://127.0.0.1:8877/api/admin/health` — must return 200 with JSON containing `"status": "ok"`.
4. Check Memory V2 functions still work: grep porter.py for `def _mem_insert`, `def _mem_search`, `def _mem_promote`, `def _mem_dismiss`, `def _mem_inject_for_dispatch`, `def _mem_extract_signals` — all 6 must be present.
5. Run existing Playwright Memory tab test if available: `cd /home/lobster/documents/porter/tests && npx playwright test --grep "Memory" 2>/dev/null` (may fail due to missing browser binary — that's OK, it's a pre-existing infra issue).
6. Final comprehensive cortex grep: `grep -n 'cortex' porter.py | grep -v '#' | grep -v 'CHANGELOG\|changelog\|release_notes\|RELEASE' | grep -v '"""' | grep -v "'''"` — should return 0 functional lines.
  </action>
  <verify>
    <automated>curl -sf http://127.0.0.1:8877/api/admin/health | python3 -c "import sys,json; d=json.load(sys.stdin); assert d.get('status')=='ok', f'Health check failed: {d}'; print('Porter healthy')" && python3 /tmp/test_grep_zero.py</automated>
  </verify>
  <done>Porter starts cleanly after cortex deletion. Health endpoint returns OK. All 6 Memory V2 core functions are present and intact. No functional cortex references remain.</done>
</task>

</tasks>

<verification>
- `python3 /tmp/test_grep_zero.py` passes all tests (zero cortex, V2 functions present, syntax valid)
- Porter health check returns 200 OK
- python3 syntax check on porter.py passes
</verification>

<success_criteria>
All Cortex code is deleted from porter.py. Porter starts and serves requests. Memory V2 is the sole memory system. Zero functional cortex references remain.
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-v2/02-01-SUMMARY.md`
</output>
