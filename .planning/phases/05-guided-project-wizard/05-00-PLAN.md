---
phase: 05-guided-project-wizard
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - /tmp/test_proj01_wizard_api.py
  - /tmp/test_proj01_detect.py
  - /tmp/test_proj01_approve.py
  - /tmp/test_proj02_agent_match.py
  - /tmp/test_proj03_activity.py
  - /tmp/test_proj04_gsd_mode.py
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04]

must_haves:
  truths:
    - "Each requirement has at least one behavioral test script"
    - "Tests detect pre-implementation state via SKIP, not false PASS"
    - "Tests run under 10 seconds each via python3"
  artifacts:
    - path: "/tmp/test_proj01_wizard_api.py"
      provides: "Wizard endpoint existence + validation test"
    - path: "/tmp/test_proj01_detect.py"
      provides: "Intent detection action test"
    - path: "/tmp/test_proj01_approve.py"
      provides: "Atomic approval transaction test"
    - path: "/tmp/test_proj02_agent_match.py"
      provides: "Agent-to-project-type matching test"
    - path: "/tmp/test_proj03_activity.py"
      provides: "Activity feed endpoint test"
    - path: "/tmp/test_proj04_gsd_mode.py"
      provides: "GSD mode persistence and routing test"
  key_links: []
---

<objective>
Create Wave 0 behavioral test scripts for Phase 5 requirements.

Purpose: Establish automated verification before implementation begins. Each test detects pre-implementation state with SKIP (feature not yet built), transitions to FAIL when partially built, and PASS when complete.
Output: 6 Python3 test scripts at /tmp/ following Phase 2/4 conventions.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guided-project-wizard/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PROJ-01 and PROJ-02 test scripts</name>
  <files>/tmp/test_proj01_wizard_api.py, /tmp/test_proj01_detect.py, /tmp/test_proj01_approve.py, /tmp/test_proj02_agent_match.py</files>
  <read_first>
    - /home/lobster/documents/porter/.planning/phases/05-guided-project-wizard/05-VALIDATION.md
    - /home/lobster/documents/porter/backend/src/config.ts
  </read_first>
  <action>
Create 4 Python3 stdlib test scripts using the Phase 2/4 convention (urllib.request, json, sys.exit with SKIP/PASS/FAIL).

Base URL: `http://127.0.0.1:3001` (Fastify backend port from config.ts).

**test_proj01_wizard_api.py:**
- POST `/api/v1/projects/wizard` with empty body -> expect 400 or 422 (validation error)
- POST `/api/v1/projects/wizard` with `{"action":"detect","message":"test"}` -> expect 200 with JSON containing `isProject` field
- If endpoint returns 404 -> SKIP "Wizard endpoint not yet implemented"
- If endpoint returns 503 -> SKIP "Wizard feature disabled"

**test_proj01_detect.py:**
- POST `/api/v1/projects/wizard` with `{"action":"detect","message":"I need a website for my bakery"}`
- Expect response JSON to contain `isProject: true` and `clarity` field
- POST with `{"action":"detect","message":"hello"}` -> expect `isProject: false`
- If 404 -> SKIP

**test_proj01_approve.py:**
- POST `/api/v1/projects/wizard` with `{"action":"approve","proposal":{...}}` using a minimal valid proposal object:
  ```json
  {
    "projectName": "Test Project",
    "projectType": "website",
    "agents": [{"templateId":"test","name":"Writer","role":"content_writer","whyChosen":"test"}],
    "milestones": ["Setup","Content","Launch"],
    "scopeLabel": "Small"
  }
  ```
- Expect 200 with `projectId` in response
- Verify project exists: GET `/api/v1/projects/{projectId}` returns 200
- If wizard 404 -> SKIP

**test_proj02_agent_match.py:**
- POST `/api/v1/projects/wizard` with `{"action":"propose","goal":"I need a website for my bakery","answers":[]}`
- Expect response JSON to contain `agents` array with at least 1 agent
- Each agent must have `name`, `role`, `whyChosen` fields
- Expect at least one agent with role containing "design" or "content" or "writer" (appropriate for website)
- If 404 -> SKIP

All scripts must:
- Use `http.client.HTTPConnection` (not urllib) to avoid timeout issues
- Print `[SKIP]`, `[PASS]`, or `[FAIL]` as first word of output
- Exit 0 on SKIP/PASS, exit 1 on FAIL
- Include auth cookie handling (GET `/api/v1/auth/me` first, if 401 -> SKIP "Not authenticated")
  </action>
  <verify>
    <automated>python3 /tmp/test_proj01_wizard_api.py && python3 /tmp/test_proj01_detect.py && python3 /tmp/test_proj01_approve.py && python3 /tmp/test_proj02_agent_match.py</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/test_proj01_wizard_api.py exists and contains `def ` and `[SKIP]` and `api/v1/projects/wizard`
    - /tmp/test_proj01_detect.py exists and contains `isProject` and `bakery`
    - /tmp/test_proj01_approve.py exists and contains `approve` and `projectId`
    - /tmp/test_proj02_agent_match.py exists and contains `propose` and `agents`
    - All 4 scripts exit 0 (SKIP is expected pre-implementation)
  </acceptance_criteria>
  <done>4 test scripts exist and exit 0 with SKIP status</done>
</task>

<task type="auto">
  <name>Task 2: Create PROJ-03 and PROJ-04 test scripts</name>
  <files>/tmp/test_proj03_activity.py, /tmp/test_proj04_gsd_mode.py</files>
  <read_first>
    - /home/lobster/documents/porter/.planning/phases/05-guided-project-wizard/05-VALIDATION.md
    - /home/lobster/documents/porter/backend/src/routes/v1/projects.ts
  </read_first>
  <action>
Create 2 Python3 stdlib test scripts.

**test_proj03_activity.py:**
- GET `/api/v1/projects/{projectId}/activity` (use a known project ID or create one first via POST)
- Expect 200 with JSON array (can be empty on fresh project)
- Each item in array (if any) must have `event_type`, `summary`, `created_at` fields
- If endpoint returns 404 -> SKIP "Activity endpoint not yet implemented"

**test_proj04_gsd_mode.py:**
- POST `/api/v1/projects/wizard` with `{"action":"detect","message":"test","gsdMode":true}`
- Check that response acknowledges gsdMode field (or SKIP if not implemented)
- This test validates the API-level GSD mode flag handling
- If wizard 404 -> SKIP

Both scripts follow same conventions:
- `http.client.HTTPConnection` to `127.0.0.1:3001`
- Print `[SKIP]`, `[PASS]`, or `[FAIL]`
- Exit 0 on SKIP/PASS, exit 1 on FAIL
- Auth cookie handling via `/api/v1/auth/me`
  </action>
  <verify>
    <automated>python3 /tmp/test_proj03_activity.py && python3 /tmp/test_proj04_gsd_mode.py</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/test_proj03_activity.py exists and contains `activity` and `event_type`
    - /tmp/test_proj04_gsd_mode.py exists and contains `gsdMode`
    - Both scripts exit 0 (SKIP is expected pre-implementation)
  </acceptance_criteria>
  <done>6 total test scripts exist and all exit 0 with SKIP status</done>
</task>

</tasks>

<verification>
All 6 test scripts exist at /tmp/ and exit with code 0 (SKIP expected before implementation).
</verification>

<success_criteria>
- 6 Python3 test scripts created at /tmp/
- Each covers a distinct requirement (PROJ-01 through PROJ-04)
- All exit 0 with [SKIP] output (pre-implementation state)
- Scripts follow Phase 2/4 convention (http.client, SKIP/PASS/FAIL)
</success_criteria>

<output>
After completion, create `.planning/phases/05-guided-project-wizard/05-00-SUMMARY.md`
</output>
