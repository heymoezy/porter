---
phase: 03-route-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - porter.py
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Every agent's system prompt is under 300 tokens (identity + guardrails only)"
    - "_build_context_suffix() no longer reads SOUL.md or RULES.md from filesystem"
    - "Memory injection happens on-demand at dispatch time, not front-loaded into system prompt"
    - "If a prompt somehow exceeds 2K tokens, the circuit breaker fires and logs a warning"
    - "Awareness toggle (aware vs sandboxed) changes the system prompt content per agent"
  artifacts:
    - path: "porter.py"
      provides: "_build_lean_identity() function replacing _build_context_suffix()"
      contains: "def _build_lean_identity"
  key_links:
    - from: "_build_lean_identity()"
      to: "personas table"
      via: "DB query for name, role, config"
      pattern: "SELECT.*FROM personas.*WHERE"
    - from: "_build_lean_identity()"
      to: "dispatch call sites"
      via: "replaces _build_context_suffix in all callers"
      pattern: "_build_lean_identity"
---

<objective>
Radically slim system prompts from ~2,500 tokens to ~200-300 tokens per agent. Replace the file-reading _build_context_suffix() with a DB-only _build_lean_identity() that contains just agent identity + how to use Porter + 2-3 anti-hallucination guardrails.

Purpose: PERF-01 requires system prompts capped at 2K tokens. The current approach front-loads memory, squad rosters, soul files, and rules files into every prompt. Porter's competitive advantage is on-demand context fetching — the prompt should plant seeds, not carry baggage.

Output: _build_lean_identity() function in porter.py, all dispatch call sites updated, baseline token measurements captured.
</objective>

<execution_context>
@/home/lobster/.claude/get-shit-done/workflows/execute-plan.md
@/home/lobster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-route-migration/03-CONTEXT.md
@.planning/phases/03-route-migration/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current prompts + create _build_lean_identity()</name>
  <files>porter.py</files>
  <read_first>
    porter.py (grep for _build_context_suffix to find the function definition around line 2561, and all call sites)
    porter.py (grep for _mem_inject_for_dispatch around line 2446 to understand current memory injection)
    porter.py (grep for "def _estimate_tokens" to find the existing token estimation function)
    porter.py (grep for "personas" table schema in _db_init to understand the config JSON column structure)
  </read_first>
  <action>
    Step 1: Create a measurement script at /tmp/measure_prompts.py that:
    - Imports sqlite3, connects to /home/lobster/documents/porter/porter.db
    - Queries all persona IDs from the personas table
    - For each persona, reads the config JSON blob and extracts: name, role, description, awareness_mode
    - Estimates what _build_context_suffix would produce (count characters / 4 for token estimate)
    - Prints a table: persona_id | name | estimated_current_tokens
    - Run the script and capture the baseline

    Step 2: Create _build_lean_identity(persona_id) function in porter.py via /tmp/patch script. The function:
    - Opens a DB connection via _db_conn()
    - Queries: SELECT name, role, config FROM personas WHERE id=?
    - Parses config JSON to extract: description (or role fallback), awareness_mode (default "aware")
    - Builds identity block:
      - "You are {name}, {role_or_description}."
      - If awareness_mode == "aware": "You work within Porter, an AI orchestration platform. Use Porter's resources (memory, project context, tools) for tasks outside your direct scope. Other agents exist and Porter coordinates delegation."
      - If awareness_mode == "sandboxed": "You are a focused assistant. Handle tasks within your assigned domain."
      - GUARDRAILS (always present): "Rules: (1) Never fabricate data or sources. (2) Never claim a task is done without verification. (3) Say when you don't know."
    - Token circuit breaker: if _estimate_tokens(prompt) > 2000, log via mlog.emit("warn", "system", "prompt.circuit_breaker", ...) and return minimal fallback: "You are {name}. Never fabricate data. Never claim tasks done without verification. Say when you don't know."
    - Close DB connection in finally block
    - Return the prompt string

    Step 3: Find all call sites of _build_context_suffix() and replace with _build_lean_identity():
    - The primary call site is in the chat dispatch / SSE streaming path
    - Replace the call, preserving the persona_id argument
    - Do NOT delete _build_context_suffix() yet — comment it out with "# DEPRECATED Phase 3: replaced by _build_lean_identity()"
    - Do NOT delete _mem_inject_for_dispatch() — it still runs for on-demand memory at dispatch time via the existing memory system, but it is no longer stuffed into the system prompt. Instead, memory is injected as a separate user-context message or tool response.

    IMPORTANT: The memory injection (_mem_inject_for_dispatch) must still function — it just no longer goes into the system prompt. If it's currently appended to the system prompt string, change it so it's appended as a separate message in the messages array (role: "system" or "user") AFTER the lean identity prompt. This preserves memory while keeping the system prompt thin.

    Step 4: Add awareness_mode column handling — the personas table config JSON blob may or may not have an "awareness_mode" key. Default to "aware" if missing. No schema migration needed (it's in the JSON config blob, not a column).

    Use /tmp/patch_*.py script approach since porter.py is ~900KB and Edit tool fails on it.
  </action>
  <verify>
    <automated>python3 -c "
import sqlite3, json
conn = sqlite3.connect('/home/lobster/documents/porter/porter.db')
conn.row_factory = sqlite3.Row
rows = conn.execute('SELECT id, name, config FROM personas').fetchall()
# Verify the function exists in source
with open('/home/lobster/documents/porter/porter.py') as f:
    src = f.read()
assert 'def _build_lean_identity' in src, 'Function not found'
assert '# DEPRECATED Phase 3' in src or '_build_context_suffix' in src, 'Old function handling unclear'
# Check guardrails text exists
assert 'Never fabricate data' in src, 'Guardrails missing'
assert 'prompt.circuit_breaker' in src, 'Circuit breaker missing'
print('PASS: _build_lean_identity exists with guardrails and circuit breaker')
conn.close()
"
    </automated>
  </verify>
  <acceptance_criteria>
    - porter.py contains `def _build_lean_identity(persona_id` (exact function signature)
    - porter.py contains `prompt.circuit_breaker` in an mlog.emit call
    - porter.py contains `Never fabricate data` (guardrail text)
    - porter.py contains `awareness_mode` (toggle handling)
    - porter.py contains `# DEPRECATED Phase 3` comment on or near _build_context_suffix
    - All call sites of _build_context_suffix replaced (grep returns 0 uncommented calls)
    - /tmp/measure_prompts.py runs without error and shows token counts
  </acceptance_criteria>
  <done>_build_lean_identity() produces 200-300 token identity blocks for all agents. _build_context_suffix() is deprecated. Memory injection still functions but is not part of the system prompt. Circuit breaker logs if prompt exceeds 2K tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Verify token counts + Playwright regression</name>
  <files>porter.py</files>
  <read_first>
    porter.py (grep for _build_lean_identity to verify the function from Task 1)
    porter.py (grep for _estimate_tokens to understand the token estimation method)
  </read_first>
  <action>
    Step 1: Create /tmp/test_lean_identity.py that:
    - Reads porter.py source code
    - Extracts the _build_lean_identity function logic (or calls it via subprocess if possible)
    - For each persona in porter.db, simulates the identity construction:
      a. Query persona name, role, config from DB
      b. Build the prompt string using the same logic
      c. Estimate tokens (len(prompt) // 4)
      d. Assert tokens < 300 for standard agents
      e. Assert tokens < 2000 for ALL agents (circuit breaker threshold)
    - Print results table: persona_id | name | tokens | PASS/FAIL
    - Exit 0 if all pass, exit 1 if any fail

    Step 2: Create /tmp/test_prompt_audit.py that:
    - Verifies _build_context_suffix is no longer called in active code paths (only exists as deprecated)
    - Grep porter.py for uncommented calls to _build_context_suffix — should be 0
    - Grep porter.py for _build_lean_identity calls — should be >= 1
    - Verify SOUL.md and RULES.md are no longer read in the prompt construction path (grep for open.*SOUL.md or open.*RULES.md in active code)
    - Exit 0 if all checks pass

    Step 3: Run the full Playwright test suite to confirm no regressions:
    cd /home/lobster/documents/porter/tests && npx playwright test

    The system prompt change should not affect UI tests since they test frontend behavior, not backend prompt assembly.
  </action>
  <verify>
    <automated>python3 /tmp/test_lean_identity.py && python3 /tmp/test_prompt_audit.py && cd /home/lobster/documents/porter/tests && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/test_lean_identity.py exits 0 (all agents under 300 tokens)
    - /tmp/test_prompt_audit.py exits 0 (no active _build_context_suffix calls, no SOUL.md/RULES.md reads)
    - All 35 Playwright tests pass (exit code 0, "35 passed" in output)
  </acceptance_criteria>
  <done>Every agent produces a system prompt under 300 tokens. No file I/O in the prompt construction path. All 35 Playwright tests pass. PERF-01 is complete.</done>
</task>

</tasks>

<verification>
- python3 /tmp/test_lean_identity.py — all agents under 300 tokens
- python3 /tmp/test_prompt_audit.py — no legacy prompt calls active
- cd /home/lobster/documents/porter/tests && npx playwright test — 35 tests green
</verification>

<success_criteria>
1. _build_lean_identity() exists and produces 200-300 token prompts for all agents
2. _build_context_suffix() is deprecated and not called in active code paths
3. Memory injection still functions (on-demand, not in system prompt)
4. Circuit breaker fires and logs for prompts exceeding 2K tokens
5. All 35 Playwright tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-route-migration/03-01-SUMMARY.md`
</output>
