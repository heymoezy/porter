---
phase: 48.3-software-dream-worker
plan: 01
subsystem: testing
tags: [bash, psql, jq, smoke-harness, fixtures, dream-worker, mock-injection]

# Dependency graph
requires:
  - phase: 48.2-transcript-capture
    provides: session_transcript_turns corpus + silo-tagged turns the Dream Worker will read
  - phase: 48.1-silo-foundation
    provides: silos.software row + prompt_path pointer + directive_immutable_moe_direct trigger
provides:
  - tests/smoke-48.3.sh — Wave 0 smoke harness covering DRW-01..DRW-13 with graceful-skip + poll-not-sleep
  - tests/fixtures/dream-response-software.json — doctrine-compliant 3-proposal fixture (delete + supersede + new_directive)
  - tests/fixtures/dream-response-malformed.json — intentionally invalid JSON for DRW-10 parse-failure path
  - tests/fixtures/dream-response-doctrine-violation.json — only-new_directive fixture for DRW-06 reject path
  - DREAM_WORKER_MOCK_RESPONSE_PATH env-var mock-injection contract that Plan 48.3-04 must honor
affects: [48.3-02 schema, 48.3-03 prompt+sampler+parser, 48.3-04 worker entry+dispatchDream, 48.3-05 admin endpoints+ship]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave 0 smoke harness shipped BEFORE implementation plans — every downstream task gets a single-shot `bash tests/smoke-48.3.sh` <verify> command"
    - "Graceful-skip via SKIP_WORKER env detection (file-existence check on backend/src/services/intellect/dream-worker.ts) — Wave 1 runs cleanly before Plans 02-05 ship"
    - "Mock-injection contract via env var (DREAM_WORKER_MOCK_RESPONSE_PATH) — production code paths NEVER read it, smoke is the only consumer"
    - "Throwaway silo_id='software-smoke-48.3' for total isolation from real 'software' silo data"
    - "Poll-with-timeout loop (20 × 0.5s = 10s ceiling) for async worker → DB INSERT round-trip — no fixed sleeps"

key-files:
  created:
    - tests/smoke-48.3.sh
    - tests/fixtures/dream-response-software.json
    - tests/fixtures/dream-response-malformed.json
    - tests/fixtures/dream-response-doctrine-violation.json
    - .planning/phases/48.3-software-dream-worker/48.3-01-SUMMARY.md
  modified:
    - .coordination/SESSIONS.md (ledger entry for this session)

key-decisions:
  - "Mock-injection contract DEFINED here for Plan 04 to honor: env var DREAM_WORKER_MOCK_RESPONSE_PATH causes dispatchDream() to read the file and return its contents instead of calling routingEngine — smoke-test-only path, production code paths must never set or read this variable"
  - "Plan PLAN.md frontmatter listed fixture as 'dream-response-software.json' (consistent with the Step-1 action block); the orchestrator's prompt mentioned 'dream-response-compliant.json' — used the PLAN.md authoritative name throughout (fixture + smoke + summary)"
  - "Three fixtures correspond to three distinct DRW assertion paths: software=happy-path (DRW-04/07/11/12), doctrine-violation=DRW-06 reject, malformed=DRW-10 parse failure. Each is single-purpose so smoke failures point to the exact path that broke"
  - "Throwaway smoke silo (silo_id='software-smoke-48.3') is INSERTed and DELETEd by the harness; uses real software.md prompt_path so DRW-03 prompt-file checks still validate the production path"
  - "Pre-seed 6 throwaway directives (4 mp-smoke-seed-* moe-direct + 2 mp-smoke-target-* dream_worker) inside the smoke silo so active_count > 4 boundary fires for doctrine assertions, and target_directive_ids in dream-response-software.json resolve against live rows (mp-smoke-target-stale + mp-smoke-target-supersede)"
  - "Wave-0 baseline behavior is non-zero exit at DRW-01 ('memory_proposals table missing') — this is correct and predicted by the plan's <acceptance_criteria>; Plan 02 will flip DRW-01/02 green; Plan 03 flips DRW-03 green; Plans 04+05 flip DRW-04..DRW-12 green"

patterns-established:
  - "Plan-checker harness convention: schema checks (DRW-01/02/03/08) run on bare DB via psql — no backend or worker dependency; backend reachability is a soft gate (curl /health) that gracefully exits 0 after schema checks if Fastify offline"
  - "Mock-injection-by-env-var: smoke harness is the only consumer; production code paths must NEVER set or read DREAM_WORKER_MOCK_RESPONSE_PATH. Plan 04's dispatchDream() honors it with a one-line `if (process.env.DREAM_WORKER_MOCK_RESPONSE_PATH) return { response: fs.readFileSync(...), latencyMs: 0, modelUsed: 'mock' }`"
  - "trap cleanup EXIT + top-of-file cleanup() sweep — every smoke row prefixed by 'smoke-48.3-' / silo_id='software-smoke-48.3' is deleted on any exit path (success, failure, or interrupt)"

requirements-completed: [DRW-13]

# Metrics
duration: 18min
completed: 2026-05-13
---

# Phase 48.3 Plan 01: Wave 0 Smoke Harness Summary

**Wave 0 smoke harness covering DRW-01..DRW-13 with mock-injection contract (`DREAM_WORKER_MOCK_RESPONSE_PATH`) that Plan 04 must honor, plus 3 hand-crafted response fixtures exercising happy-path / doctrine-violation / parse-failure assertion paths**

## Performance

- **Duration:** 18 min
- **Started:** 2026-05-13T05:44:47Z
- **Completed:** 2026-05-13T06:02:47Z
- **Tasks:** 2
- **Files modified:** 4 (3 fixtures + 1 smoke script; coordination ledger updated)

## Accomplishments

- Shipped `tests/smoke-48.3.sh` (248 lines, executable, syntax-clean) covering all 13 DRW requirements (DRW-01..DRW-13). Schema checks (DRW-01/02/03/08) run on bare DB via psql, backend-dependent checks (DRW-04..DRW-12) gated behind reachability + worker-file existence gates.
- Shipped 3 response fixtures in `tests/fixtures/`:
  - `dream-response-software.json` — doctrine-compliant happy path (1 delete + 1 supersede + 1 new_directive, exercises refinement-before-append)
  - `dream-response-malformed.json` — invalid JSON (trailing commas) for DRW-10 parse-failure rejection
  - `dream-response-doctrine-violation.json` — all-new_directive proposals when active>4 for DRW-06 validateRefinementDoctrine reject
- Defined the `DREAM_WORKER_MOCK_RESPONSE_PATH` env-var contract that Plan 48.3-04's `dispatchDream()` must honor. Production code paths must never set or read this variable — smoke harness is the only consumer.
- Verified Wave-0 baseline: `bash tests/smoke-48.3.sh` exits 1 on `[FAIL] DRW-01: memory_proposals table missing` — exactly the predicted failing-baseline. Plans 02-05 will flip each DRW green incrementally.

## Task Commits

Each task was committed atomically:

1. **Task 1: 3 dream-response fixtures** — `2cf0a61` (test)
2. **Task 2: smoke-48.3.sh harness** — `2f350d3` (test)

**Plan metadata:** _next commit_ (docs: complete 48.3-01 plan)

## Files Created/Modified

- `tests/smoke-48.3.sh` — Wave 0 smoke harness, 248 LOC, executable, covers DRW-01..DRW-13 with graceful-skip + poll-with-timeout + idempotent cleanup
- `tests/fixtures/dream-response-software.json` — doctrine-compliant Sonnet response (3 proposals: delete + supersede + new_directive) using `mp-smoke-target-stale` and `mp-smoke-target-supersede` directive IDs that the harness seeds into the throwaway smoke silo
- `tests/fixtures/dream-response-malformed.json` — invalid JSON with trailing commas before `]` and `}` — `jq -e .` exits non-zero, no fence wrapper for fallback recovery
- `tests/fixtures/dream-response-doctrine-violation.json` — all-new_directive proposals with `active_directive_count_before: 6`, well-formed JSON + valid Zod, doctrine violation fires inside `validateRefinementDoctrine()` only
- `.coordination/SESSIONS.md` — added GSD Phase 48.3 Executor Plan-01 entry (claim scope: tests/ + fixtures + .planning/ only)

## Decisions Made

- Used the PLAN.md authoritative fixture name `dream-response-software.json` throughout (rather than the orchestrator's `dream-response-compliant.json` reference); the plan body, frontmatter, and verify command all use the `software` suffix consistently. No deviation from the executed plan.
- Mock-injection contract is a pure smoke-test convenience: env var name + behavior locked here, Plan 04 must honor it with a single-line check at the top of `dispatchDream()`. Keeping the contract in env-var form (rather than a code flag) means production CI never accidentally triggers it.
- Pre-seeded smoke silo directives use ID prefix `mp-smoke-` (not `smoke-48.3-`) so cleanup glob in PLAN 02+'s migrations cannot accidentally collide if a migration ever wants to insert a real `smoke-48.3-*` row.
- Did NOT touch ROADMAP.md plan-progress in this commit (no SUMMARY-counted plan completed for the phase yet; gsd-tools roadmap update is for orchestrator).

## Deviations from Plan

None — plan executed exactly as written. The fixture-name discrepancy between the orchestrator's prompt summary and the PLAN.md body is a labeling difference, not a deviation: the PLAN.md is authoritative and lists `dream-response-software.json` in both its `files_modified` frontmatter and its Task 1 Step-1 action block. Followed PLAN.md verbatim.

## Issues Encountered

None during execution. One context-loading observation:
- `48.3-RESEARCH.md` exceeded the Read tool's 25k token limit on full-file load. Read only the head (50 lines) for context on locked decisions; PLAN.md already inlined the integration-point details needed for fixtures + harness construction.

## User Setup Required

None — pure tests/ + fixtures work, no environment variables, no external service configuration, no DB migrations, no backend code.

## Next Phase Readiness

- **Plan 02 (schema):** Ready to run. Will flip DRW-01 + DRW-02 + DRW-08 green by shipping `memory_proposals` + `dream_runs` tables + indexes + CHECK constraints + 2 workflow rows + scheduler `every_week` bucket.
- **Plan 03 (prompt + sampler + parser):** Ready to run after Plan 02. Will flip DRW-03 green by writing the prompt file at `silos.prompt_path` with 5 substitution markers + Refinement Doctrine references.
- **Plan 04 (worker entry):** Ready to run after Plan 03. MUST honor the `DREAM_WORKER_MOCK_RESPONSE_PATH` env-var contract defined in this plan — one-line check at top of `dispatchDream()` that reads the fixture and bypasses `routingEngine` entirely.
- **Plan 05 (admin endpoints + ship):** Ready to run after Plan 04. Plan 05's live unmocked checkpoint will validate the full DRW-05 raw-passthrough contract (claude_cli gateway + no `Workspace directives:` injection marker in system_prompt) — smoke covers DRW-05 only by mocked-path shape consistency.

## Self-Check

Verifying claims before declaring done.

**Files exist:**
- FOUND: tests/smoke-48.3.sh
- FOUND: tests/fixtures/dream-response-software.json
- FOUND: tests/fixtures/dream-response-malformed.json
- FOUND: tests/fixtures/dream-response-doctrine-violation.json

**Commits exist:**
- FOUND: 2cf0a61 (Task 1 — fixtures)
- FOUND: 2f350d3 (Task 2 — smoke harness)

**Verify commands:**
- `bash -n tests/smoke-48.3.sh` → exits 0 (syntax clean)
- `test -x tests/smoke-48.3.sh` → exits 0 (executable)
- `jq -e . tests/fixtures/dream-response-software.json` → exits 0 (valid JSON)
- `jq -e . tests/fixtures/dream-response-malformed.json` → exits non-zero (intentionally invalid)
- `jq -e . tests/fixtures/dream-response-doctrine-violation.json` → exits 0 (valid JSON, doctrine-invalid)
- `bash tests/smoke-48.3.sh` → exits 1 on `[FAIL] DRW-01: memory_proposals table missing` (correct Wave-0 baseline; will turn green after Plan 02)

## Self-Check: PASSED

---
*Phase: 48.3-software-dream-worker*
*Completed: 2026-05-13*
