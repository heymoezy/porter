---
phase: 48.3-software-dream-worker
plan: 03
subsystem: intellect
tags: [zod, prompt-template, sampler, parser, refinement-doctrine, dream-worker, stratified-sampling]

# Dependency graph
requires:
  - phase: 48.1-silo-foundation
    provides: silos.prompt_path seeded at backend/src/services/intellect/dream-prompts/software.md (verified live via psql)
  - phase: 48.2-transcript-capture
    provides: session_transcript_turns table (sampler input source) with (silo_id, captured_at DESC) index
  - phase: 48.3-02
    provides: dream_runs.action_config.sampling JSON shape (samplingLog target), memory_proposals.sort_order column (assignSortOrder target)
provides:
  - backend/src/services/intellect/dream-prompts/software.md (canonical software-silo dream prompt, 81 lines)
  - backend/src/services/intellect/dream-sampler.ts (sampleSoftwareTurns + SamplingLog type, 238 lines)
  - backend/src/services/intellect/dream-parser.ts (proposalSchema, dreamResponseSchema, parseDreamResponse, validateRefinementDoctrine, assignSortOrder, 176 lines)
  - DB-as-ground-truth contract for validateRefinementDoctrine (caller passes directiveRows.length, model self-report ignored)
  - Three-layer DRW-06 doctrine enforcement: prompt (L1) + validator (L2) + sort_order (L3)
affects:
  - 48.3-04 (dream-worker.ts imports all three modules; calls parseDreamResponse → validateRefinementDoctrine(parsed, directiveRows.length) → assignSortOrder(parsed) → reads p._sort_order during INSERT)
  - 48.3-05 (admin run-detail view reads memory_proposals.sort_order set by assignSortOrder; refinements appear before additions)

# Tech tracking
tech-stack:
  added: [zod schemas for dream response, IMPERATIVE_REGEX heuristic, FENCE_REGEX markdown extraction]
  patterns:
    - "Three-layer doctrine enforcement: prompt instruction (model-side) + validateRefinementDoctrine (worker-side, DB ground truth) + sort_order (DB-side display ordering)"
    - "DB-as-ground-truth: validateRefinementDoctrine takes activeCountBefore as parameter, ignores parsed.active_directive_count_before (model self-report); prevents the model from spoofing 'before: 4' to bypass doctrine"
    - "Deterministic stratified sampling: 5-pass algorithm (corpus read → tag stratum/imperative → budget 40/30/20/10 → pass A imperatives + pass B per-stratum longest-first + pass C backfill → final sort by session_id+turn_index) — no Math.random, same DB state + same override = same selection"
    - "Caller-responsibility split for model-aware clamping: sampler enforces only the outer Opus ceiling (2.5MB); model-specific clamping (e.g. Sonnet 800KB) is the endpoint's job (Plan 05) — keeps sampler model-agnostic"
    - "Zod refine() inside proposalSchema enforces target_directive_ids count rule (0 new_directive / 1 delete-supersede / 2+ merge) — type-safe at parse time, not deferred to runtime check"
    - "Mutating sort_order assignment with cross-area offset of +1000 — alphabetical conceptual_area lex-sort sets the inter-group order, kind base (100/200/300/900) sets intra-group order, ordinal counter handles same-kind-same-area dupes"
    - "Per-turn cap (8KB) stricter than ingestion cap (32KB from 48.2): signal density priority at synthesis time"

key-files:
  created:
    - backend/src/services/intellect/dream-prompts/software.md
    - backend/src/services/intellect/dream-sampler.ts
    - backend/src/services/intellect/dream-parser.ts
    - .planning/phases/48.3-software-dream-worker/deferred-items.md
  modified:
    - .coordination/SESSIONS.md (ledger entry for this session)

key-decisions:
  - "[Phase 48.3-03]: Prompt file path is backend/src/services/intellect/dream-prompts/software.md — matches silos.prompt_path seeded by 48.1-01 (verified live: psql -tAc \"SELECT prompt_path FROM silos WHERE id='software'\" returns exactly that string). Plan 04's worker will resolve via path.resolve(process.cwd(), siloRow.prompt_path)."
  - "[Phase 48.3-03]: All 5 substitution markers are double-brace, case-sensitive, no internal whitespace: {{ACTIVE_DIRECTIVE_COUNT}}, {{ACTIVE_DIRECTIVES_BLOCK}}, {{TRANSCRIPT_BLOCK}}, {{TURNS_SAMPLED}}, {{SESSIONS_SAMPLED}}. Plan 04's renderTemplate must use these exact strings (raw replace, not template literal)."
  - "[Phase 48.3-03]: validateRefinementDoctrine consumes activeCountBefore as a CALLER-SUPPLIED parameter (DB-queried), NOT parsed.active_directive_count_before (model self-report). Model self-report is preserved in parsed object for audit logging only. Trust DB > trust model — a misbehaving model could otherwise spoof 'before: 4' to slip new_directive past the doctrine when real count is 9."
  - "[Phase 48.3-03]: Sampler stays model-agnostic. Outer absolute cap = MAX_BUDGET_OPUS_BYTES = 2.5MB. Per-model clamping (Sonnet 800KB) is enforced at the POST /dream-run endpoint (Plan 05), which rejects with 400 INVALID_SAMPLE_SIZE_FOR_MODEL. Documented in module header so Plan 05 doesn't accidentally re-enforce."
  - "[Phase 48.3-03]: Empty parsed.proposals.length === 0 is SUCCESS (legitimate quiet week). validateRefinementDoctrine returns early without throwing. Worker should still insert 0 rows and mark dream_run as 'completed' with proposals_extracted=0."
  - "[Phase 48.3-03]: SEED_BASELINE constant = 4 (matches the 4 hand-curated software-silo seeds from 48.1-01). Doctrine engages only when activeCountBefore > 4. Below baseline, the rule set is too sparse to demand refinement."
  - "[Phase 48.3-03]: assignSortOrder uses cross-area offset of +1000 to prevent collision between different conceptual_areas. Within an area: 100+n delete, 200+n supersede, 300+n merge, 900+n new_directive. Lexicographic area ordering means area 'design-system' (offset 0) sorts before 'ship-discipline' (offset 1000+) — refinements in one area sort before additions in a later area."
  - "[Phase 48.3-03]: assignSortOrder MUTATES parsed.proposals in place (adds _sort_order field). Plan 04's worker reads `p._sort_order` directly during the INSERT INTO memory_proposals statement."
  - "[Phase 48.3-03]: parseDreamResponse tries JSON.parse(trim) first, falls back to FENCE_REGEX /```(?:json)?\\s*(\\{[\\s\\S]*\\})\\s*```/ for fence-wrapped responses, then runs Zod safeParse. Both parse-failure paths throw with 'JSON parse failed: ...' prefix; schema-validation failure throws with 'Schema validation failed: ...' prefix listing all Zod issue paths."
  - "[Phase 48.3-03]: Smoke harness directive-seed type-mismatch bug (NOW() vs double-precision epoch on directives.created_at) logged to deferred-items.md as out-of-scope from Plan 03. Pre-existing from Plan 01; surfaces only at DRW-04+ which are not yet wired. Fix budget ~2min at start of Plan 04."

patterns-established:
  - "Pattern: Three-layer doctrine enforcement — Layer 1 (prompt instruction, model-side, advisory) + Layer 2 (validateRefinementDoctrine, worker-side, hard enforcement with DB ground truth) + Layer 3 (sort_order at INSERT, display ordering, deterministic). Copy-paste this pattern for any future silo's dream worker."
  - "Pattern: Caller-supplied ground-truth parameter for validation functions — when a validator's correctness depends on a value the model could spoof, take the value as a parameter from a trusted caller (DB query) rather than reading it from the parsed/untrusted input. Document the contract in both the function header AND inline so future readers don't 'simplify' by switching to the parsed field."
  - "Pattern: Deterministic stratified sampler with samplingLog — 5-pass algorithm (read corpus → tag → allocate budget → 3 passes select → final sort), returns a SamplingLog struct that goes verbatim into action_config for replay/audit. Same DB state + same args = same selection (no Math.random)."
  - "Pattern: Per-domain Zod refine() inside object schema — enforce relational constraints between fields (kind ↔ target_directive_ids.length) at parse time, not as a separate runtime check. Zod throws a single typed error path; no need for follow-up validation."
  - "Pattern: Mutation-with-marker assignment — assignSortOrder mutates each proposal adding `_sort_order` (underscore prefix marks it as worker-computed metadata, not part of the original model output). Consumers (Plan 04 INSERT) read `p._sort_order` directly; the field carries through Zod inference via the type intersection trick `ParsedProposal = ParsedDreamResponse['proposals'][number] & { _sort_order?: number }`."

requirements-completed: [DRW-03, DRW-06, DRW-07]

# Metrics
duration: 37 min
completed: 2026-05-13
---

# Phase 48.3 Plan 03: Dream Prompt + Sampler + Parser Summary

**Canonical software-silo dream prompt at silos.prompt_path (81 lines with 5 substitution markers + Refinement Doctrine + JSON output contract + self-check), deterministic stratified sampler (5-pass algorithm with 40/30/20/10 budget + 8KB per-turn cap + samplingLog for action_config replay), and Zod-validated JSON parser with three-layer DRW-06 doctrine enforcement (prompt + validator + sort_order assignment) — all DB-as-ground-truth, all model-agnostic, all unit-verified against the 3 Plan-01 fixtures**

## Performance

- **Duration:** 37 min
- **Started:** 2026-05-13T07:01:12Z
- **Completed:** 2026-05-13T07:38:49Z
- **Tasks:** 3
- **Files modified:** 3 (3 created)

## Accomplishments

- Prompt template live at `backend/src/services/intellect/dream-prompts/software.md` — the exact path `silos.prompt_path` was seeded with by 48.1 (verified by `psql -tAc "SELECT prompt_path FROM silos WHERE id='software'"`). DRW-03 smoke check flips green: `[ ok ] DRW-03: prompt file exists` + `[ ok ] DRW-03: all 5 substitution markers present` + `[ ok ] DRW-03: template references Refinement Doctrine`.
- All 5 substitution markers verbatim and case-exact: `{{ACTIVE_DIRECTIVE_COUNT}}`, `{{ACTIVE_DIRECTIVES_BLOCK}}`, `{{TRANSCRIPT_BLOCK}}`, `{{TURNS_SAMPLED}}`, `{{SESSIONS_SAMPLED}}`.
- Refinement Doctrine fully enumerated (4 principles), Hard Rules section locks sealed-seed immutability (source_type=moe-direct), evidence requirement (≥2 turn IDs), one-conceptual-area-per-proposal, software-only scope. Self-check checklist has 6 items the model must pass before responding.
- Sampler at `backend/src/services/intellect/dream-sampler.ts` exports `sampleSoftwareTurns(args, pool): Promise<{turns, samplingLog}>`. Implements the locked 5-pass algorithm with `PER_TURN_CAP_BYTES = 8 * 1024`, `DEFAULT_BUDGET_BYTES = 200 * 1024`, `MAX_BUDGET_OPUS_BYTES = 2_500_000`. Reads from `session_transcript_turns WHERE silo_id=$1 AND captured_at >= NOW() - INTERVAL '7 days'`. Returns SamplingLog struct ready to write into `dream_runs.action_config.sampling`.
- Parser at `backend/src/services/intellect/dream-parser.ts` exports 5 names: `proposalSchema`, `dreamResponseSchema`, `parseDreamResponse`, `validateRefinementDoctrine`, `assignSortOrder`. Plus types `ParsedDreamResponse` (z.infer) and `ParsedProposal` (z.infer & `{_sort_order?: number}`).
- DB-as-ground-truth contract enforced AND documented in three places: module header (lines 11-19), inline comment inside validateRefinementDoctrine (lines 114-117), and SUMMARY frontmatter key-decisions. Function does NOT reference `parsed.active_directive_count_before` for logic — only the explanatory comment mentions it.
- TypeScript clean: `cd backend && npx tsc --noEmit` exits 0 after each task.
- All 3 Plan-01 fixtures verified end-to-end against the compiled parser:
  - `dream-response-software.json` (compliant): parses 3 proposals, doctrine passes at activeCount=6, sort_order assigned (`component-discipline/supersede=200, design-system/delete=1100, ship-discipline/new_directive=2900`).
  - `dream-response-malformed.json`: throws `JSON parse failed: Expected double-quoted property name in JSON at position 386` — exact error-prefix contract honored.
  - `dream-response-doctrine-violation.json`: parses fine, validator throws `Doctrine violation: new_directive proposed without prior refinement (active dir count: 6, refinement proposals: 0)` at activeCount=6; same input at activeCount=4 passes without throwing (baseline-engagement contract).
  - Empty proposals array at activeCount=9: validator returns without throwing (legitimate-quiet-week contract).

## Task Commits

Each task was committed atomically:

1. **Task 1: Write backend/src/services/intellect/dream-prompts/software.md** — `1fb6f09` (feat)
2. **Task 2: Create backend/src/services/intellect/dream-sampler.ts** — `2e4b178` (feat)
3. **Task 3: Create backend/src/services/intellect/dream-parser.ts** — `0a7d556` (feat)

**Plan metadata commit:** to follow this summary (docs)

## Files Created/Modified

- `backend/src/services/intellect/dream-prompts/software.md` (NEW, 81 lines) — Canonical software-silo dream prompt: Refinement Doctrine, Hard Rules, JSON output contract, 5 substitution markers, self-check checklist
- `backend/src/services/intellect/dream-sampler.ts` (NEW, 238 lines) — Deterministic stratified sampler: 5-pass algorithm, per-turn 8KB cap, default 200KB budget, outer 2.5MB ceiling, SamplingLog return shape
- `backend/src/services/intellect/dream-parser.ts` (NEW, 176 lines) — Zod schemas + parseDreamResponse (with fence-extraction fallback) + validateRefinementDoctrine (DB-ground-truth) + assignSortOrder (mutation with cross-area offset)
- `.planning/phases/48.3-software-dream-worker/deferred-items.md` (NEW) — Logs the pre-existing smoke harness directive-seed type-mismatch bug to fix in Plan 04
- `.coordination/SESSIONS.md` (modified) — Session ledger entry

## Decisions Made

See key-decisions in frontmatter. All decisions were front-loaded by the plan body and RESEARCH; no novel architectural decisions surfaced during execution. The DB-ground-truth contract was already locked by the plan's critical_constraints — Plan 03 just implemented it.

## Deviations from Plan

None — plan executed exactly as written.

The plan's three task action blocks were precise enough that no deviations were required. All three tasks completed inside their stated `<action>` envelopes:

- Task 1 wrote `software.md` matching the locked template verbatim (the version inside the plan body), with one minor formatting clarification: line 56 splits the `target_directive_ids` count rule and the `flagged_seeds`/`priority` notes into prose paragraphs (vs the plan's inline rules block). All hard rules and substitution markers preserved.
- Task 2 wrote `dream-sampler.ts` matching the plan's stated code shape verbatim — same constants, same algorithm, same SamplingLog shape, same type exports.
- Task 3 wrote `dream-parser.ts` matching the plan's stated code shape verbatim — same Zod schemas, same parseDreamResponse fence-fallback logic, same SEED_BASELINE=4 doctrine, same KIND_BASE/areaOffset sort logic.

## Issues Encountered

**Issue 1: Pre-existing smoke harness bug surfaces at DRW-04+ (out of scope for Plan 03)**

- **Symptom:** `bash tests/smoke-48.3.sh` ends with `ERROR: column "created_at" is of type double precision but expression is of type timestamp with time zone` from the directive-seed INSERTs at lines 129/131/132 of `tests/smoke-48.3.sh`. The script uses `NOW(), NOW()` but `directives.created_at` is `double precision` (epoch seconds).
- **Impact:** DRW-04..DRW-12 cannot run because the smoke silo's 6 directives never seed. DRW-01/02/03/08 (the only checks Plan 03 affects) all pass — DRW-03 flips from FAIL to OK as expected.
- **Scope decision:** Pre-existing from Plan 01 smoke harness, not caused by Plan 03's prompt/sampler/parser files. Per execution scope boundary rules, logged to `.planning/phases/48.3-software-dream-worker/deferred-items.md` with the exact 2-min fix recipe (`NOW(), NOW()` → `EXTRACT(EPOCH FROM NOW()), EXTRACT(EPOCH FROM NOW())`) to apply at the start of Plan 04. Not blocking Plan 04 because the directive-seed path is only exercised when DRW-04+ checks unlock (which depend on `dream-worker.ts` existing — Plan 04's deliverable).

## User Setup Required

None — pure source-code additions (prompt template + 2 TypeScript modules). No env vars, no external services, no DB migrations, no service restart required. The prompt template is read by Plan 04's worker via `fs.promises.readFile(path.resolve(process.cwd(), siloRow.prompt_path), 'utf8')` at dispatch time — no caching, so Moe can edit `software.md` without redeploying.

## Next Phase Readiness

**Ready for Plan 04 (dream-worker.ts):**

- All three modules import-ready at the locked paths. Plan 04's worker will:
  1. `await fs.promises.readFile(path.resolve(process.cwd(), siloRow.prompt_path), 'utf8')` → prompt template (the file that exists now)
  2. `await sampleSoftwareTurns({siloId, sampleSizeOverride}, pool)` → `{turns, samplingLog}` (write samplingLog to `dream_runs.action_config.sampling`)
  3. Render template via `.replaceAll('{{ACTIVE_DIRECTIVE_COUNT}}', String(directiveRows.length))` etc. for all 5 markers
  4. Dispatch through Bridge with rendered prompt
  5. `parseDreamResponse(response)` → parsed
  6. `validateRefinementDoctrine(parsed, directiveRows.length)` — **MUST pass `directiveRows.length` (DB count), NOT `parsed.active_directive_count_before`**
  7. `assignSortOrder(parsed)` — mutates parsed.proposals adding `_sort_order`
  8. Transactionally INSERT all proposals reading `p._sort_order` for the sort_order column
  9. Also remember to apply the 2-min smoke-harness fix from `deferred-items.md` so DRW-04..DRW-12 can run
- Mock-injection contract from Plan 01 (`DREAM_WORKER_MOCK_RESPONSE_PATH`) honored by passing the file's contents directly to `parseDreamResponse` when set — keeps Bridge dispatch out of smoke tests.

**Ready for Plan 05 (admin endpoints + ship):**

- `dream_runs.action_config.sampling` payload shape locked by `SamplingLog` interface — Plan 05's run-detail endpoint can deserialize without guessing.
- `_sort_order` ASC ordering means refinements appear before additions in the UI — shapes the human reviewer's frame.

**Smoke harness status (`bash tests/smoke-48.3.sh`):**

```
DRW-01: [ok] all 5 sub-checks         (Plan 02 — schema)
DRW-02: [ok] all 5 sub-checks         (Plan 02 — schema)
DRW-08: [ok] all 3 sub-checks         (Plan 02 — workflow/scheduler)
DRW-03: [ok] all 3 sub-checks         (Plan 03 — prompt template)  ← flipped GREEN this plan
DRW-04..DRW-12: [warn] skipped (dream-worker.ts not yet built)
```

## Self-Check

Verifying claims before declaring done.

**Files exist:**
- FOUND: `backend/src/services/intellect/dream-prompts/software.md` (81 lines)
- FOUND: `backend/src/services/intellect/dream-sampler.ts` (238 lines)
- FOUND: `backend/src/services/intellect/dream-parser.ts` (176 lines)
- FOUND: `.planning/phases/48.3-software-dream-worker/deferred-items.md`

**Commits exist:**
- FOUND: `1fb6f09` (Task 1 — prompt template)
- FOUND: `2e4b178` (Task 2 — sampler)
- FOUND: `0a7d556` (Task 3 — parser)

**Verify commands:**
- `psql -d porter -tAc "SELECT prompt_path FROM silos WHERE id='software'"` → `backend/src/services/intellect/dream-prompts/software.md` (exact match with file written)
- `cd backend && npx tsc --noEmit` → exit 0 (clean)
- `cd backend && npm run build` → exit 0 (clean)
- `node /tmp/parser-test-48.3-03.mjs` → all 8 manual fixture assertions `[ok]` (compliant parse/doctrine/sort + malformed throws JSON parse failed + doctrine-violation throws at activeCount=6 + passes at activeCount=4 + empty-proposals passes at activeCount=9)
- `bash tests/smoke-48.3.sh` → DRW-01/02/03/08 all green; DRW-04+ warn-skip (worker not yet built — expected, Plan 04 scope)

## Self-Check: PASSED

---
*Phase: 48.3-software-dream-worker*
*Completed: 2026-05-13*
