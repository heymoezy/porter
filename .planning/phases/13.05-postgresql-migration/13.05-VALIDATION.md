---
phase: 13.05
slug: postgresql-migration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-23
updated: 2026-03-23
---

# Phase 13.05 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright (existing) + custom migration script validation |
| **Config file** | `tests/playwright.config.ts` |
| **Quick run command** | `npx tsx scripts/migrate-sqlite-to-pg.ts` (includes row counts + spot-checks + FTS) |
| **Full suite command** | `cd tests && npx playwright test` |
| **Estimated runtime** | ~60 seconds |

---

## Plan/Wave Structure

| Plan | Wave | Files | Requirement | Description |
|------|------|-------|-------------|-------------|
| 01 | 1 | schema.ts, client.ts, config.ts, drizzle.config.ts, migrate-consolidated.ts, package.json, docker-compose.yml, .env.example | PG-01 | Schema rewrite + client swap + consolidated DDL + infrastructure |
| 02 | 2 | 12 service/plugin files | PG-02 | Service + auth plugin SQL conversion |
| 03 | 2 | memory.ts, conversations.ts, health.ts, index.ts | PG-03, PG-01 | FTS tsvector conversion + health endpoint + boot sequence |
| 04 | 2 | 16 route files + seed-templates.ts | PG-02 | Bulk route SQL conversion |
| 05 | 3 | scripts/migrate-sqlite-to-pg.ts, package.json | PG-04, PG-01 | Data migration + validation + cleanup |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && npx tsc --noEmit` (type-check)
- **After Plan 01:** Verify `grep -c pgTable backend/src/db/schema.ts` returns 35+
- **After Plans 02-04:** Verify `grep -rn 'sqlite\.' backend/src/ --include="*.ts" | grep -v node_modules | wc -l` returns 0
- **After Plan 05:** Run full migration script + Playwright tests
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 01-T1 | 01 | 1 | PG-01 | compile | `cd backend && grep -c pgTable src/db/schema.ts && grep -c sqliteTable src/db/schema.ts` | pending |
| 01-T2 | 01 | 1 | PG-01 | compile | `cd backend && grep Pool src/db/client.ts && grep postgresql drizzle.config.ts` | pending |
| 02-T1 | 02 | 2 | PG-02 | grep | `grep -c sqlite backend/src/services/scheduler.ts` returns 0 | pending |
| 02-T2 | 02 | 2 | PG-02 | grep | `grep -rn sqlite backend/src/services/ backend/src/plugins/ --include="*.ts" \| wc -l` returns 0 | pending |
| 03-T1 | 03 | 2 | PG-03 | grep | `grep websearch_to_tsquery backend/src/routes/v1/memory.ts backend/src/routes/v1/conversations.ts` | pending |
| 03-T2 | 03 | 2 | PG-01 | grep | `grep migrateConsolidated backend/src/index.ts && grep db_engine backend/src/routes/v1/health.ts` | pending |
| 04-T1 | 04 | 2 | PG-02 | grep | `grep -c sqlite` on 6 high-traffic route files returns 0 each | pending |
| 04-T2 | 04 | 2 | PG-02 | grep | `grep -c sqlite` on 11 remaining route files + seed-templates returns 0 each | pending |
| 05-T1 | 05 | 3 | PG-04 | script | `npx tsx scripts/migrate-sqlite-to-pg.ts` exits 0 with spot-checks + FTS | pending |
| 05-T2 | 05 | 3 | PG-01 | integration | `curl http://127.0.0.1:3001/api/v1/health \| grep db_engine` | pending |
| 05-T3 | 05 | 3 | PG-01 | compile | `npx tsc --noEmit` + zero sqlite refs in backend/src/ | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

- [ ] `npm install pg @types/pg` — PostgreSQL driver (Plan 01, Task 1)
- [ ] `backend/src/db/schema.ts` rewritten with pgTable (Plan 01, Task 1)
- [ ] `backend/src/db/client.ts` exports pool (Plan 01, Task 2)

*Existing Playwright infrastructure covers regression testing.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual data integrity spot-check | PG-04 | Script does automated spot-check; human reviews edge cases | Review migration script output for any MISMATCH or MISSING entries |
| Server startup confirmation | PG-01 | Requires running server | Start Porter, verify health endpoint, test a few API calls |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
