---
phase: 13.05-postgresql-migration
plan: "03"
subsystem: database
tags: [postgresql, fts, tsvector, websearch_to_tsquery, health, migration]

# Dependency graph
requires:
  - phase: 13.05-01
    provides: pool export from client.ts, migrate-consolidated.ts

provides:
  - memory.ts using websearch_to_tsquery + search_vector for concept FTS (PG-03)
  - conversations.ts using websearch_to_tsquery + search_vector for message FTS (PG-03)
  - health.ts returning db_engine: 'postgresql' and db_connected (PG-01 gate)
  - index.ts calling migrateConsolidated(pool) at boot (consolidated migration boot)

affects:
  - 13.05-04
  - any phase reading health endpoint (db_engine field now present)
  - any route doing FTS on concepts or messages (websearch_to_tsquery pattern)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - websearch_to_tsquery('english', $N) against pre-built search_vector column (not inline to_tsvector)
    - db_engine and db_connected top-level fields in health response for PG gate checks

key-files:
  created: []
  modified:
    - backend/src/routes/v1/memory.ts
    - backend/src/routes/v1/conversations.ts
    - backend/src/routes/v1/health.ts

key-decisions:
  - "websearch_to_tsquery used instead of plainto_tsquery for all user-facing FTS — handles multi-word queries, spaces, special chars safely; to_tsquery throws on 'project manager'"
  - "search_vector column (pre-built tsvector) used instead of inline to_tsvector() — avoids runtime tsvector computation on every query"
  - "db_engine: 'postgresql' added as top-level health field alongside existing database.engine nested field — satisfies PG-01 gate check requirement"

patterns-established:
  - "FTS pattern: search_vector @@ websearch_to_tsquery('english', $N) with ts_rank for scoring"
  - "Health gate pattern: db_engine top-level field for PG gate check detection"

requirements-completed: [PG-03, PG-01]

# Metrics
duration: 7min
completed: 2026-03-24
---

# Phase 13.05 Plan 03: Critical FTS and Boot Sequence Summary

**websearch_to_tsquery FTS on pre-built search_vector columns in memory/conversations routes, db_engine health gate, and consolidated migration boot — index.ts and health.ts already correct from prior work**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-03-24T08:00:00Z
- **Completed:** 2026-03-24T08:02:07Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- memory.ts: FTS now uses `search_vector @@ websearch_to_tsquery('english', $1)` against the pre-built tsvector column (PG-03 complete)
- conversations.ts: All three FTS query sites (search endpoint, conversation list, message count) converted to `search_vector @@ websearch_to_tsquery` (PG-03 complete)
- health.ts: Added `db_engine: 'postgresql'` and `db_connected` as top-level fields in health response (PG-01 gate satisfied)
- index.ts and health.ts pool import: Already correct from Plan 01/02 work — zero changes needed
- TypeScript compilation: zero errors across all 4 files

## Task Commits

Each task was committed atomically:

1. **Task 1: Convert FTS5 queries in memory.ts and conversations.ts to tsvector** - `fd24485` (feat)
2. **Task 2: Update health endpoint with db_engine and rewrite boot sequence in index.ts** - `2a35f85` (feat)

**Plan metadata:** committed with SUMMARY.md update

## Files Created/Modified

- `backend/src/routes/v1/memory.ts` - FTS upgraded from plainto_tsquery to websearch_to_tsquery on search_vector
- `backend/src/routes/v1/conversations.ts` - FTS upgraded from plainto_tsquery to websearch_to_tsquery on search_vector (3 query sites)
- `backend/src/routes/v1/health.ts` - Added db_engine: 'postgresql' and db_connected top-level fields

## Decisions Made

- `websearch_to_tsquery` chosen over `plainto_tsquery` (which was what was found in the files): the plan specification explicitly requires `websearch_to_tsquery` because it safely handles multi-word queries like "project manager" without throwing syntax errors
- `search_vector` column used instead of inline `to_tsvector('english', content)`: pre-built tsvector avoids runtime computation overhead on every query; requires the column to exist (created in migrate-consolidated.ts from Plan 01)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Upgraded plainto_tsquery to websearch_to_tsquery in FTS queries**
- **Found during:** Task 1 (FTS conversion check)
- **Issue:** Both memory.ts and conversations.ts already had sqlite removed and pool added (from prior work), but used `plainto_tsquery` and inline `to_tsvector()` instead of `websearch_to_tsquery` on `search_vector`. Plan spec requires `websearch_to_tsquery` (not `plainto_tsquery`) for user-facing search safety.
- **Fix:** Replaced all 3 query sites in conversations.ts and 2 in memory.ts with `search_vector @@ websearch_to_tsquery('english', $N)` pattern
- **Files modified:** backend/src/routes/v1/memory.ts, backend/src/routes/v1/conversations.ts
- **Verification:** `grep websearch_to_tsquery` confirms 3 matches in conversations.ts, 3 in memory.ts; no `plainto_tsquery` or `to_tsvector` remaining
- **Committed in:** fd24485 (Task 1 commit)

**2. [Rule 1 - Bug] Added db_engine top-level field to health.ts**
- **Found during:** Task 2 (health.ts acceptance criteria check)
- **Issue:** health.ts had `database: { engine: 'postgresql' }` nested but plan requires `db_engine: 'postgresql'` as a top-level field for PG-01 gate check detection
- **Fix:** Added `db_engine: 'postgresql'` and `db_connected: dbStatus === 'up'` as top-level response fields alongside existing nested database object
- **Files modified:** backend/src/routes/v1/health.ts
- **Verification:** `grep 'db_engine' health.ts` returns `db_engine: 'postgresql'`
- **Committed in:** 2a35f85 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs — fields/functions present but wrong variant)
**Impact on plan:** Both fixes align implementations with plan spec requirements. No scope creep. index.ts was already correct (migrateConsolidated + pool import) from Plan 01/02.

## Issues Encountered

None - TypeScript compilation clean, all verification checks pass.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- PG-03 (FTS) and PG-01 (health gate) requirements complete
- memory.ts, conversations.ts, health.ts, index.ts all using pool — zero sqlite references
- Ready for Plan 04: bulk route conversion of remaining routes

---
*Phase: 13.05-postgresql-migration*
*Completed: 2026-03-24*
