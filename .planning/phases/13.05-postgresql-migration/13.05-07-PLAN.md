# Plan 07 — Update Admin Frontend Proxy

**Wave:** 4 (after Plan 06)
**Goal:** Both frontends call Brain. No admin backend needed.

## Tasks

### 7.1 — Update admin frontend vite.config.ts
- Change proxy targets from :5180 (admin backend) to :3001 (Brain Fastify)
- `/api/v1` → `http://127.0.0.1:3001`
- `/api/admin` → `http://127.0.0.1:3001`
- Keep `/api/events` → `http://127.0.0.1:8877` (SSE from legacy porter.py if still needed)

### 7.2 — Verify admin frontend works against Brain
- Login flow: cookie-based auth through Brain
- All /api/admin/* routes respond correctly
- Health dashboard loads
- Users page loads
- Forge pipeline page loads

### 7.3 — Update product frontend proxy (cleanup)
- Product frontend vite.config.ts currently proxies /api/v1 to :3001 and /api to :8877
- Keep as-is (already correct — Brain owns the API)

### 7.4 — Update admin CLAUDE.md
- Update architecture section to reflect new unified backend
- Remove references to admin's own Fastify server
- Document that admin is frontend-only, all API calls go through Brain

## Dependencies
- Plan 06 (admin routes merged into Brain) must be complete

## Verification
- Admin frontend at :5175 fully functional against Brain at :3001
- Product frontend at :5174 fully functional against Brain at :3001
- Zero running admin backend process needed
- Both frontends can login, navigate, perform CRUD
