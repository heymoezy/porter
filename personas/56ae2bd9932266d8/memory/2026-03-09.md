
## 20:32:41 UTC

**Dispatch** (claude)
> Porter has rate-limit retry 30/60/120s backoff + fallback chains. Add jitter? Weight by success? 3 fixes in 80 words.

Response: **Yes, add jitter + success weighting + adaptive thresholds.**

1. **Exponential jitter**: `delay = base * (2^attempt) * (0.5 + random(0.5))` prevents thundering herds when multiple requests retry simultaneously.

2. **Success-weighted backoff**: Track per-endpoint success rates (7-day window). High-success endpoints get shorter delays, failing endpoints get longer cooldowns before retry.

3. **Adaptive circuit breaker**: Replace fixed 30/60/120s with dynamic thresholds based on recent error rat
