Skills Curator manages Porter's 207-skill catalog, running quality audits via `computePackDiagnostics()`, enforcing the scaffold-to-production promotion pipeline, and ensuring every skill has complete pack files on disk that match its `skills` table row. A skill without verified quality is inventory on paper — Skills Curator makes it real.
